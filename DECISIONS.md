# DECISIONS.md — 왜 그렇게 정했나 (기각된 대안 포함)

> `DESIGN.md`가 **무엇을** 정했는지라면, 이 문서는 **왜** 그렇게 정했고 **무엇을 버렸는지**를 남긴다.
> 목적: 나중에(다른 세션·다른 사람이) "이거 이렇게 하면 되지 않나?"라고 이미 폐기된 길을 다시 파는 걸 막는다.
> 새 결정을 내릴 때마다 여기에 한 항목씩 추가한다. (기획 세션 소유)

---

## 제품

### 발견(discovery) 서비스로 전환 — 2026-07-30
- **버림**: "특가 목록" 사이트(목적지를 사용자가 정하고 우리는 가격만 보여줌).
- **채택**: "시간 남는데 어디 싸게?"에 **목적지를 정해주는** 발견 서비스.
- **왜**: 목적지 우선 검색은 스카이스캐너·네이버와 정면 충돌 → 이길 수 없음. 타깃(20~30대 즉흥 여행자)은 애초에 목적지가 없다. 포지셔닝을 뒤집어야 존재 이유가 생긴다.

### 지도만으론 부족 → 카드 피드 병행 — 2026-08-01
- **문제 인식(사용자)**: "지도로 넘어와버리면 결국 출발지·목적지를 정하게 되네."
- **핵심 통찰**: 지도는 **lean-forward**(내가 훑고 고름), 타깃은 **lean-back**("정해줘"). 지도만 두면 결국 검색 UI가 된다.
- **채택**: 지도(무대) + 왼쪽 **"오늘의 발견" 카드 피드**. 카드만 봐도 답이 나오고, 지도는 탐색용.
- **버림**: (a) 지도 단독 (b) 카드 스와이프 단독(틴더식) — 카드만 두면 발견의 공간감이 사라짐.

### 브랜드명이 곧 인터랙션 — "진짜 갈래말래?"
- hero 카드 배지를 "오늘의 한 방" → **"진짜 갈래말래?"**로. 범용 카피 대신 우리만의 목소리.

---

## 인터랙션

### 핀 인코딩: 크기 → **색 농도** (2026-08-01)
- **처음**: 쌀수록 핀을 크게. **버림**.
- **왜**: 실제로 그려보니 크기 편차가 산만하고, 사용자가 "핀 크기가 왜 다르지?"라고 되물음 = 직관적으로 안 읽힌다는 신호.
- **채택**: 크기 고정 + **색이 진할수록 저렴**. 색 스케일은 "현재 화면에 보이는 딜들 사이의 상대값"(거리대가 다르면 절대가격도 다르므로).

### LOD: 지역별 목록이 아니라 **배율 임계값** (2026-08-01)
- **문제**: 일본 소도시(다카마쓰·마쓰야마)까지 다 찍으니 지도가 깨짐. 반대로 빼면 우리 차별점(롱테일)이 사라짐.
- **버림**: "소도시는 아예 제외", "일본만 특별 처리".
- **채택**: 도시마다 `tier: major|minor` + **줌 배율이 임계값을 넘으면 지역 불문 minor 등장**. 일본이든 동남아든 같은 규칙.

### "말래" 버튼 제거 (2026-08-01)
- **왜**: 브랜드명과 딱 맞아 넣고 싶었지만, 실수로 누르면 카드가 사라져 되돌리기 어렵다(파괴적).
- **채택**: `갈래 → 자세히 보기`만. "말래"는 배지 문구로만 살림.

### 전환: 외부 직행이 아니라 **인라인 확장 상세** (2026-08-01)
- **버림**: 카드 클릭 → 바로 예약처로.
- **왜**: 우리 핵심 자산은 신뢰. 확인 없이 외부로 던지면 "가격 다르다"는 배신감이 그대로 이탈로 이어짐.
- **채택**: 클릭 → 카드가 그 자리에서 확장(가격 근거 + 비교 링크 + 광고 고지). hover=컴팩트, click=상세.

### 거리 단계: 버튼 3단계 유지, 자유 줌 기각 — 2026-08-22
- **버림**: 드래그·핀치로 자유롭게 돌아다니는 지도(②), 단계 안에서 제한적으로 움직이는 절충안(③).
- **왜**:
  1. **제품이 "정해주는" 서비스다.** 자유 줌은 "네가 찾아봐"로 한 걸음 더 가는 것이고,
     이미 같은 이유로 카드 피드를 붙였다(지도만 두면 검색 UI가 된다, 2026-08-01).
  2. **모바일.** 지도 드래그와 페이지 스크롤, 핀치와 브라우저 확대가 충돌한다.
  3. **결정적 — 자유 줌이면 오늘 발견한 문제들이 몇 배 어려워진다.** 단계가 3개면 뷰 3개에서만
     라벨·LOD 규칙이 성립하면 되지만, 자유 줌은 **모든 배율·모든 위치에서** 성립해야 하고
     겹침 회피를 매 프레임 계산해야 한다. 성능 여유도 아직 모른다(B17 미실측).
- **채택**: 단계 버튼 3개. 다만 **죽어 있던 `＋/－` 버튼(B1)을 제거하지 않고 단계 스테퍼로 재활용**한다
  (사용자 제안). 익숙한 어포던스를 살리면서 자유 줌은 들이지 않는다. 단계바와 동작이 겹치는 건 의도 —
  단계바는 "몇 단계 중 어디"를 보여주고 `＋/－`는 빠르게 넘긴다.

### 단계 라벨: 지리 이름 → **거리 표현** — 2026-08-22
- **버림**: `가까운 곳` / `＋동남아` / `＋유럽·미주` (지리 이름).
- **왜**: `＋유럽·미주`는 **두 가지로 거짓말**을 하고 있었다 — 미주를 실제로 담지 못했고(B14),
  라벨에 없는 **중동(두바이·도하)·오세아니아(시드니·오클랜드)**를 담고 있었다.
  데이터는 매일 바뀌므로 **지리 이름은 앞으로도 계속 틀린다.** 게다가 목적지를 안 정한 사용자에게
  "동남아를 고르라"는 건 destination-last 포지셔닝과 어긋난다.
- **채택**: `가까운 곳` / `조금 더 멀리` / `아주 멀리`. 상대 거리라 **항상 참**이다.
- **버린 대안(문구)**: 각운 세트(`가까운 데`/`좀 더 먼 데`/`갈 데까지`)는 브랜드명의 "갈"을 살리고
  리듬도 좋았으나 **"갈 데까지"에 막장 뉘앙스**가 섞인다. 마음 상태 세트(`이 근처`/`좀 멀어도 돼`/
  `어디든 상관없어`)는 타깃 심리에 가장 정확했지만 **버튼 라벨로 너무 길다**(특히 모바일).
  → 사용자 선택으로 담백한 세트 채택. **신뢰 사안이 아닌 곳에서도 과하지 않게 간다.**
- **잃는 것(감수)**: 누르기 전에 어디가 나올지 모른다. 대신 **누르면 지도가 즉시 답한다** —
  라벨은 버튼이고 답은 지도다.

### LOD: 배율 임계값 → **"이번 단계가 연 거리대는 전부"** — 2026-08-22
- **버림**: `MINOR_SCALE` 배율 임계값(2026-08-01 채택안). **자기 이름을 배신하는 규칙이었다.**
- **왜**: far(scale 230)에서 임계값 1200을 못 넘어 `minor`가 전부 숨었다. 서울 장거리 딜 20건 중
  12건이 `minor`라 **"유럽·미주" 단계인데 유럽·미주가 8건만** 그려졌다. 화면 밖으로 잘린 2건(B14)보다
  **LOD가 숨긴 12건이 더 컸다.** 단계 이름이 약속한 것을 LOD가 되돌리고 있었다.
- **채택**: 이번 단계가 연 거리대는 **전부**, 이전 단계는 `major`만.
  사용자가 그 단계를 누른 건 **그 거리대를 보겠다는 뜻**이므로 그 거리대를 숨기지 않는다.
- **주의**: `tier`(major/minor) 자체는 유지한다. 도시의 인지도를 나타내는 값이지 화면 밀도 장치가 아니다.

### far 뷰 투영: 중심을 **44° 동쪽으로** — 2026-08-22
- **버림**: 중심 `78°E`(중앙아시아). 왼쪽 절반을 **딜이 0건인 아프리카·대서양**에 쓰고 미주를 잘라냈다.
- **왜 122°E인가**: 모든 딜을 감싸는 **최소 경도 호가 239.5°**이고 그 중심이 **121.8°E**다(빈 구간은
  대서양 120.5°). 계산으로 나오는 값이지 취향이 아니다.
- **고정 배율을 쓰지 않는다**: `preserveAspectRatio="slice"`가 좌우를 잘라내므로 **가시 범위가 창 비율에
  따라 달라진다.** 1280×800에서 상한 189, 세로로 긴 창에서는 110까지 내려간다.
  → 배율은 **무대 실제 폭에서 계산**한다.
- **기각한 대안**: 4단계 분리(`＋`의 누적 의미가 깨짐) · 단계 이름 축소(미주 딜을 영영 못 봄) ·
  미주만 별도 점프 뷰(연속성 상실) · 전 지구 축소(한국이 지나치게 작아짐).

### 필터: 숨김이 아니라 **흐림(dim)** + 매칭 상단 정렬
- **왜**: 숨기면 지도의 밀도·맥락이 무너진다. 흐리면 "조건에 맞는 게 뭔지"는 강조하면서 전체 그림은 유지.
- **추가**: 필터를 켜면 **거리 단계 무관 전 지역**에서 매칭(해변 찾는 사람에게 "가까운 곳" 제약은 무의미).

### hero: 신선도 게이트 → **타이브레이커로 강등** (2026-08-22)
- **버림**: "hero = `fresh`(<24h) 중 할인율 최고"(2026-08-01 결정).
- **왜**: `seen`을 실투입하고 재보니 방문 시점 기준 **`fresh`가 3%**다
  (fresh 3% · ok 45% · stale 52%). 이건 고장이 아니라 **하루 한 번 수집하는 구조의 정상 모습**이고
  분포가 하루 주기로 오르내린다. 결과적으로 **하루 중 상당 시간 hero 후보가 0~1건**이 되어
  게이트가 "가장 믿을 만한 것"이 아니라 **"아무거나"를 뽑는다.**
- **판단 기준을 바꿨다**: "언제 재느냐"가 아니라 **"가장 나쁜 시간대에도 hero가 뽑히는가"**
  (백엔드 세션 제안). 할인율 기준은 항상 뽑힌다.
- **채택**: 할인율 최고 → 가격 낮은 순 → 신선한 순. 신선도는 **배지가 이미 정직하게 말하므로**
  hero 선정까지 이중으로 막을 필요가 없다.
- **감수**: 제주처럼 최고 할인율이 도장 임계값(15%)에 못 미치면 **hero에 도장이 없다.**
  hero 배지는 "오늘 우리가 고른 한 장"이지 "할인 최고"라는 약속이 아니다.

### 딜 0건인 날: 막다른 길 → **안내 + 노선 페이지** (2026-08-22)
- **문제**: `origins`가 비면 화면0에 **핀도 안내도 없었다.** 방문자는 빈 한국 지도만 보고 나간다.
  `DESIGN.md`에 문구만 있고 **구현에 없었다.**
- **채택**: 지도 자리를 안내로 교체 + **노선별 페이지 링크.**
  그날 발견 홈이 줄 게 없어도 **26개 노선 시세는 살아 있다** — 빈손으로 돌려보내지 않는다.
- **역할 분담**: 생성 쪽 방어(수집 실패 시 이전 산출물 유지)는 백엔드 `BE1`이 맡는다.
  실측상 **4일 연속 실패**라야 이 화면이 뜨므로 이건 **마지막 방어선**이다.
  프론트가 데이터 복구를 흉내 내지 않는다.

### "왜 지금" 훅 폐기 — 자리를 없애서 푼다 (2026-08-22)
> 2026-08-01의 "**왜 지금 훅은 데이터로 계산 가능한 것만**" 결정을 **뒤집는다.**
> 그때는 계산 가능한 것이 충분히 있다고 전제했는데, 재보니 아니었다.

- **문제**: 훅 분기 배정을 실측하니 **`경유로 확 싸진 특가`가 피드의 54%(51건)**를 먹고 있었다.
  그런데 `deals.json`에 **직항 대비 가격이 없다.** 즉 "경유해서 싸졌다"는 인과를
  **뒷받침할 데이터가 아예 없는데 절반이 넘는 카드가 그걸 단언**하고 있었다.
  게다가 분기 순서가 잘못돼 **실제 할인율이 있는 38건이 근거 있는 문구를 버리고 근거 없는 문구를 택했다.**
- **버림: 문구만 고치기.** `경유로 확 싸진 특가`를 지우고 다른 걸 채우려 했으나,
  **대안을 지어내는 순간 같은 실수를 반복한다** — 실제로 대안으로 "3시간이면 도착"을 예로 들었다가
  **소요시간 필드도 없다**는 걸 프론트 세션이 잡아냈다.
- **버림: 임계값만 올리기.** 근거 약한 딜을 `{when} 최저가`로 몰면 **94건 중 절반이 같은 문구**가 되어
  `PRODUCT.md`의 "정보 나열형 회피"와 충돌한다.
- **채택: 훅이라는 자리를 없앤다.** 훅의 목적("왜 지금")에 답할 수 있는 건 **할인율뿐이고 딜의 14%에만 있다.**
  나머지 86%를 위해 자리를 유지하는 한 계속 지어내게 된다.
  → 할인율은 **도장**으로, 직항/경유는 **날짜 줄**로 보내고 훅 줄은 지운다.
  **없는 주장을 할 자리 자체를 없애는 것**이 문구를 고치는 것보다 강하다.
- **부수 효과**: 카드에 **직항 여부가 새로 생긴다.** 45건(43%)이 직항인데
  지금까지 화면에 한 번도 안 나왔다 — `경유로 확 싸진 특가`가 경유를 근거 없이 칭찬하는 동안
  **직항이라는 진짜 장점은 한마디도 못 하고 있었다.**

### 도장을 3단계 티어로 — 무지개 없이 "SSR" 만들기 (2026-08-22)
- **사용자 제안**: "할인율이 높으면 딱지가 커지거나 화려해지면 좋겠다. 게임 SSR 카드처럼 무지개 테두리 같은?"
- **버림: 무지개·다색 테두리.** `DESIGN.md`의 **"시그니처색은 코랄 하나, 두 번째 강조색을 만들지 않는다"**와
  **색각이상 안전** 원칙을 동시에 깬다. 그리고 우리 제품에서 다색 네온은 **"AI틱함"으로 읽힐 위험**이 크다.
- **채택: 같은 코랄 안에서 무거워지는 3단계.** 희소성 인코딩이라는 **발상은 그대로 살리고**
  계단을 **색이 아니라 채도·크기·무게·레이어**로 만든다.
  - T1 `15~24%` **테두리** → T2 `25~34%` **채움** → T3 `35%+` **그라디언트+흰 링+후광+광택, 썸네일 위로 떠오름**
  - T3만 **레이어를 이탈**(카드 경계를 넘어 썸네일 위로)한다. 색을 늘리지 않고 "특별함"을 만드는 가장 강한 수단이다.
  - 밝기 차이로도 구분되므로 **색각이상 안전이 유지된다.**
- **왜 3단계인가 — 데이터가 피라미드다.** 실측 **9건 / 4건 / 2건**(전체의 8.7% / 3.8% / 1.9%).
  임의로 나눈 게 아니라 분포가 그렇게 생겼다.
- **T3가 매일 나오지 않는 걸 받아들인다.** 오늘 제주는 최고 할인율이 11%라 **도장이 하나도 없다.**
  **매일 뜨는 배지는 배지가 아니다.**
- **문구는 티어별로 바꾸지 않는다.** `역대급` 같은 말을 얹고 싶어지지만 **역대 데이터가 없다** —
  `COPY.md`의 "주장할 수 있는 것/없는 것"에 걸린다. 시각이 말하게 두고 문구는 숫자만.

### 무지개: **만들어 보고 기각** (2026-08-22)
- 사용자가 원칙을 알면서도 **"한 번만 만들어서 보여달라"**고 했고, 그대로 했다.
  **일부러 못생기게 만들지 않았다** — 비교가 되려면 각 안의 가장 좋은 버전이어야 한다.
  R1(회전하는 conic 링) · R2(홀로그램: 상시 무지개 결 + 흐르는 광택) · R3(전면 무지개) 셋을
  T3 도장에 실제로 입혀 나란히 놓았다(`design/card.html` §07).
- **실물을 보고 사용자가 현행(코랄)을 유지하기로 했다.**
- **이 과정 자체가 결론보다 중요하다.** 안 만들어 봤으면 "무지개면 더 좋았을 텐데"가 계속 남는다.
  **원칙을 근거로 거절하는 것보다, 만들어 보여주고 같이 기각하는 쪽이 결정을 오래 간다.**
- 남은 판단 재료(기각 사유):
  - R3는 **코랄이 완전히 사라져** 다른 서비스처럼 보이고,
    **색각이상에서 무지개는 구분되지 않아** T2↔T3 계단 정보가 통째로 사라진다.
  - R1은 링이 계속 돌아 **시선을 붙잡는다.** 피드에 T3가 두 장 뜨면 서로 싸운다.
  - R2가 실물 포일 카드에 가장 가깝고 코랄도 살아남지만, **현행 대비 얻는 게 크지 않았다.**
- 시안은 **지우지 않고 `design/card.html` §07에 "기각" 표시로 남긴다.** 다음에 같은 얘기가 나오면 그걸 본다.

### `{N}일 중 최저`: 정의를 바꿔 41% → 10%로 (2026-08-22)
- **처음 요청한 정의(`low` = 오늘 포함 최저)는 틀렸다.** 백엔드 실측으로 **41%에 붙는다**는 게 드러났다.
- **왜 그렇게 흔한가**: `deals.json`의 `price`는 **이미 최근 3~4일 중 최저가**다.
  최저를 뽑아놓고 "최저냐"를 묻는 **동어반복** 구조였다. 내 정의의 결함이다.
- **하한선을 올려도 안 된다** — 14일로 올려도 26%다. **도장이 100%에 붙어 무의미했던 문제의 재판**이었다.
- **채택(백엔드 제안)**: `low`를 **오늘 제외 이전 기간 최저**로 정의.
  그러면 프론트가 `price < low`로 **진짜 신기록**을 판정하고 **낙폭까지 계산**할 수 있다.
  임계값이 계약이 아니라 **프론트 파생**이 되어 나중에 조정도 쉽다.
- **신기록만으론 부족해서 낙폭 5%를 더 걸었다** — 신기록만 보면 34%인데
  `0.8% 더 싼` 것까지 "최저"라고 외치게 된다. **기술적 사실이지만 사용자에겐 같은 가격**이고,
  그런 게 문구의 신뢰를 깎는다. **5% 이상으로 좁히면 10%** — 도장(14%)과 비슷한 희소성이다.
- **교훈**: 요청하기 전에 **"몇 %에 붙는지"를 먼저 재야 한다.**
  희소성이 곧 의미인 장치(도장·배지·강조 문구)는 **비율이 설계의 일부**다.
  이번엔 백엔드가 구현 전에 재줘서 헛구현을 막았다.

### URL 해시로 상태를 남긴다 — 공유가 비로소 가능해진다 (2026-08-22)
- **문제**: `discover.js`에 `location`·`history`·`hashchange`·`localStorage`가 **한 번도 없었다**(검증 0건).
  새로고침하면 출발지가 날아가고, **딜 하나를 가리키는 링크를 만들 수 없었다.**
  백로그의 "커뮤니티 시딩(뽐뿌 등)"은 **그런 링크가 있어야 성립**하는데 홈 주소밖에 보낼 게 없었다.
- **채택**: `#{허브}` / `#{허브}-{목적지}`, 전부 `pushState`.
  - **해시를 쓰는 이유**: 정적 사이트라 **서버 리라이트가 없다.** 경로(`/deal/SEL-DAD`)는 404가 난다.
  - **키가 유일함을 확인하고 정했다** — `(허브, 목적지)`가 딜 1건에 1:1(99/99, 중복 0).
    백엔드가 그 단위로 최저가 1건만 내보내기 때문이다. **계약이 URL 설계를 가능하게 했다.**
- **버림: 필터·정렬·거리 단계를 URL에 넣기.** 공유받은 사람에게 **남의 필터를 강요**하게 된다.
  다만 **거리 단계는 URL에 없어도 상태는 바꾼다** — `#SEL-LAX`로 들어왔는데 `가까운 곳` 단계면
  핀이 화면 밖이라 F15가 재발한다.
- **닫기를 `history.back()`으로** 처리한다. 안 그러면 URL과 화면이 어긋난다.

### 사라진 딜: 오류가 아니라 **다른 허브 제안** (2026-08-22)
- 딜은 매일 바뀌므로 어제 공유한 링크가 오늘은 없을 수 있다. **딥링크를 도입하면 반드시 생기는 상태**다.
- **버림: 오류 화면.** 출발지(허브)는 여전히 유효하므로 **정상 피드를 보여주고 안내만 얹는다.**
- ⭐ **채택: 같은 목적지의 다른 허브 딜을 제안한다.**
  실측 — **목적지 59곳 중 31곳(53%)이 여러 허브에서 간다.** 절반은 빈손으로 안 돌려보낸다.
- **도시명은 다른 허브에 딜이 있을 때만 쓴다.** 딜이 아예 없으면 한글 도시명을 알 방법이 없다
  (`deals.json`에만 있다). **IATA 코드를 화면에 내보내지 않는다.**

### 배지와 태그를 **형태로 가른다** (2026-08-22)
- **문제(사용자)**: "태그랑 도형이 같아서 구분이 안 된다."
  확인해 보니 **날짜 배지·직항 배지·태그가 전부 알약**(radius 99)이었다. 색도 비슷했다.
- **역할이 다르다**: 배지는 **"이 딜의 상태"**(언제·어떻게 가나), 태그는 **"목적지가 어떤 곳인가"**다.
- **채택**: 배지는 알약으로 두고 **태그를 각진 사각(radius 5) + 테두리만**으로.
  **모양과 채움 둘 다** 바꿔 확실히 갈랐다(한 축만 바꾼 안보다 낫다).
  배경을 비워 **무게도 낮아져** 카드 맨 아래 보조 정보라는 위계와 맞는다.
- **버림: 텍스트만.** 가장 깔끔하지만 **"고를 수 있는 것"이라는 신호가 사라진다** —
  "화면에 보이는 태그는 고를 수 있어야 한다"는 원칙을 시각적으로 배신한다.
- **날짜 배지 코랄은 되돌렸다**(사용자 선택). 한때 중립색으로 내렸는데,
  코랄이 날짜 배지·가격·도장 셋에 걸려도 **도장이 기울어져 있고 14%에만 붙어** 형태로 구분된다.

### 직항/경유 자리: 태그 줄 → **가격 줄 우측** (2026-08-22)
- **문제(사용자)**: "직항이 태그에 같이 들어가는 게 별로다."
  맞는 지적이었고, 확인해 보니 **내 스펙이 날짜 줄과 태그 줄 양쪽에 두어 중복**이기까지 했다.
- **왜 태그 줄이 틀렸나**: 태그는 **"어떤 곳인가"**, 교통은 **"어떻게 가는가"**다.
  종류가 다른 정보를 한 줄에 두면 **배지가 태그처럼 보여 성격이 흐려진다.**
- **버림: 날짜 줄 끝.** 여정 정보끼리 묶이는 건 논리적으로 맞지만
  `다음 달 9/12(토)~9/15(화) · 3박4일 · 직항`은 **카드 폭 340px에서 너무 길다.**
- **채택: 가격 줄 우측.** 사용자가 실제로 저울질하는 쌍이 **"이 값에 직항이냐"**이고,
  그 자리가 비어 있었다. 가격 옆이라 반드시 읽힌다.
- **자리는 하나, 무게는 셋** — 위치를 통일하되 중·장거리 직항만 배지로 올린다.

### 직항 배지: 옅은 배경 → **청록 채움 + 기호** (2026-08-22)
- **문제**: 첫 안은 `--soft` 배경 + `--coast` 글자였는데 **"잘 안 보인다"**(사용자).
- **채택**: `--coast` **채움** + 흰 글자 + **비행기 기호 `✈`**.
  - **코랄을 쓰지 않는다** — 도장과 색이 겹치면 두 배지가 같은 종류로 읽힌다.
    `--coast`는 이미 지도·차트에 쓰는 중립색이라 **새 강조색을 만드는 게 아니다.**
  - **기호를 하나 들인 이유**: 카드를 훑을 때 **글자를 읽기 전에 걸린다.** 스캔 속도가 오른다.
    `✈`는 변이 선택자 없이 써서 **텍스트 글리프로 렌더**되고 글자 색을 상속한다(색 이모지가 아니다).
- **기각**: 흰 배경 + 굵은 테두리(B4) — **도장과 형태가 비슷해** 종류 구분이 흐려진다.

### 배지는 드문 좋은 소식에만 — 경유는 배지가 아니다 (2026-08-22)
- **사용자 제안**: "경유 같은 것도 딱지로 붙이고 태그처럼 쓰자. 근데 경유는 장점이 아니니까 다른 방식으로."
- **채택**: 뒷부분이 맞다. **배지는 자랑, 텍스트는 사실**로 나눈다.
  - **중·장거리 직항** → `직항` **배지**. 중거리 36%·장거리 14%만 직항이라 **드물다.**
  - **근거리 직항** → 날짜 줄 텍스트. **75%가 직항이라 당연**하고, 당연한 건 배지가 아니다.
  - **경유** → 날짜 줄 텍스트(`--sub`). **배지 아님.**
    장점이 아닌 것을 자랑 형식에 담으면 **시각 언어가 거짓말을 한다.** 감추지도 않는다.
- **왜 전부 배지로 만들지 않나**: 교통은 **모든 딜에 있는 사실**이라 배지로 만들면 100%가 배지를 달고
  **배지라는 장치가 죽는다.** 도장을 100%에서 14%로 줄인 것과 같은 이유다.
- 결과: 배지 **0개 72% · 1개 24% · 2개 4%.**

### 할인 도장: 100%에 붙던 것을 14%로 (2026-08-22)
- **문제**: 도장이 `특가 {N}%↓`로 **무조건** 붙었다. 실측하니 `0%` 23건(24%)·`1~9%` 48건(51%) —
  **넷 중 셋이 10% 미만 할인인데 전부 "특가"를 달고 있었다.** "특가"가 아무것도 뜻하지 않는 상태.
- **채택**: `discount ≥ 15%`일 때만. 실측 **14%**의 딜에만 붙는다.
  임계값 미만이면 **도장이 없고, 카드 우상단이 비는 게 정상**이다.
- **왜 15%인가**: `≥20%`면 11%, `≥25%`면 6%로 너무 희소해 도장이라는 장치가 죽는다.
  15%는 항공권에서 체감되는 폭이면서(40만원이면 6만원) **일곱 장 중 한 장**이라 눈에 띈다.
- 딜을 빼지는 않는다. **할인율 0이어도 "다낭 21만원"은 좋은 발견이다.**
  우리가 파는 건 발견이지 할인율이 아니다. 문제는 딜이 아니라 **그걸 "특가"라고 부르는 것**이었다.

### "특가": 임계값 통일이 아니라 **단어 분리** (2026-08-22)
- **문제**: 제품 안에 정의가 둘. 발견 홈 `discount ≥ 25`, 노선 페이지·알림 `중앙값의 65%`(=`35%↓`).
- **버림: 임계값을 35%로 통일.** 그러면 발견 홈에서 "특가"라 부를 딜이 **104건 중 2건**뿐이다.
- **채택: 발견 홈에서 "특가"라는 단어를 쓰지 않는다.** 둘은 **재는 대상이 다르다** —
  노선 쪽은 그 노선 시세의 **급락(사건)**, 발견 홈은 오늘 최저가와 평소 시세의 **편차**다.
  같은 이름을 붙인 게 잘못이지 같은 임계값을 써야 하는 게 아니었다.
  발견 홈은 `평소보다 N%↓`라는 **서술만** 쓰고, "특가"는 노선 페이지·알림에 남긴다.

### 필터를 켜면 지도를 `아주 멀리`로 — 그리고 라벨만 걷어낸다 (2026-08-22)
- **처음 본 문제**: 필터를 켜면 전 도시가 한꺼번에 찍혀 지도 밀도가 무너진다(B6).
- **재측정으로 드러난 진짜 문제**: 밀도가 아니라 **매칭 딜이 화면 밖에 있다는 것**이었다.
  `가까운 곳`에서 `해변`을 켜면 매칭 22건 중 **화면 안은 10건뿐**이다. 피드엔 뜨는데 지도엔 없다 —
  PH1에서 "**피드에 있는 딜은 지도에서 찾을 수 있어야 한다**"고 못 박은 F15가 필터로 재현된다.
- **채택 ①**: 필터를 켜면 **`아주 멀리` 뷰로 전환**하고, 단계바를 `전 지역에서 찾는 중`으로 **치운다.**
  버튼을 비활성으로 남기지 않는 이유는, 필터 중에는 **"거리 단계"라는 개념 자체가 성립하지 않기** 때문이다.
  필터를 끄면 직전 단계로 복귀한다.
- **버림: 비매칭을 LOD로 추려 밀도를 낮추기.** 실측하니 59개 → 44~49개로 **10~15개밖에 안 줄었다.**
  기계는 늘고 효과는 없다.
- **채택 ②: 라벨을 매칭에게만 준다.** **밀도를 만드는 건 점이 아니라 글자다.**
  핀은 전부 흐리게 남겨 "숨김이 아니라 흐림" 원칙을 지키면서, 라벨 경쟁을 매칭끼리로 좁혔더니
  **매칭 라벨이 거의 다 살아남았다**(해변 22/22 · 자연 14/14 · 문화 16/16 · 도시 27/32).

### 접힌 필터 도크: 개수 → **조건 문구** (2026-08-22)
- **버림**: `필터 · 2`. **접힌 도크의 목적은 "지금 뭐가 걸려 있나"를 알려주는 것인데 개수는 그 답이 아니다.**
- **채택**: `해변 · 50만 이하`처럼 조건을 그대로. 최대 2개 + `외 N`.

### 예산 슬라이더: 고정 상한 → **데이터에서 만드는 범위** (2026-08-22)
- **버림**: 고정 `min 10만 / max 100만`. 실제 최고가가 **191만**이라 상한 초과 9건을
  **걸러낼 수만 있고 되살릴 수 없었다.**
- **버림: 상한만 200만으로 올리기.** 숫자 하나 바꾸는 최소 수정이지만, 데이터가 바뀌면 또 어긋난다.
  같은 버그를 나중에 다시 만난다.
- **버림: 로그 눈금.** 딜의 91%가 100만 이하라 선형 트랙의 뒤쪽이 헐겁다. 로그로 펴면 저가 구간
  해상도가 좋아지지만, **슬라이더를 반쯤 옮겼는데 금액이 4배 뛰는 걸 사용자가 이해하지 못한다.**
  트랙이 실제 가격 범위를 정직하게 나타내는 쪽을 택했다.
- **채택**: `min`·`max`를 **현재 출발지 데이터에서** 5만 단위로 올려 만든다.
  - `min`을 최저가에서 **올림**하는 게 포인트다 — 슬라이더를 맨 왼쪽까지 내려도 **최소 1건이 남는다.**
    조작만으로 빈 화면을 만들 수 없다.
  - **최대치는 `제한 없음`으로 표기**(필터 꺼짐). 부수 효과로 `155만` 같은 어색한 숫자가 화면에 안 나온다.

### 태그 어휘 확장: 12종 → **24종**, 상위(필터)/하위(표시) 분리 (2026-08-22)
> 같은 날 내린 "카드 태그도 필터와 같은 6종으로 제한" 결정을 **부분적으로 뒤집는다.**
> 그 결정은 **필터 관점에선 옳았지만 카드 표현을 망가뜨렸다.**

- **문제(사용자)**: "태그가 조금 더 다양하고 관광지 장점을 잘 살렸으면."
- **실측**: `해변` 하나만 붙은 도시 **10곳**(몰디브·싼야·푸켓·괌·세부…), `도시` 하나만 **8곳**(두바이·나고야…).
  태그 1개짜리가 **38%**. **몰디브와 싼야가 같은 태그였고, 두바이와 나고야가 같은 태그였다.**
- **버림: 표시 규칙만 바꾸기.** 숨겨둔 하위 태그(`휴양`·`쇼핑` 등)를 우선 표시하도록 시뮬레이션했더니
  **제주·다낭·오키나와·푸꾸옥·싼야가 전부 `휴양 · 해변`**으로 똑같았다. 조합은 20→26가지에 그쳤다.
  **어휘가 빈약한 게 원인이라 표시 규칙으로는 못 고친다.**
- **채택: 어휘를 24종으로 늘리고 84개 목적지를 다시 배정한다**(`TAGS.md`).
  - **상위 6종 = 필터**(변경 없음), **하위 18종 = 카드 표시**.
  - **불변식: 하위는 반드시 상위를 동반한다.** 그래야 "보이는 태그는 고를 수 있어야 한다"가 유지된다.
    `design/build_tags.py`가 매번 검증한다.
- **개수를 고정하지 않는다** (2026-08-22 사용자 제안): **하위 전부 + 상위 1개**, 최대 4개.
  **태그 개수 자체가 "즐길 게 얼마나 많은가"라는 신호**가 된다 —
  `싼야`·`프랑크푸르트`는 2개, `도쿄`·`파리`·`두바이`는 4개.
  고정 2개였을 땐 데이터가 이미 갖고 있던 그 신호를 **잘라 버리고 있었다**(원본은 2~6개로 가변).
  실측 분포 **2개 37곳 · 3개 26곳 · 4개 21곳**.
- **표시 규칙도 실측으로 정했다**: "하위 우선 최대 2개"가 고유 표시 **86%**,
  "배열 앞 2개 그대로"는 **50%**였다(상위가 앞에 와서). 순서 조정 후 최종 **99%**(78곳 중 77곳).
- **표시 태그는 희소할수록 좋다** — 필터 어휘가 희소하면 빈 결과가 뜨지만(그래서 상위는 6종),
  표시 태그는 드물수록 그 도시를 구별한다. **`폭포`가 토론토 한 곳뿐인 건 결함이 아니라 목적이다.**
- **시즌 표현은 넣지 않았다** — `벚꽃`·`단풍`·`설경`은 계절을 타서 데이터가 썩는다.
- **`사찰` 대신 `사원`** — 이스탄불 모스크·델리 힌두사원까지 담아야 한다.

### 분위기 칩: 5종 → **6종**, 카드 태그도 같은 6종으로 (2026-08-22)
- **문제**: 통제 어휘는 12종인데 필터 칩은 5종이라, **딜의 72%가 고를 수 없는 태그를 카드에 달고 있었다.**
  `이스탄불`×3·`델리` 4건은 칩 5종이 하나도 안 붙어 **분위기 필터를 켜면 영영 매칭되지 않았다**
  (필터가 흐림 방식이라 사라지지도 않고 계속 흐린 채 남는다 — 프론트 세션 실측).
- **버림 ①: 칩을 12종 전부로.** `야시장` 1곳(타이베이)·`트레킹` 1곳(카트만두)·`액티비티` 3곳뿐이라
  **거의 항상 빈 결과가 뜨는 칩**이 된다(백엔드 실측).
- **버림 ②: 카드 태그를 클릭하면 그 태그로 필터.** 12종을 다 고를 수 있게 되지만,
  카드 클릭(=확장 상세) 안에 또 클릭이 생겨 모바일 터치 영역이 엉킨다.
- **채택: 칩 6종 + 카드 노출도 같은 6종.** `해변`·`도시`·`미식`·`자연`·`문화`·`온천`.
  **원칙은 "화면에 보이는 태그는 고를 수 있어야 한다".**
- **왜 그 6종인가 — 취향이 아니라 중복 측정이다.** 나머지 6종은 상위 태그에 거의 완전히 포섭된다:
  `휴양`은 **100%가 `해변`**(정보량 0), `쇼핑` 95%가 `도시`, `유적` 100%가 `문화`,
  `야시장` 100%가 `미식`, `트레킹` 100%가 `자연`. 즉 12종은 실제로 **6개 축**이다.
- 커버리지 **100%**(현행 5종은 96%). `온천`은 7건뿐이라 한 번 뺐다가, **"온천 가고 싶다"는 검색 의도가
  명확한 반면 `문화`는 모호**하다는 이유로 되살렸다. 6종이면 둘 다 담긴다.
- **계약은 안 바꾼다.** `CONTRACT.md`의 어휘 12종은 그대로 두고 **화면에서만 걸러낸다** —
  백엔드 작업을 부르지 않는 선에서 풀 수 있는 문제였다.

### 필터 위치: 헤더 → **지도 우측 하단 도크** (2026-08-01)
- **왜**: 헤더에 두니 필터가 결과(피드+핀)와 멀고 사이트가 무거워 보임. 지도앱 관행대로 지도 위 도크가 자연스러움.
- **날짜 프리셋은 보편 표현만**: `이번 주`/`이번 주말`/`다음 달` + 달력. **"광복절 연휴" 같은 시즌 한정 표현 금지**(지금만 유효해서 코드가 썩는다).

---

## 디자인

### "AI틱함" 탈출 (2026-07-31 ~ 08-01)
- **문제(사용자)**: "사이트가 너무 AI틱해. 사람이 만든 것처럼 예뻤으면."
- **버림**: 손글씨 폰트(Gaegu/나눔펜) — "전문성이 떨어져 보임". 크림색+세리프+코랄이라는 AI 기본 조합.
- **채택**: **Pretendard 900 + 시그니처 코랄(#F2603F)** 단일. 손맛은 악센트로만(로고 비행운, 특가 도장의 기울기).
- **교훈**: 개성은 폰트 장난이 아니라 **크기 대비·색·지도**에서 나온다.

### 지도: 직선 실루엣 → **실측 해안선(50m)**
- **버림**: 해안선을 단순화한 "캐주얼 직선 실루엣"(뾰족해 보이거나 "부서져" 보였음), 도트맵(너무 흐림).
- **채택**: Natural Earth **50m** + DP 단순화(161KB). **왜 50m**: 110m엔 제주·오키나와·괌·발리 같은 **섬이 아예 없어서 핀이 바다에 뜸**.

### 비교 패널 중립화 (2026-08-06)
- **버림**: 스카이스캐너만 코랄로 강조 + "중립/전체 비교" 태그 노출.
- **왜(사용자)**: "중립 비교라면서 왜 하나만 빨간색이야?" — 강조 자체가 중립성을 깨뜨림.
- **채택**: 4곳 동등한 흰 버튼, 태그 숨김(데이터 필드는 유지).

---

## 데이터 · 신뢰

### 가짜 가격 그래프 제거 (2026-08-06)
- **문제**: 확장 상세의 "최근 가격 추이" 스파크라인이 **목업 더미 데이터**였음(하드코딩된 배열).
- **왜 심각**: 신뢰가 유일한 자산인 서비스에서 가짜 차트는 치명적.
- **채택**: 삭제 후 **실데이터** "평소 시세(중앙값) vs 발견가" 막대로 교체. 진짜 30일 추이는 히스토리가 쌓인 뒤에.

### 표시가는 "발견가", 근사치 표기는 **거부** (2026-08-06)
- **버림**: `₩142,522` → `"14만원대~"` 같은 근사 표기.
- **왜(사용자)**: "정확한 가격은 어그로로 좋아. 문제는 **신뢰성 있는 어그로**를 끄는 것."
- **채택**: 정확한 숫자 유지 + **"발견가 · 스캔 시점"** 라벨 + "실시간 최저가는 각 사이트에서 확인" 고지.

### 실시간 비교 엔진은 만들지 않는다
- **왜**: 클릭 시점에 여러 소스 실시간 조회 = 서버 + 각 소스 제휴 + 안티봇. 스카이스캐너가 하는 일이고 $0 정적 사이트로 불가능. 브라우저에서 직접 읽는 건 CORS·안티봇으로 막힘.
- **채택**: **"발견은 우리, 최저가 비교는 메타서치로 handoff"**. 비교 링크를 나란히 주고 사용자가 고른다.
- **미래 옵션(보류)**: Cloudflare Workers 무료 티어로 Travelpayouts 실시간 조회 — 서비스 검증 후에.

### 신선도(found_at) — 하드 필터가 아니라 신뢰 신호 (2026-08-06, **스펙 확정 2026-08-22**)
- **측정 결과**: 수집 시점 기준으로도 `found_at` ≤24h는 **31%**뿐(≤48h 53%, ≤72h 78%).
- **한계**: 1일로 조이면 딜의 2/3가 사라지고 **소도시 롱테일이 먼저 잘림**. 게다가 캐시 신선도는 **남들이 검색해야** 갱신되므로 우리가 더 자주 호출해도 못 올린다.
- **방향(합의된 설계, 미구현)**: 삭제 필터 대신 **"N시간 전 발견" 배지 + 신선순 정렬 신호**로 쓰고, 아주 오래된 것만 제외. → 딜 수 유지 + 어그로 유지 + 신뢰 부여.

---

### 신선도 배지 스펙 확정 — 2026-08-22
> 위 "신뢰 신호로 쓴다"는 방향을 **구현 가능한 스펙**으로 확정. 무엇을 만드는지는 **`SPEC.md` §CH3 "신선도 배지"**.

- **재측정(2026-08-20 실데이터, 딜 143건)**: `<3h` 21% · `<24h` 36% · `<48h` 54% · `<72h` 74% · `<5일` 97% · `>5일` 0%.
  08-06 측정보다 조금 나쁜데, 원인은 **"(허브,도시)당 최저가 1건" 중복 제거가 오래된 캐시를 우대**하기 때문이다
  (싼 가격이 오래 남아 있을수록 이긴다). 즉 우리가 호출을 늘려도 이 편향은 사라지지 않는다.

- **어떤 시각을 보여주나: `fetched_date`(우리 스캔) 대신 `found_at`(소스 관측)** — 후자가 더 나쁜 숫자(최대 5일)인데도 채택.
  **왜**: 사용자가 알고 싶은 건 "이 가격이 언제 본 가격이냐"지 "너희가 언제 일했냐"가 아니다.
  유리한 숫자를 고르는 순간 배지는 신뢰 장치가 아니라 마케팅이 된다.

- **버림 ①: 3단계 신호등(초록/노랑/빨강).** 코랄 단일 시그니처와 색각이상 안전 조합을 동시에 깨고,
  무엇보다 딜의 1/4에 빨간 딱지를 붙이는 건 **우리 입으로 "이거 못 믿어요"를 외치는 것**. 카드 온도가 죽는다.
  → 채택: 신선한 것만 코랄 점으로 조용히 강조, 나머지는 명도·불투명도 차이로만.

- **버림 ②: 배지를 카드에 새 요소로 추가.** 카드는 이미 사진·도시·가격·날짜·도장·"왜 지금"·태그로 포화.
  → 채택: **신선도는 딜이 아니라 가격의 속성**이므로 기존 가격 밑 라벨 `발견가 · 스캔 시점`의
  "스캔 시점"을 실제 값으로 **치환**한다. 새 픽셀 0.

- **버림 ③: 정렬 토글에 "신선순" 추가 / 필터 도크에 신선도 항목 추가.**
  **왜**: 아무도 "신선한 항공권"을 찾아 여행하지 않는다. 신선도는 **사용자의 욕구가 아니라 우리의 품질 문제**이고,
  품질 관리를 사용자에게 위임하는 UI는 책임 전가다. → 채택: 뒤에서 처리 — **hero는 `fresh`에서만 뽑고**,
  정렬은 **동점일 때만** 신선한 쪽을 올린다. 토글은 3종(가성비·임박·할인율) 그대로.

- **버림 ④: 핀에 신선도 인코딩.** 핀 색은 이미 `가격`이다(크기 인코딩을 폐기하며 색으로 몰아준 결정 참조).
  두 번째 색 채널은 첫 번째를 읽을 수 없게 만든다.

- **버림 ⑤: 백엔드가 "3시간 전" 문자열을 구워서 내려주기.** 정적 사이트는 하루 한 번 생성 → 다음 날 방문자에게 거짓말.
  → 채택: 백엔드는 **절대 시각(`seen`)만**, 표기는 브라우저가 방문 시점에 계산. 캐시된 페이지도 **알아서 나이를 먹는다**.

- **하드 컷은 7일 하나만.** 3일로 조이면 딜 26%와 소도시 롱테일이 먼저 잘려 서비스의 차별점이 사라진다.
  실측 최대가 5일이므로 7일 컷은 **오늘 기준 손실 0건** — "유령 가격"만 막는 안전선이다.

- **계약 영향**: `deals.json`에 `seen`(ISO8601) 필드 **추가**(추가라 안전). `found_at` 원본이 **UTC**라
  그대로 쓰면 모든 딜이 9시간씩 늙어 보인다 → 백엔드가 KST로 정규화해 오프셋을 붙여 내려준다.

- **근거 정정 (2026-08-22, 백엔드 세션)**: 처음엔 "최대 나이 80h = 3일 수집 창 + 9h"를 UTC의 근거로 들었으나
  **이 논증은 틀렸다.** 수집 필터가 `timedelta.days`(내림)를 써서 실제 컷이 72h가 아니라 **96h**이므로,
  80h는 타임존과 무관하게 정상 범위다. 올바른 근거는 **최소 나이**다 — UTC로 해석하면 매일 가장 신선한 값이
  `0h` 부근에 붙고, KST로 해석하면 8.8h가 되며 최대 나이가 필터 컷을 넘어 저장 자체가 불가능해진다.
  **결론(UTC)은 그대로, 근거만 교체.** 교훈: 상한값의 우연한 일치보다 **하한값이 0에 붙는지**가 강한 신호다.


## 수익 · 제휴

### Trip.com 제휴는 **거절됨** (트래픽 3개월 조건)
- 현재 승인된 유일한 수익원은 **Aviasales**(Travelpayouts, ~1.1~1.6%, 영어 UX).
- `affiliates.py`의 Trip.com 코드(tp.media 래퍼)는 **대기 상태로 유지** — 승인 시 env 4개만 넣으면 활성화.
- **닭과 달걀**: 승인 조건이 트래픽인데 트래픽이 없다 → 비교 패널로 UX를 먼저 세우고 트래픽을 모은 뒤 재신청.

### 영어 예약처는 **수수료가 실제로 날 때만** 노출 (2026-08-06)
- **왜(사용자)**: "한국인 전용 사이트인데 영어 Aviasales로 보내는 게 맞나?"
- **채택**: `TP_MARKER`가 설정된 경우에만 Aviasales를 비교 목록에 추가. 마커가 없으면(=수수료 0) 숨긴다. 한국어 4곳(스카이스캐너·네이버·구글·Trip.com)만 노출.

### 이메일 알림은 v2로 연기
- 파이프라인(구독·발송)은 이미 있지만 네비에서 제외. 지금은 **발견 경험**에 집중.

---

## 프로세스

### 디자인 먼저, 코드는 나중 (2026-07-30)
- **문제**: "코딩 → 보고 반응(밤티다/AI틱) → 갈아엎기"가 반복되어 비효율.
- **채택**: 기획·목업(HTML 시안)으로 룩앤필 합의 → 확정 후 구현. `design/` 폴더의 목업이 그 산출물.

### 3세션 분업 (2026-08-06)
- 기획·프론트·백엔드를 **git worktree**로 분리. 프론트↔백은 `CONTRACT.md`(deals.json)로만 소통.
- **왜**: 각 세션이 좁은 컨텍스트에 집중할수록 결과물이 좋아진다. 파일 담당 구역을 나눠 충돌을 구조적으로 방지.

## 2026-09-01 — 도장 임계값 `15/25/35`를 유지한다 (뒤집힘을 알고도)

**결정**: T1 `15~24%` / T2 `25~34%` / T3 `35%+`를 그대로 둔다.

**발단**: 09-01 데이터에서 T3(5건)가 T2(3건)보다 흔했다. 가장 화려한 등급이 중간보다
잦으면 희소성 신호가 깨진다고 보고, "할인율 분포의 `25~34%` 골짜기가 여러 날 재현되면
임계값을 옮긴다"를 기준으로 걸었다. 백엔드가 30일 이력으로 검증해줬다.

**기준이 발화하지 않았다**: 30일 합산에 골짜기가 없다(139·157·68·50·30·33). 오늘의
`2/1/3/2`는 하루 표본의 흔들림이었다. 여기서 40%로 옮겼다면 **오늘 데이터에 맞춘 것**이 됐다.

**다만 뒤집힘은 구조적이었다**: 원인은 임계값이 아니라 표본 크기다. T2·T3가 하루 3.9·2.1건
이라 어느 쪽이 흔들려도 순서가 바뀐다. 30일 합계로는 296:118:63으로 멀쩡한데 특정 날
화면에서는 10%(3/30일) 확률로 무너진다. 즉 오늘 본 현상은 열흘에 한 번 재발한다.

**그래도 옮기지 않는 이유 — 부재가 뒤집힘보다 비싸다**

  15/25/35 (유지)   뒤집힘 3/30일   T3 없는 날  5일(17%)   T3 2.1건/일
  15/25/40          뒤집힘 0일      T3 없는 날 10일(33%)   T3 1.1건/일
  15/30/45          뒤집힘 0일      T3 없는 날 18일(60%)   T3 0.5건/일

뒤집힘은 하루 안에서 거의 안 보인다. 사용자는 하루치 화면만 보고 등급별 건수를 세지 않는다.
할인율순 정렬에서 T3가 앞에 몰려도 위계는 단조 감소라 어색하지 않고, "오늘 35%+가 5건
있다"는 사실이지 거짓이 아니다. 카드 하나의 의미는 절대 안 깨진다.

반면 부재는 누적된다. 재방문자에게 "화려한 도장을 본 적이 없다"로 쌓이고, 그 등급을 위해
만든 시각 자산이 3일 중 1일은 안 쓰인다. 이 관점은 백엔드가 짚어줬다.

**그리고 지금은 임계값을 만질 때가 아니다**: discount의 기준인 median이 기간 창 없이 전체
이력을 쓴다(백엔드 BB4). 이력이 길어질수록 중앙값이 옛 가격에 끌려가 할인율이 과대평가된다.
지금 맞춘 눈금은 BB4 수정 후 다시 맞춰야 한다. 재는 자를 고치기 전에 눈금을 옮기지 않는다.

**기각한 대안**
- `15/25/40` — 뒤집힘은 0이 되지만 T3 부재가 17%→33%로 두 배. 위 이유로 기각.
- `15/30/45` — T3가 60%의 날에 안 뜬다. 시각 자산이 사실상 사장된다.
- 순위 기반(상위 N%를 T3로) — 날마다 기준이 달라져 "35% 싸다"는 절대 의미가 사라진다.
  같은 이유로 이전에도 상대 표현을 기각했다.
- T2·T3 병합(2단) — 뒤집힘이 구조적으로 불가능해지지만, 사용자가 좋아한 3단 위계를
  보이지도 않는 통계 인공물 때문에 없애는 셈이다.

**트리거**: BB4 정리 후 30일 분포를 다시 받는다. T3가 피드의 2%를 꾸준히 넘으면 재검토.

## 2026-09-01 — 태그는 "사진이 큰 자리"에만, 사진 위에 얹는다

**결정**: 작은 카드(피드 본문)에는 태그를 **넣지 않는다.** 히어로와 확장 상세처럼 사진이
전폭인 자리에만 **사진 위에** 얹는다.

**발단**: "확장 상세가 아닌데도 카드가 너무 크다. 결국 많이 보여줘야 좋은 것 아니냐"는 지적.
design/build_density.py로 380x700 패널에 실제 딜을 그려 다섯 안을 비교했다.

  D0 지금 확정한 카드   129px   히어로 아래 3.0장
  D1 날짜를 한 줄로     113px   3.4장
  D2 태그를 사진 위로   103px   3.8장   <- 채택
  D3 둘 다               87px   4.4장
  D4 한 줄 리스트        68px   5.5장
  D5 날짜 옆에 태그      93px   4.1장

**왜 D2인가**: 훅 줄은 이미 뺐고(F18) 남은 다섯 줄은 전부 일을 한다. 그래서 더 줄이려면
무언가를 정말 포기해야 한다. D2는 태그를 세로에서 들어내되 없애지는 않는다 — 사진 위로
옮긴다. 129 -> 103px(-20%), 3.0 -> 3.8장(+27%).

**"많이 보여주는 게 좋다"는 반만 맞다**: 이 서비스는 정해주는 것이라 히어로 카드는
"오늘은 이거"라는 답이므로 크게 남긴다. 줄이는 건 그 아래 둘러보는 자리이고, 거기선
밀도가 곧 이득이다 — 3장은 목록으로 안 읽힌다.

**작은 썸네일에는 왜 아예 안 넣나**: 52~62px에 태그를 얹으면 사진도 태그도 안 보인다.
1개만 얹는 것도 해봤지만(초안), 그러면 "개수가 곧 즐길 거리"라는 신호가 1개로 고정돼
오히려 거짓말이 된다. 없는 편이 낫다.

**잃는 것**: 작은 카드에서 "태그 개수 = 즐길 거리" 신호를 스크롤 중에 못 읽는다. 그 신호는
히어로와 확장 상세에만 남는다. 2026-08-22에 확정한 "즐길 게 많으면 태그도 많아진다"는
살아 있되 보이는 자리가 줄었다. 밀도와 맞바꾼 것이고 사용자가 알고 택했다.

**날짜 두 단은 그대로 둔다**: 가독성 때문에 두 단으로 바꾼 것이라(2026-08-22) 밀도를 위해
되돌리지 않는다. D1/D3/D5가 더 빽빽하지만 그 이유로 채택하지 않았다.

**기각한 대안**
- D4(한 줄 리스트) — 도장까지 사라져 "얼마나 싼가"를 카드에서 못 읽는다.
- D5(날짜 옆에 태그) — 태그 개수가 살아남아 매력적이었으나, 날짜를 한 줄로 되돌려야 해서
  위 이유와 충돌한다.
- 작은 썸네일에 태그 1개 — 위 참조.

## 2026-09-01 — 확장 상세는 패널 안에서 펼친다 (지도 위 플로팅 폐기)

**결정**: 작은 카드를 누르면 **그 자리에서 아코디언처럼 펼쳐진다.** 지도는 동시에 해당
핀을 키우고 이름표를 띄운다. 모바일(<=860px)은 지금처럼 하단 시트다.

**현행**: discover.js showCard/place가 상세를 지도 위 핀 옆에 띄운다(플로팅). 즉 오른쪽
패널을 눌렀는데 가운데 지도에서 카드가 뜬다.

**왜 바꾸나 — 누른 곳이 반응해야 한다**
- 시선이 튄다. 패널을 보다가 지도 가운데로 눈을 옮겨야 한다.
- 방금 보던 딜들이 가려진다. 발견 서비스에서 비교가 끊긴다.
- 핀이 화면 밖이면 뜰 자리가 애매하다 — B14(far 뷰에서 major 2곳이 화면 밖)와 묶여 있다.
  패널에서 열면 이 버그와 무관해진다.

E2는 카드가 그 자리에서 펼쳐지고 지도는 핀을 키워 답한다. 두 곳이 각자 자기 언어로
반응하지, 한쪽이 다른 쪽 자리를 빼앗지 않는다.

**기각한 대안**
- E1 지도 위 플로팅(현행) — 위 이유. 지도-딜 연결은 가장 강하지만 대가가 크다.
- E3 패널을 상세로 교체 — 공간은 넉넉하나 피드 맥락이 사라져 다음 딜로 가려면 매번
  목록으로 돌아가야 한다. "둘러보기"가 "검색"이 된다.
- E4 패널 위 시트 — 데스크톱에선 덮인 카드가 안 보여 비교가 끊긴다. 다만 모바일에서는
  화면이 좁아 나란히 못 두므로 이 방식을 유지한다.

**열고 닫기 규칙**
  작은 카드 누름      그 자리에서 펼침 + 지도 핀 확대·이름표 + pushState #{허브}-{목적지}
  다른 카드 누름      먼저 것 접고 새 것 펼침 (동시에 하나만)
  지도 핀 누름        패널이 그 카드로 스크롤하며 펼침 (반대 방향도 같은 규칙)
  x 또는 같은 카드    접힘 + #{허브}로 되돌림
  브라우저 뒤로가기   사이트를 안 떠나고 상세만 닫힘
  정렬·필터 변경      펼친 것을 접음 (현행 collapse()와 동일)
  그 딜이 사라짐      출발지만 적용 + 안내 (F5, 다른 허브에 있으면 제안)

목업 design/detail.html · 상세 스펙 SPEC.md §CH4

## 2026-09-01 — 상세는 지도 위에 띄우되, 지도가 그 자리로 미끄러진다 (E1 + 이동)

**결정**: E1(지도 위 플로팅)을 유지하되 **지도가 그 핀으로 이동한 뒤** 카드가 핀에서 피어난다.
같은 날 내가 E2(패널 안 펼치기)를 추천했다가 뒤집었다.

**내 추천이 틀렸다**: E1을 반대한 이유는 "시선이 튄다"였는데, 사용자가 "스르륵 넘어가는
느낌이면 어디로 가는지 정확히 알 수 있다"고 지적했다. 맞다 — 지도가 움직이면 시선이 따라간다.
튀는 게 문제였는데 움직임이 그걸 없앤다. 오히려 "네가 고른 곳은 여기다"를 말해 주는 장치가
된다. 지도가 본체인 서비스에서 이건 큰 이득이고, 나는 정적인 두 안을 비교하느라 "전환 자체가
정보를 나른다"는 걸 못 봤다.

**B14와의 결합도 이 방식이 푼다**: "핀이 화면 밖이면 열 자리가 없다"가 E1의 약점이었는데,
지도가 그 핀으로 이동하니 항상 자리가 생긴다. 약점이 기능이 됐다.

**배율은 안 건드린다**: 거리 단계(가까운 곳/조금 더 멀리/아주 멀리)가 뷰를 정하는 모델이라
여기서 확대까지 하면 단계 모델이 깨진다. 이동만 한다.

**핀을 무대 아래쪽 62%에 놓는다**: 카드가 핀 위로 피어나기 때문이다. 가운데에 두면 카드가
무대 밖으로 넘친다.

**타이밍**
  지도 이동      620ms  cubic-bezier(.22,.61,.36,1)
  핀 확대·이름표 300ms  이동과 동시
  항로 그리기    500ms  stroke-dashoffset
  카드 피어남    +300ms 뒤 340ms  cubic-bezier(.2,.9,.3,1.2)

300ms를 기다리는 이유: 지도가 아직 움직이는 중에 카드가 뜨면 카드가 미끄러지는 것처럼
보인다. 시선이 목적지에 도착한 뒤 열어야 "여기다"가 된다. 전체 체감 0.9초.

**prefers-reduced-motion이면 전부 0ms**. 큰 화면 이동이라 어지럼증을 유발할 수 있어
선택이 아니라 필수다.

**닫을 때 지도는 되돌리지 않는다**: 되돌리면 방금 온 길이 무의미해지고 멀미가 난다.
사용자는 그 지역을 보고 있는 중이다.

**기각한 대안**
- E2 패널 안 펼치기 — 내가 추천했으나 지도-딜의 공간적 연결이 약해진다. 발견의 본체가 지도다.
- E3 패널을 상세로 교체 — 피드 맥락이 사라져 "둘러보기"가 "검색"이 된다.
- E4 패널 위 시트 — 데스크톱에선 덮인 카드가 안 보여 비교가 끊긴다. 모바일에서는 유지.
- 이동 + 확대 — 거리 단계 모델이 깨진다.

목업 design/open.html (실제로 눌러볼 수 있다) · 스펙 SPEC.md §CH4

## 2026-09-01 — 카드는 도장 우선, 신기록은 %로 쓰지 않는다

**결정**: 피드 카드는 도장 우선(없으면 신기록), 확장 상세는 둘 다. 그리고 신기록은 어디서도
백분율로 쓰지 않는다 — `N일 중 최저`로만 쓴다.

**프론트가 뒤집자고 제안했다**(겹치면 신기록만 남기자). 근거가 좋았다.
- 신기록이 더 드물다(7% vs 20%)이고 "배지는 드문 좋은 소식에만" 원칙에 맞는다
- 함의가 한쪽으로만 성립한다 — 신기록은 대체로 도장을 함의하지만 역은 아니다
- hero 규칙(할인율 1순위)은 low·obs_days가 들어오기 전에 정해진 것이라 이 질문의 근거가
  될 수 없다고 정확히 짚었다. 자기 논거의 한계를 먼저 말한 셈이다

**그런데 가장 강한 논거가 내 설계엔 해당하지 않았다.** "41%↓ 옆에 16.8%↓가 있으면 뒤엣것이
초라해 보인다"는 지적인데, 나는 신기록을 애초에 퍼센트로 쓰지 않기로 했다. 16.8%는 프론트가
분석용으로 계산한 값이지 화면에 나갈 문구가 아니다.

다만 그 지적 덕분에 **암묵이던 걸 규칙으로 명시**했다. 안 적어두면 나중에 누군가 "낙폭도
숫자로 보여주자"고 할 수 있고, 그러면 정확히 그 왜곡이 생긴다.

**수치도 갱신됐다.** 프론트 분석은 낙폭 5%·obs>=14·BB4(median 알갱이) 이전 데이터였다.
확정 조건으로 다시 재면 겹침은 6건이 아니라 3건이고, T3는 8건 중 1건만 겹친다.
"T3 시각 자산의 40%를 잃는다"는 비용이 12.5%로 줄어든다.

**기각한 대안**
- 겹치면 신기록만 — 위 이유. 도장은 정량적이고 즉시 읽히는 우리 시그니처 시각 자산이다.
- 자리를 나눠 둘 다(도장은 썸네일 위, 신기록은 가격 아래) — 프론트도 "강한 신호가 약해
  보이는 문제는 못 푼다"고 했다. 카드에 주장이 둘이라는 문제도 그대로다.

## 2026-09-01 — `when` 어휘에 `이번 달`·`내년 N월`을 넣는다

**결정**: 상대 날짜 라벨을 7단계 통제 어휘로 만들고 `이번 달`과 `내년 N월`을 신설했다.
백엔드가 실측과 함께 제기했다(BB7·BB8).

**오늘이 9월인데 "9월"은 아무 말도 아니다** — 44건(35%)이 그 상태였다. `이번 주`와 `다음 달`
사이가 비어 있어 7일 초과 이번 달이 월 이름으로 떨어진 것이다. 월초일수록 몰리므로 매달 초에
3분의 1이 이렇게 된다.

그리고 딜 출발일이 2027-08까지 뻗어 있어 `7월`·`8월`이 지난 2026년이 아니라 2027년이다.
13건(10%)이 지난 달로 읽힐 수 있었다.

**순서가 곧 규칙이다** — 위에서부터 먼저 맞는 것 하나. 특히 4(다음 달)가 6(내년 N월)보다
먼저다. 12월에 1월 출발이면 "내년 1월"이 아니라 "다음 달"이다. 더 가깝게 느껴지는 쪽이
사실에도 맞다.

`YYYY년 N월`은 방어용이다. 현재 데이터에 없지만 없으면 미래에 조용히 "3월"로 떨어진다.

## 2026-09-01 (2) — 도장 임계값을 15/25/35 → 15/28/42로 옮긴다

**결정**: T1 `15~27%` / T2 `28~41%` / T3 `42%+`. 같은 날 오전에 "유지"로 정했던 걸 뒤집는다.

**왜 뒤집나 — 유지의 근거가 잘못 잰 데이터였다**

유지 근거는 "25~34% 골짜기는 하루치 잡음"이었다. 그런데 그 골짜기 자체가 median을
잘못 재서 생긴 것이었다. 백엔드가 BB4에서 median을 (허브,도시) 단위 "그날 최저가의
중앙값"으로 고치자 분포가 달라졌다.

  T2 하루 3.9건 -> 7.2건    사다리 중간이 두꺼워졌다
  뒤집힘 10% -> 0%          BB4가 고친 것
  T3 2%대 -> 4.1%           BB4가 드러낸 것

백엔드 표현이 정확하다 — "고친 것은 위계, 드러낸 것은 희소성". 예전엔 할인율이 눌려
있어 T3가 인위적으로 드물어 보였다. 내가 걸어둔 트리거(T3가 피드의 2%를 꾸준히 넘으면
재검토)가 발화했고, 이번엔 재는 자가 고쳐진 뒤의 숫자라 눈금을 옮길 근거가 된다.

**어떻게 골랐나 — 하루치로 정하지 않았다**

오늘 데이터로는 45%가 맞아떨어졌는데, 오늘은 45~49% 구간이 0건이라 45와 50이 같은
값이 됐다. 전형적인 표본 부족이고 전에 딱 이걸로 한 번 틀렸다. 그래서 목표 넷만 주고
백엔드가 성숙 15일(딜 1590건) 그리드로 계산했다.

  1  T3 <= 2.5%           원래 설계 의도. 4%는 30장 스크롤에 1~2개라 흔하다
  2  T3 < T2 < T1 내내    뒤집힘 0을 잃지 않는다
  3  T3 부재 <= 20%       부재는 누적된다
  4  T2 >= 3%             중간이 얇아지면 사다리가 다시 위태롭다

결과 15/28/42 — T3 1.7% · T2 7.3% · T3 역전·동률 0일 · T3 부재 0일.

**기각한 대안**
- 15/30/50 — 기준 넷을 문자 그대로 만족하는 유일한 조합이지만 T3가 0.9%로 너무 드물고
  T3 없는 날이 생긴다(1/15). "T3컷이 가장 낮은 것"이라는 내 방침에도 42 < 50이 맞다.
- 15/30/38, 15/30/40 — T3컷이 낮아 매력적이나 진짜 역전이 3일·1일 생긴다.
- 15/25/35 유지 — 위 이유.

**내 판정 기준 ②가 과하게 적혔다**: "T3 < T2 < T1"을 엄격 부등호로 쓰는 바람에
현행 15/25/35도 동률 3일로 탈락했다. 백엔드가 "역전과 동률은 다르다"고 갈라줬다.
동률은 두 칸이 같은 날일 뿐 화면에서 "화려한 게 더 많다"로 보이지 않는다.

**T2 = T1 동률 2일은 받아들인다**: 없애려면 T2컷을 30으로 올려야 하는데 그러면 T2가
5.1%로 얇아지고 T3 역전이 생긴다. T1과 T2는 둘 다 코랄 계열(테두리 vs 채움)이라
극적인 단차는 T3로 넘어갈 때다. 가장 화려한 등급의 희소성이 이 설계의 핵심이다.

**임계값은 화면에 안 나온다**: 카드는 실제 할인율(41%↓)을 보인다. 28·42가 어중간한
숫자여도 사용자는 모른다.

**재검토 트리거**: 성숙 구간이 15일뿐이고, BB15 이전 날짜엔 대형 도시가 없고, 30일 창이
아직 안 문다. 한 달 뒤 셋이 해소된 분포로 다시 재고 그때도 T3가 2.5%를 넘으면 또 본다.

## 2026-09-01 (3) — 날짜는 두 축, 예산은 히스토그램 + 바로가기 칩

**날짜**: "언제"(when 어휘 그대로) × "며칠"(신설). "날짜 지정"을 누르면 달력이 열린다.

현행 칩엔 "이번 달"도 "그 이후"도 없어 77건(61%)을 좁힐 수 없었다. 보이긴 했다("아무때"로) —
못 하는 게 좁히는 것이었다. when 어휘를 그대로 칩으로 쓰면 빈 구간이 사라지고, 화면에 뜨는
라벨과 고르는 칩이 같은 말이라 배울 게 없다.

"며칠"은 nights 필드가 이미 있는데 아무도 안 쓰고 있었다. 1~3박 48 · 4~6박 27 · 7~13박 35 ·
2주 이상 15로 고르게 퍼져 있다. 주말 2박3일과 일주일 휴가는 전혀 다른 여행이다.

달력을 기본으로 두지 않는 이유: 달력은 목적지가 정해진 사람의 도구다. 우리 사용자는 반대로
날짜가 먼저 있고 목적지를 찾는다. 그래서 "날짜 지정" 안에만 둔다.

**예산**: 사용자가 "웹은 B3(히스토그램), 모바일은 B2(프리셋)"를 제안했다. 방향은 맞았고
근거도 확인됐다 — 데스크톱 손잡이 18px은 터치 최소 44px(iOS 44pt · Material 48dp)의
2.4배 모자라 손가락으로 못 집는다.

다만 갈라놓는 것보다 합치는 쪽으로 갔다.

- 칩과 슬라이더는 경쟁하지 않는다. 칩은 슬라이더를 그 값으로 옮기는 바로가기이고, 슬라이더는
  그 사이 값을 고르는 도구다. 하나를 고르는 문제가 아니었다.
- 히스토그램은 읽는 것이라 터치 크기와 무관하다. 모바일에서 빼면 "딜이 어디 몰려 있나"를
  모바일 사용자만 못 본다.
- 기기마다 다른 UI를 주면 "내 폰엔 왜 없지"가 생긴다. 폰으로 보고 데스크톱에서 예약하는
  사용자를 상정하면 같은 물건이어야 한다.

모바일 손잡이는 44px로 키운다. 시트 끌기와 드래그 방향이 겹치므로 슬라이더를 잡는 동안
시트 드래그를 막아야 한다(프론트 구현 주의).

칩 경계는 실측에서 뽑았다 — 중앙값 387,387원, 30만 47곳 · 50만 77곳 · 100만 111곳.

**기각한 대안**
- 현행 슬라이더(10만~100만) — 18건(14%)이 범위 밖이라 끝까지 밀어도 안 나온다. 선형 눈금이
  40배 범위를 못 담는다.
- 프리셋만(모바일) — 나쁘지 않고 구현이 부담되면 여기서 시작해도 된다. 칩이 슬라이더의
  바로가기라 나중에 얹어도 구조가 안 깨진다. 다만 히스토그램 정보를 모바일만 잃는다.
- 슬라이더만(모바일) — 시트 끌기와 부딪힌다.

## 2026-09-01 (4) — 패널 폭 380px 유지 (바꾸지 않는 것도 결정이다)

**결정**: 그대로 380px.

**가설이 틀렸다**: "좁히면 카드 글자가 잘린다"고 예상하고 잘리는 건수를 세는 지표를
만들었는데, 320px에서도 0건이었다. 도시명이 길어야 쿠알라룸푸르(여섯 자)이고 가격은
아홉 자리가 최대라 한글 서비스는 애초에 글자가 짧다.

그래서 지표를 바꿨다 — "잘리느냐"가 아니라 "가장 긴 줄이 본문을 얼마나 채우나".

  320  지구 +7%   본문 204px  가장 긴 줄이 74% 채움
  380  기준       본문 264px  57%
  460  지구 -10%  본문 344px  44%

**양쪽 다 남는 장사가 아니다**
- 좁히면: 60px을 내주고 지구는 +7%뿐이다. slice 크롭 때문에 무대가 넓어져도 보이는
  viewBox 가로는 그만큼 안 늘어난다.
- 넓히면: 본문이 넉넉해져도 채울 글자가 없다. 빈 여백만 는다.

380px이 이미 균형점이라 바꿀 근거가 데이터에 없다.

**모바일에서 다시 본다**: 390px 폭에서는 패널이 전폭이라 이 저울이 통째로 달라진다.
PH5 모바일 항목 소관이다.

**참고 — "스르륵 이동"이 지도 압박을 덜었다**: 상세를 열면 지도가 그 핀으로 움직이므로
처음부터 다 보일 필요가 줄었다(같은 날 확정). 그게 없었다면 지구 크기를 더 지키려고
좁히는 쪽을 검토했을 것이다.

## 2026-09-01 (5) — 화면0을 없앤다 (내 추천을 사용자가 뒤집었고 그게 맞다)

**결정**: 전체 화면 인트로를 없애고 서울을 기본 출발지로 둔다. 헤더 알약으로 바꾼다.

**내가 틀렸다**: Z2(지도 위 시트 + 기억)를 추천하면서 Z3(화면0 없음)에 "제품 이해 △"를
줬다. 근거는 "출발지를 고르는 행위가 제품을 이해시킨다"였다.

사용자가 "일단 서울로 잡고 변경할 수 있게 하면 어떠냐"고 되물었고, 따져보니 그쪽이 맞다.
딜을 바로 보여주는 게 더 잘 설명한다 — "어디서 출발해요?"는 아무 가치도 안 준 채 질문부터
하고, "서울 → 제주 33,004원"은 그 자체로 설명이다. 첫 화면이 질문이면 이탈 지점이 하나
늘고, 발견 서비스는 일단 보여줘야 한다.

내가 "제품 이해"를 "우리 모델을 설명하는 것"으로 좁게 봤다. 사용자에게 제품 이해란
"이게 나한테 뭘 해주나"이고, 그건 딜이 답한다.

**서울이 기본인 근거**: 딜의 55%(69/126)이고 인천이 관문이다. 지오로케이션은 안 쓴다 —
정적 사이트라 서버가 없고 위치 권한을 물을 만한 가치가 아니다.

**남는 위험 하나 — "서울 전용 서비스"로 읽히는 것**
서울이 아닌 45%가 바꾸는 버튼을 못 찾으면 그대로 나간다. 그래서 헤더 처리를 셋 그렸다.

  P1 조용한 회색 알약(현행)  눈에 안 띔 — 장식으로 읽힌다
  P2 코랄 알약 + 첫 방문 한 줄 띠      <- 채택
  P3 코랄 알약 + 드롭다운 미리 펼침    결국 작아진 화면0, 딜을 가린다

P2는 막지 않는다. 뒤에 딜이 이미 보이는 채로 "지금 서울 출발로 보고 있어요"라고 알리고
닫으면 끝이다.

**드롭다운에 건수를 붙인다**: 제주 8곳처럼 보이면 고르기 전에 안다. 지금은 제주를 고른
다음에야 8곳뿐인 걸 알게 된다. 필터 건수와 같은 원리다.

**남겨둔 문제**: "서울"은 인천+김포 가상 허브(SEL)인데 알약은 "서울"이라 말한다. 김포 딜을
보고 인천으로 갈까 헷갈릴 자리라 확장 상세에서 공항을 밝힌다.

**기각한 대안**
- Z0 현행 전체 화면 — 고르기 전엔 아무것도 안 보이고 건수도 안 알려준다.
- Z1 전체 화면 + 건수 — 나아지지만 여전히 지도를 통째로 가린다.
- Z2 지도 위 시트 + 기억 — 내가 추천했던 것. 막지는 않지만 여전히 질문이 먼저다.

## 2026-09-01 (6) — 모바일은 지도가 배경, 카드는 3단 시트

**결정**: 지도가 화면 전체를 채우고 카드 시트가 peek(160) / half(340, 기본) / full 세 높이로
올라온다. 사용자가 끌어서 정한다.

**현행의 진짜 문제는 비중이 아니라 방향이었다**: 지도 56%는 나쁘지 않은데 카드가 가로로
흘러 390px에 1.8장밖에 안 보인다. 발견 서비스에서 비교가 안 되는 게 치명적이고, 세로로
읽는 화면에서 손가락 방향이 바뀌는 것도 부담이다.

**비중을 우리가 정할 문제가 아니다**: 지도를 볼 때와 목록을 훑을 때 필요한 비중이 다르다.
고정하면 둘 중 하나는 늘 답답하다. half 기본 2.6장, full 5.2장.

**상세와 같은 은유**: 확장 상세도 하단 시트로 확정했으므로, 카드를 누르면 같은 시트 안에서
상세로 밀려 들어가고 뒤로가기로 돌아온다. 시트 두 장이 겹치지 않는다.

**자유 줌·팬을 안 넣은 게 여기서 값을 했다**: 지도에 드래그 제스처가 없으므로 시트 끌기와
안 부딪힌다. 자유 줌을 넣었다면 이 설계가 성립하지 않았다. 8월 22일 결정이 지금 배당을
준 셈이다.

**남은 위험**: 드래그가 세 군데서 부딪힌다 — 시트 높이 조절, 시트 안 스크롤, 예산 슬라이더.
슬라이더를 잡는 동안 시트 드래그를 막는 것과 같은 처리가 필요하다.

**기각한 대안**
- 탭 전환(지도|목록) — 카드는 5.0장으로 가장 많이 보이지만 지도와 목록이 서로를 못 본다.
  핀을 누르고 카드를 보려면 탭을 옮겨야 한다. 지도가 본체인 제품에서 그 단절은 크다.
- 지도 40% 고정 + 세로 리스트 — 현행보다 두 배 가까이 보이고(3.3장) 단순하지만 비중이
  고정이라 지도를 크게 볼 수 없다.
- 현행 유지 — 위 이유.

## 2026-09-01 (7) — 날짜 필터는 when 값으로 거른다 (버그 종류를 없앤다)

**결정**: 프론트는 "이번 주"를 다시 계산하지 않는다. `when` 값과 문자열 비교만 한다.
`날짜 지정`에서만 `dep`으로 범위를 본다.

**발단**: 백엔드 코드 리뷰가 `이번 주말` 라벨이 다음 주말에도 붙는 걸 찾았다. 계약을 달력
주 기준으로 고쳤다. 그 소식을 받은 프론트가 자기 코드를 보다 **같은 원인의 버그를 필터에서**
찾았다.

  프론트 실측 · 일요일에 "이번 주"를 누르면 정상 0건인데 18건이 나온다(전부 다음 주 딜)
                토요일에 "이번 주말"을 누르면 오늘·내일 출발 10건이 사라진다

**원인이 하나인데 두 곳에서 따로 터졌다**: 상대 거리(N일 이내)로 재면서 라벨은 달력 단위를
말했다. 백엔드는 라벨이, 프론트는 필터가 틀렸다.

**그래서 계산하는 곳을 하나로 만든다**: 백엔드가 when을 계산하고, 프론트는 그 값으로 거른다.
그러면 배지와 필터가 구조적으로 어긋날 수 없다. 같은 사실을 두 곳에 적으면 하나는 틀린다 —
백엔드가 timeutil.py로 얻은 교훈과 같고, 내가 design/_fmt.py로 얻은 교훈과도 같다.

**일요일에 "이번 주"가 0곳인 건 정상이다**: 이번 달력 주의 평일이 다 지났다. 프론트가
"나중에 버그로 오인될 수 있다"고 물었는데, 우리는 이미 칩에 건수를 보이므로 화면이 스스로
설명한다. 0곳 칩은 흐리게 하고 못 누르게 한다.

**"그 이후" 경계**: when이 이번 주말·다음 주말·이번 주·이번 달·다음 달 중 아무것도 아닌 것.
즉 N월·내년 N월·YYYY년 N월. 별도 날짜 계산이 없다.

**기각한 대안**
- 프론트가 달력 주를 직접 계산 — 지금 터진 그 구조다. 두 곳이 갈라진다.
- 칩 이름을 상대 거리로 바꾸기(`7일 안에`) — 정의와 이름은 맞지만 배지(`이번 주`)와 칩이
  다른 말이 된다. "화면에 뜨는 라벨과 고르는 칩이 같은 말이면 배울 게 없다"는 원칙과 어긋난다.

## 2026-09-01 (8) — far 뷰 핀 밀도: 없애지 말고 낮춘다

**결정**: minor 핀을 5px 반투명·후광 없음으로 낮춘다. **지우지 않는다.**

**발단**: 사용자가 "아주 멀리로 가면 점이 너무 많다. 주요 도시에만 찍으면 어떠냐"고 제안했다.
체감이 맞았다 — 서울 70건 중 66%가 16px 안에 이웃을 갖는다.

**다만 몰리는 곳은 "먼 곳"이 아니라 "가까운 곳"이다**: 겹치는 46개 중 동남아 13 · 중국 12 ·
일본 8 = 33개가 근·중거리다. 장거리(유럽 6 · 미주 2)는 넓게 퍼져 안 겹친다. 줄여야 할 건
가까운 쪽인데 "major만"은 먼 쪽도 같이 지운다.

**major만으로 가면 34건이 지도에서 사라지고 그중 14건이 장거리다** — FLOWS F15("피드에 있는
딜은 지도에서 찾을 수 있어야 한다")가 깨지고, "아주 멀리"가 자기 목적을 배신한다.

**A2는 지우지 않고 낮춘다**: major는 그대로, minor는 5px 반투명. 시각 무게가 major의 1/5이라
뭉치는 느낌이 사라지는데 딜은 하나도 안 없어진다. 카드를 누르면 그 핀이 커진다 — 조용한
점도 부를 수 있다.

**기각한 대안**
- major만(사용자 제안) — 위 이유. 다만 완전히 버리진 않는다. minor를 낮춰도 답답하면
  "아주 멀리"에서 근거리 minor만 접는 절충이 가능하다. 그때도 먼 곳은 남긴다.
- 클러스터(가까운 것끼리 묶기) — 70개가 31개 표시로 줄어 가장 깔끔하다. 다만 단계 버튼이
  이미 "거리대를 고르는" 장치인데 클러스터를 누르면 또 단계가 바뀐다 — 같은 일을 하는
  조작이 둘이 된다. 밀도가 더 심해지면 그때 꺼낸다.

## 2026-09-01 (9) — 라벨을 숨기는 근거를 정정한다

트윈 중 라벨을 숨기는 규칙에 "충돌 검사라 매 프레임 돌리면 비싸다"고 적었는데 틀렸다.

프론트 실측: 라벨·핀·카드를 다 합쳐 0.08ms다. 진짜 병목은 path(WORLD) 15~17ms로 전체의
99.7%다. 내가 의심했던 innerHTML 재생성도 병목이 아니었다.

**결론(숨긴다)은 맞았지만 근거가 틀렸다.** 진짜 이유는 하나다 — 움직이는 글자는 못 읽는다.

근거를 안 고치면 다음 사람이 그 틀린 근거로 다른 결정을 한다. 예컨대 "성능 때문에 숨기는
거라면 라벨을 줄이면 트윈이 빨라지겠네"로 이어질 수 있고, 그건 완전히 헛수고다.

**그리고 §CH1 트윈 스펙이 현행 구조로는 성립하지 않는다**: 400/600/620ms 트윈이 매 프레임
path(WORLD)를 다시 만들어 프레임 예산을 100% 먹는다. 프론트가 해법을 검증해뒀다 —
rotate를 고정하면 center·scale 변경이 평면 아핀변환이 되어 <g transform> 하나로 끝난다
(최대 오차 1.8e-12px). 렌더 구조 변경이 트윈보다 먼저다.

시간 값(400/600/620ms)과 로그 보간, 라벨 숨김은 그대로 유효하다.

## 2026-09-01 (10) — BM·유입 계획: 숫자를 먼저 냈다

**발단**: 사용자가 "BM 설정이랑 홍보 계획은 안 짜도 되냐"고 물었다. 확인해보니 PRODUCT.md에
수익원(Aviasales 커미션 1.1~1.6%)과 제약($0 운영, Trip.com 3개월 조건)은 이미 있었는데
숫자가 없었다. 아무도 "그래서 얼마 벌리나"를 계산한 적이 없다.

**계산 결과 — 이건 사업이 아니라 실험이다**
건당 커미션 5,229원(중앙값 387,387원 x 1.35%). 우리는 클릭만 보내므로 깔때기가 두 단이다
(방문 -> 클릭 -> 예약처 결제). CTR 20% · 예약 전환 2% = 최종 0.4%로 잡으면:

  하루  100명  ->  월  62,748원
  하루 1,000명 ->  월 627,480원

하루 100명이면 월 6만원이다. 솔직히 적는 게 맞다. 다만 운영비가 $0라 손익분기가 사실상
0이고 잃는 건 시간뿐이라, 판정 기준을 매출이 아니라 "다음 문이 열리는가"로 잡았다.

**유료 광고는 계산으로 배제했다**
방문 1명의 기대 수익 = 0.4% x 5,229원 = 21원. 국내 CPC는 100~300원이다. 10배 이상
적자라 어떤 시나리오에서도 안 맞는다. 유입은 전부 무료 채널이어야 한다.

**오늘 확정한 UX가 유입 채널의 전제였다**
이걸 모르고 UX 관점으로만 정했는데, 없었으면 채널 절반이 불가능했다.
- URL 딥링크 -> 커뮤니티 시딩의 전제. 없으면 홈 주소만 보낼 수 있다.
- IA-1(홈->노선 진입로) -> 구독의 유일한 진입점이 노선 페이지인데 거기 도달이 막혀 있었다.
- 신뢰 결정들(F17·F18·낙폭 5%) -> 도장이 100%에 붙어 있거나 훅이 거짓말하면 시딩이
  역효과다. 우리가 통제하는 건 딜의 질뿐이다.

**마일스톤을 매출이 아니라 "다음 문"으로 잡았다**
  M1 하루 30~100명(3개월)  Trip.com 재신청 자격. 붙으면 예약처가 늘어 CTR·전환이 오른다
  M2 검색 유입 > 시딩 유입   SEO가 스스로 굴러가기 시작했다는 신호
  M3 구독자가 재방문을 만든다 발견 서비스의 약점이 재방문 이유다

첫 지출(도메인 연 2만원)은 M1 이후. GitHub Pages가 리다이렉트를 자동으로 걸어 SEO 자산을
안 잃으므로 서두를 이유가 없다.

**접는 조건도 적었다**: 3개월간 하루 10명을 못 넘으면 유입 가설이 틀린 것이다. 그때는
제품을 더 다듬는 게 아니라 채널을 다시 생각한다.

**전환율은 전부 가정이다** — 우리 실적이 0이라 실측이 없다. 그래서 단일 예측이 아니라
민감도 표로 적었다.

## 2026-09-01 (11) — 배너 광고를 안 붙인다. 수익원은 제휴 하나로 간다

**질문**: "광고를 붙일 수는 없나?"

**계산**: 방문 1명당 기대 수익이 제휴 20.9원 vs 애드센스 1.0~3.0원(RPM 1,000~3,000원)이다.
제휴가 7~21배 낫다.

**그런데 배너는 더하는 게 아니라 빼는 쪽이다 — 원칙 셋을 동시에 깬다**

1. 신뢰 — 우리 자산은 "근거 없는 말을 하지 않는다"다. 도장을 100%->25%로 줄이고 훅을
   없애고 낙폭 5%를 건 게 전부 그것 때문인데, 배너는 우리가 통제할 수 없는 문구를 우리
   화면에서 말하게 한다.
2. 스택 — CLAUDE.md 고정 원칙이 "런타임 CDN 의존 0"이다. 애드센스는 외부 스크립트고
   index.html은 이미 304KB다.
3. 동의 배너 — 추적 쿠키를 쓰므로 쿠키 동의 배너가 생긴다. 지도를 안 가리려고 도크를
   90->52px로 줄인 노력과 정면으로 충돌한다.

덧붙여 지금은 승인도 안 난다 — 애드센스는 트래픽·콘텐츠 요건이 있고 우리 방문자는 0이다.

**대안은 문맥 제휴다**: 같은 방문자인데 이미 "다낭 3박4일"을 고민 중이라 관련성이 높다.
숙소 3박 30만원 x 4~7% = 12,000~21,000원으로 항공권 커미션(5,229원)의 2~4배다.

**그런데 지금은 안 붙인다 — 붙일 자리가 없다.** 확장 상세는 "이 항공권을 살까"를 결정하는
자리다. 거기에 숙소·보험·유심을 얹으면 결정을 돕는 게 아니라 광고판이 된다. 예약처 4곳 +
고지만으로 이미 빽빽하다.

한 화면에 하나의 결정. 숙소는 다른 순간의 일이고 우리에겐 아직 그 순간이 없다.
그 자리를 만드는 건 제품 결정이지 수익 결정이 아니다.

**지금 정할 것은 "얼마 버나"가 아니다**: 방문자가 0이라 무엇을 붙여도 0원이다.
- 수익원은 제휴 하나로 간다.
- 문맥 제휴는 가능성으로 열어두되 자리가 생기면 붙인다. 트래픽이 먼저다.

**기각한 대안**
- 애드센스 등 디스플레이 광고 — 위 이유.
- 확장 상세에 숙소 제휴 추가 — 자리가 없다. 한 화면에 하나의 결정.

## 2026-09-01 (12) — 배너 광고 판단 정정: "원칙 셋"은 과장이었다

**사용자 지적**: "1,2,3 다 잘 이해가 안 된다. 쿠키 동의 배너는 다른 사이트처럼 하면 되고
2번도 해결 가능한 거 아니냐."

**맞다. 내가 과장했다.**
- 스택 규칙(런타임 CDN 의존 0)은 우리가 정한 것이지 법칙이 아니다. 돈이 되면 바꿀 규칙이다.
- 쿠키 동의 배너는 첫 방문에 흐림+하단 띠로 다들 하는 해결된 패턴이다.

둘 다 해결 가능한 엔지니어링 문제인데 "원칙을 깬다"고 써서 금지 사유처럼 만들었다.
결론이 같아도 근거가 부실하면 나중에 뒤집을 때 판단이 안 선다.

**진짜 이유는 산수다.**

1. 손익분기가 아슬아슬하다. 배너를 붙이면 클릭 대상이 하나 늘어 예약처 클릭이 분산된다.
   RPM 2,000원이면 CTR이 상대 9.6%만 떨어져도(20% -> 18.1%) 순증이 0이다. 광고가 전환을
   5~14% 갉아먹는 건 흔하다. 순증이 0이거나 마이너스일 수 있는데 쿠키 동의·외부 스크립트·
   심사는 확실히 는다.

2. 절대액이 무의미하다. 하루 100명이면 배너 월 6,000원이다. 그 돈에 쿠키 배너를 사는 건
   안 맞는다.

3. 신뢰는 금지 사유가 아니라 조건이다. 배너는 우리가 통제 못 하는 문구를 우리 화면에서
   말하게 하지만, 그건 "절대 안 된다"가 아니라 "붙인다면 자리와 형식을 우리가 정한다"다.

**재검토 트리거**: 하루 500명을 넘으면 다시 계산한다. 그때는 실측 CTR이 있으므로 손익분기를
가정이 아니라 측정으로 판단할 수 있다. 도장 임계값을 median 고치기 전에 안 옮긴 것과 같은
원칙 — 재는 자가 생긴 뒤에 정한다.

**교훈**: 결론이 옳아도 근거를 부풀리면 안 된다. 오늘만 세 번째다 — 라벨 숨김의 성능 근거,
IA-1의 46%, 그리고 이번 "원칙 셋". 셋 다 결론은 맞았고 근거가 틀렸다.

## 2026-09-01 (13) — 초기 구간엔 수익 모델이 없다. 그게 정답이다

**질문**: "하루 100명 이하일 때 수익 모델은 어떻게 잡히나?"

**답: 없다.** 솔직히 적는 게 맞다.

  하루  10명  월 1.2건 예약  5,810원   그달 0건일 확률 30%
  하루  30명  월 3.6건      17,431원
  하루 100명  월  12건      58,106원

예약은 0.4건 같은 게 없다. 하루 10명이면 그달 0건일 확률이 30%다. 초기에 "이번 달 매출
0원"은 정상이고 실패 신호가 아니다. 이 구간의 판단은 매출이 아니라 트래픽으로 해야 한다.

**괜찮은 이유는 비용이 사실상 없어서다**: 호스팅 0 · 도메인 0 · API 0 · Claude Haiku 연
2,600원. 합계 월 217원. 최악의 경우 1년에 커피 한 잔 값을 잃는다. 손익분기가 사실상 0이라
수익 0으로도 무한히 버틸 수 있고, 그게 이 구간의 전략이다.

**이 구간에서 수익을 늘리려는 시도는 전부 손해다** — 셋 다 계산으로 확인했다.
- 배너: 순증 0에 가깝고 하루 100명에 월 6,000원
- 유료 광고 유입: 방문당 21원 vs CPC 100~300원, 10배 적자
- 숙소·보험 제휴: 붙일 자리가 없다

그래서 "아무것도 안 하는 게 최선"이다. 반직관적이지만 계산이 뒷받침한다.

**대신 저절로 쌓이는 것이 있다**: 가격 이력 33일 -> 1년이면 365일. 계절성까지 잡히고
"평소 시세"와 "N일 최저"가 정확해진다.

이게 핵심이다 — 스카이스캐너는 "지금 얼마"를 말하고 우리는 "평소보다 얼마나 싼가"를
말하는데, 그건 시간이 지나야만 생기는 자산이다. 돈을 못 버는 동안에도 매일 쌓인다.

**검토한 다른 수익원(전부 기각)**
- 후원/도네이션 — 트래픽 0에 성립 안 함
- 유료 구독 — 무료 대안이 널렸다
- 데이터 판매 — Travelpayouts 재가공이라 라이선스 문제
- B2B — 규모가 없다

## 2026-09-01 (14) — 광고 자리는 정해두되 코드는 준비하지 않는다

**질문**: "어디에 배너를 넣을지랑 넣는 시스템 정도는 준비하는 게 좋지 않나? 나중에 넣으려고
할 때 다 뜯어고치는 일이 없게."

**맞는 문제의식인데 "준비"의 형태가 다르다.** 자리를 비워두는 것은 지금 화면에 빈 공간을
만들어 손해고, 어디 넣을지 정해두는 것은 문서 작업이라 공짜다. 뒤만 한다.

**결정: 발견 홈에는 안 넣는다. 노선 페이지 맨 아래에만.**

두 페이지가 다른 사용자를 받는다 — 홈은 목적지가 없는 사람(제품의 본체, 신뢰가 자산),
노선 페이지는 검색으로 온 목적지를 이미 정한 사람(SEO 유입). 그래서 다른 규칙을 써도 된다.

**홈은 애초에 넣을 자리가 없다.** 오늘 하루 종일 잰 게 그거였다 — 헤더 아래면 지도가 더
짧아지고(이미 세로 74%), 패널이면 방금 번 +27% 밀도를 도로 쓰고, 피드 카드 사이면
"이것도 딜인가?" 혼동이 생긴다.

**맨 아래인 이유**: 데이터 섹션을 끊지 않고 우리 구독 CTA와 경쟁하지 않는다. 노출이 낮아
RPM도 낮겠지만, 바닥 자리로 수지가 안 맞으면 그게 곧 "하지 말라"는 답이다.

**지금 코드로 준비할 게 없다 — 그게 이 결정의 핵심이다**
- 노선 페이지는 build_site.py가 통째로 생성하는 정적 템플릿이라 나중에 섹션 하나 추가하는
  건 몇 줄이고 뜯어고칠 레이아웃이 없다.
- 반대로 홈은 retrofit이 비쌌을 것이다(지도·패널·시트가 맞물려 있다). "홈엔 안 넣는다"는
  결정이 그 위험까지 같이 없앤다.
- 안 쓰는 훅을 미리 심지 않는다. 쓰지 않는 코드는 썩고 나중에 "이거 왜 있지"를 유발한다.

**붙일 때 지킬 것 셋을 미리 적어뒀다**
1. 광고 스크립트는 노선 페이지에서만 로드한다 — 그래야 홈에 쿠키 동의 배너가 안 뜬다.
2. CLAUDE.md의 "런타임 CDN 의존 0"을 그때 명시적으로 예외 처리한다. 규칙을 조용히 어기지
   않는다.
3. (광고) 고지는 제휴 링크와 같은 기준으로 붙인다.

## 2026-09-01 (15) — 광고 자리 정정: 홈에 자리가 있다 (내가 틀렸다)

**사용자 지적**: "근데 홈에 넣는 게 맞지 않음?"

**맞다. 내가 틀렸다.** 직전에 "홈은 레이아웃이 빡빡해서 자리가 없다"고 썼는데, 재보니 반대다.
나는 오늘 만든 레이아웃을 지키려고 "빡빡하다"를 근거로 썼다. 편향이었다.

**실제로는 홈에 빈 자리가 있다 — 그것도 지금 낭비되는 자리다.**

같은 날 프론트가 알려준 기하 제약이 답이었다. 등거리원통도법에서 지구는 항상 2:1이라
미주까지 담으면(배율 <= 199) 지구 세로가 무대보다 짧다. 데스크톱에서 74%만 채운다.

  둘 다 만족하려면  무대 종횡비 >= 1.59
  지금 데스크톱     900x680 = 1.32     세로 115px가 빈 바다

그 빈 자리가 정확히 배너 크기다.

  배너 없음   900x680  1.32  지도 74%
  배너 115px  900x565  1.59  지도 100%

배율은 그대로다. 지구가 작아지는 게 아니라 낭비되던 빈 바다만큼만 잘라낸다.
배너를 넣으면 지도가 오히려 꽉 찬다.

**자리: 지도 열 하단.** 헤더 아래가 아니라 지도 밑이다.
- 패널을 안 건드려 카드 밀도(+27%)가 그대로다.
- 헤더 아래가 아니라 첫인상이 광고가 아니다.
- above the fold라 노선 페이지 맨 아래보다 노출이 훨씬 좋다.

**그래도 지금은 안 붙인다 — 이유가 "자리"에서 "타이밍"으로 바뀐다.**
누적 $100이 되어야 입금되는데 하루 100명이면 1년 9개월, 하루 30명이면 6년이다.
재검토 트리거는 그대로 하루 500명이다.

**코드 준비도 여전히 필요 없다 — 다만 이유가 다르다.** 프론트가 far 배율을 상수에서
함수로 바꾸는 중이라(CH1 T5) 무대 높이가 바뀌면 배율이 자동으로 따라온다.

**붙일 때 지킬 것에 하나가 늘었다**: 쿠키 동의 배너와 출발지 안내 띠가 동시에 뜨면 첫
화면이 공지판이 된다. 동의가 먼저, 안내가 그다음이다.

**교훈**: 오늘 네 번째다 — 라벨 숨김 성능 근거, IA-1 46%, 배너 "원칙 셋", 그리고 이번
"홈엔 자리가 없다". 앞의 셋은 근거를 부풀린 것이고 이번엔 **내가 만든 것을 지키려고**
반대 근거를 안 찾았다. 결이 다른 실수다.
