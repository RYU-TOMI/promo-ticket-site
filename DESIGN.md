# 갈래말래 — 디자인 시스템 (DESIGN.md)

> 모든 화면의 단일 기준. 무드보드에서 확정(2026-08-01). 코드 토큰은 `collector/theme.py`.
> 제품 본질은 `PRODUCT.md`, 진행/규칙은 `PROJECT.md` 참조.

## 원칙
1. **지도가 주인공** — 발견 서비스. 첫 화면은 헤더 외 전부 지도(풀스크린).
2. **정보보다 감정** — "떠나고 싶다"를 일으키되, **AI틱함을 경계**(사람이 공들인 편집물).
3. **프로 타이포 + 선택적 손맛** — 구조·본문은 Pretendard, 손맛은 악센트로만(로고 비행운·특가 도장·마커 밑줄).

## 컬러 토큰
| 토큰 | HEX | 역할 |
|---|---|---|
| `--accent` (시그니처) | `#F2603F` | 로고·버튼·핀·포인트·특가 |
| `--accent2` | `#C6472A` | 비행기 아랫면·눌림 상태 |
| `--ink` | `#20353A` | 제목·본문 텍스트 |
| `--sub` | `#5E7A7C` | 보조 텍스트·라벨 |
| `--sea` | `#EDF4F3` | 지도 바다·앱 배경 |
| `--land` | `#D2E7DE` | 지도 육지 |
| `--coast` | `#33534F` | 지도 윤곽선·차트 라인 |
| `--line` | `#E6EDEC` | 테두리·구분선 |
| `--soft` | `#F0F5F4` | 칩 배경 |
| `--card` | `#FFFFFF` | 카드·헤더 표면 |
| `--bg` | `#F4F8F7` | 서브페이지 배경 |

- 시그니처색은 **한 가지(코랄)**로 통일. 차트 데이터 라인은 `--coast`(청록), 강조·최저점은 `--accent`.
- 다크: 배경 `#0F2A29`, 텍스트 `#EAF3F0`, accent `#FF7A57` 계열(별도 매핑, 추후).

## 타이포그래피 — Pretendard (Variable)
| 역할 | weight / size | 비고 |
|---|---|---|
| Display/Hero | 900 · 2.6–2.9rem | letter-spacing -0.035em. "어디, 갈까?" |
| Heading | 900 · 1.3–1.5rem | 섹션 제목 |
| Subhead | 700 · 1.05–1.4rem | 노선명 등 |
| Body | 400/500 · 1rem | line-height 1.6 |
| Label | 700 · 0.78–0.85rem · `--sub` | 날짜·필드 라벨 |
| Price | 900 · tabular-nums | 가격 |
- **손글씨/세리프 폰트 금지**(아마추어로 읽힘). 개성은 크기·마커 악센트·색·지도에서.

## 로고
- 워드마크 **갈래말래**(Pretendard 900, -0.03em), "말래" `em`은 `--accent`.
- "말래" 밑에 **페이드 비행운**(투명→코랄 그라디언트 스트로크, 우상향 곡선) + 끝에 **접힌 종이비행기**(윗면 `--accent`, 아랫면 `--accent2`, 흰 크리스).
- 비행기·항적은 **워드마크 폭 안에 가둠**(메뉴 침범 금지), 로고-메뉴 간격 ≥ 40px.
- 헬퍼: `theme.logo()`.

## 스페이싱 · 라운드
- 라운드: 앱/큰 컨테이너 18px, 카드 16px, 버튼/필드 11–12px, 칩 99px, 아이콘 10px.
- 리듬: 8px 기반. 헤더 높이 60px.

## 컴포넌트
- **버튼(CTA)**: `--accent` 배경, 흰 글자, 800, radius 12. 보조 버튼: 흰 배경 + `--line` 테두리.
- **칩(필터)**: `--soft` 배경 · `--sub` / 활성 `.on` = `--accent` 배경 흰 글자.
- **필드(출발 선택)**: 1.5px `--line` 테두리, radius 11.
- **특가 도장(stamp)**: 회전(-9°), `--accent` 2px 테두리+글자, radius 6. "특가 41%↓".
- **결과 카드**: 흰 배경, radius 16, 그림자, 점선 구분선, 도장 악센트, 가격+예약.
- **핀**: `--accent` 점(r 8.5) + 흰 링(2.5) + `--accent` 후광(opacity .15). 라벨은 배경색 외곽선(paint-order stroke).
- **헤더**: 흰 배경 + 하단 `--line`. 로고 좌 · 메뉴(발견/오늘의특가/노선별/알림) · 유틸(검색·알림·로그인).
- **줌 컨트롤**: 흰 라운드, ＋／－.

## 지도 스타일
- **실제 해안선**: d3-geo(`geoEquirectangular`) + `geoPath`로 Natural Earth 렌더. 초기 "직선 실루엣" 실험은 **폐기** — 실측 해안선이 더 낫다고 확정(2026-08-01).
- **LOD 해상도**: 광역 뷰 110m, 한 지역으로 확대하면 50m 해안선으로 교체(확대 시 각지지 않게). ※ 현재 `docs/data/world.geojson`는 110m 축소본 — **확대용 50m 지역본 준비 필요**(코딩 전 리스크).
- 바다=`--sea`, 땅=`--land`, 윤곽=`--coast` 얇게(round join).
- **서울 통합**: 인천·김포 → "서울" 한 핀. 출발지는 항상 한국.
- **핀 스냅**(필요 시): 좌표가 바다에 뜨면 육지로 살짝 당김.
- 벤더링: `docs/assets/d3-geo.min.js` + `d3-array.min.js`(전역 `d3`, 순서 중요). 런타임 CDN 의존 0.

## 발견 인터랙션 (핵심 플로우) — 2026-08-01 확정
검색이 아니라 **발견**. "시간 남는데 어디 싸게?"에 지도가 답한다. (시각 확인: `design/storyboard.html`)
1. **출발지 선택**(화면0) → **한국 지도 + 출발 공항 5핀**(인천·김포=서울 통합, 부산·대구·제주)에서 하나 탭 → 지도가 근거리로 **줌아웃 트윈**하며 열림.
2. **단계 줌(거리 확장)** — 자유 줌 아님, 하단 **단계 버튼**으로 뷰 자체 전환(모바일·정보량에 유리):
   - `가까운 곳` = 한국+일본+중국동부+대만 (확대)
   - `＋동남아` = 줌아웃하며 동남아 추가
   - `＋유럽·미주` = 더 줌아웃
   - 전환은 projection scale/center **트윈**.
3. **핀 = 목적지 도시.**
   - **인코딩: 크기 고정 + 색 진하기 = 가격**(쌀수록 진한 코랄). 크기로 가격 표현은 산만해서 **폐기**(2안 확정).
   - 색 스케일은 **현재 화면에 보이는 도시들 내 상대값**(거리대가 다르면 절대가격도 다르므로).
4. **LOD(줌 레벨별 표시)** — 지역 불문 **배율 임계값** 기준.
   - 도시마다 `tier: major|minor`. 축소=major만, 확대(scale ≥ 임계값)=minor까지 **자동 등장**.
   - 일본 확대→다카마쓰·마쓰야마·히로시마·규슈; 동남아 확대→치앙마이·푸켓·크라비·푸꾸옥·나트랑·씨엠립.
   - minor 라벨은 옅고 작게. 밀집 지역은 필요 시 "라벨은 호버 시에만" 규칙 추가.
5. **호버(모바일 탭) → 항로 + 카드.**
   - 출발지→도시 **곡선(quadratic) 항로**가 그려지는 애니메이션(종이비행기 톤). 한 번에 하나만.
   - 곡선 끝에 **사진 카드**: 사진 + 도시명 + 최저가 + 특가 도장 + 분위기 태그 + CTA.
   - 모바일: 탭=표시, 바깥 탭=닫기(호버 없음 대응).
6. **핀/카드 클릭 → 목적지 상세**(가격·날짜·예약 링크·노선 분석).

## 홈 레이아웃 (지도 + 카드 피드) — 2026-08-01 확정
지도가 무대(lean-forward), 왼쪽 **"오늘의 발견" 카드 피드**가 lean-back 경로. 둘은 양방향 연동. (시각: `design/feed_map.html`)
- **왜 둘 다**: 순수 지도만이면 결국 출발지·목적지 고르는 "검색"이 됨 → 개백수(정해줘) 이탈. 카드 피드로 lean-back 보완.
- **피드(좌 340px, 모바일=하단 가로 스크롤)**: 맨 위 **"오늘의 한 방"** hero 카드(사진 크게 + `갈래`/`말래` 버튼) → 아래로 큐레이션 카드. 각 카드 = 사진·도시·최저가(`~`)·**날짜**·특가 도장·"왜 지금" 훅·분위기 태그.
- **날짜 표기 필수**: 상대 라벨(`이번 주말`, `광복절 연휴`, `3주 뒤`) + 실제 일자(`8/8(금)~8/10(일) · 2박3일`).
- **지도**: 핀(색=가격)+단계 줌+LOD. 핀/카드 활성 시 **항로 곡선 + 플로팅 카드**(항로 끝).
- **양방향 연동**: 피드 카드 hover→항로 날아감+핀/플로팅카드 켜짐; 핀 hover→피드 카드 하이라이트(스크롤). 활성 1개.
- **"오늘의 발견" = "오늘 출발"이 아니라 "오늘 큐레이션한 딜"** — 출발일은 딜마다 다름(이번 주말~수개월). 그래서 카드에 날짜가 없으면 판단 불가.
- **갈래 = 인터랙션(말래는 제거)**. hero 배지 문구는 브랜드명 살려 **"진짜 갈래말래?"**. "말래"(넘김/닫기) 버튼은 **실수로 카드 사라질 위험**이라 두지 않음 → `갈래 → 자세히 보기`만.

## 필터 · 정렬 — 2026-08-01 확정
- **위치 = 지도 우측 하단 "필터 도크"**(헤더 바 아님). 지도앱 스타일 플로팅 패널, 기본 **펼침**, 헤더 눌러 접기(접어도 활성 개수 `필터 · 2` 표시). 줌 컨트롤은 우측 상단으로.
- **날짜(보편 프리셋 + 달력)**: `아무때`(기본)·`이번 주`·`이번 주말`·`다음 달`·`날짜 지정`. 특정 시즌(연휴명)은 지금만 유효하므로 **금지**, 상대 프리셋만. `날짜 지정` → 달력(시작~끝) 기간 필터.
- **분위기**: 해변/도시/온천 토글(단일).
- **예산은 슬라이더**: 상한 조절, 실시간.
- **필터 = 전 지역 매칭**: 필터를 켜면 **현재 거리 단계 무관** 전 지역·전 등급에서 찾음(피드 헤더 "전 지역에서 찾는 중"). "해변"이면 근거리 단계여도 방콕·다낭까지 올라옴. 단계 버튼은 필터 없을 때의 탐색용.
- **필터 = "숨김"이 아니라 "흐림"(dim)**: 조건 밖 핀·카드는 제거하지 않고 흐리게(저채도·낮은 불투명도). 지도 밀도·맥락 유지.
- **필터 적용 시 피드는 "매칭 상단 정렬"**: 조건에 맞는(안 흐린) 딜을 **피드 맨 위로**(hero=매칭 최저가), 안 맞는 건 흐린 채 아래로. 매칭 0건이면 빈 상태 문구.
- **피드 정렬 토글**(필터와 별개, 피드 상단): **가성비순(기본)·임박순·할인율순**. 매칭 그룹 내에서 이 기준으로 정렬.

## 전환 · 큐레이션 · IA — 2026-08-01 확정
- **전환(갈래) = 인라인 확장 상세**: 핀/카드 **클릭** → 플로팅 카드가 그 자리에서 확장(hover=컴팩트, click=상세). 상세 = 최근 30일 **가격 추이 스파크라인**(최저점 강조) + 직항/소요시간 + **예약 CTA** + "(광고) 수수료 고지". 바로 외부로 던지지 않고 "가격 진짜"를 먼저 보여줌(신뢰). 상세에서 예약 CTA만 외부 제휴로.
- **"오늘의 발견" 큐레이션 규칙(v1)**: hero("진짜 갈래말래?") = **매칭 중 할인율 최고**(동률이면 임박). 나머지는 정렬 토글 기준. hero는 정렬 바꿔도 고정.
- **"왜 지금" 훅**: 데이터로 계산 가능한 것만 규칙 생성 — `평소보다 N%↓`(중앙값 대비)·`이번 주말/연휴 출발`·`최근 최저`. 감성 카피는 규칙 결과에 얹기.
- **IA(네비)**: `발견`(지도+피드=홈) · `노선별`(SEO 26p). "오늘의특가"는 발견 피드와 중복이라 **삭제**. **알림(이메일 구독)은 나중(미구현) → 네비에서도 제외**(추후 v2, [[flight-deal-site-research]] 파이프라인 재활용).
- **빈 상태(위트 톤)**: 필터 0건 → "이 조건엔 딜이 없어요 — 조건을 바꿔보세요". 오늘 딜 없음 → "오늘은 조용하네요, 내일 다시 와요" 류.
- **모바일**: 추천안(피드=하단 가로 스크롤, 필터 도크=하단 시트, 플로팅 카드=하단 시트)으로 가되 **구현하며 확정**.

## 가격·데이터 정책 (신뢰가 핵심 자산)
- **최저가는 어그로용이되 정직하게**: 날짜 무관 최저(과거 제외)를 후킹으로 쓰되 **"최저 ₩000~ · 특정일 기준"**처럼 `~`와 기준 라벨 필수. 상세 진입 시 가격이 달라도 낚임 느낌이 안 나게.
- **커버리지=넓게, 표시=데이터 게이팅**: 지방 소도시까지 목록 유지(`dests.py`), 단 **유효한 최근 최저가가 있는 도시만 그날 핀 표시**. 얇은/계절 노선은 특가 뜬 날만 등장 → 자동으로 "갈 만한 곳만" 남음.
- 데이터 3일 지연 → "조회 시점 기준, 예약처 확인" 문구 유지.

## 신선도 배지 — 2026-08-22 확정
가격에는 **나이**가 있다. 우리 데이터는 Travelpayouts 캐시라 "우리가 스캔한 시각"(최대 3일 전)과
"소스가 그 가격을 마지막으로 본 시각"(`seen`, **최대 5일 전**)이 다르다. 사용자에게 의미 있는 건 후자다.
→ **딜을 지우는 하드 필터가 아니라, 가격에 붙는 신뢰 신호로 쓴다.** (근거·기각안: `DECISIONS.md`)

### 등급 3단계 (`seen` 기준, **방문 시점**에 프론트가 계산)
| 등급 | 나이 | 문구 | 점 | 톤 |
|---|---|---|---|---|
| `fresh` | < 24h | < 3h면 **"방금 확인"**, 아니면 **"N시간 전 확인"** | ● `--accent` (r 2.5) | `--ink` 700 |
| `ok` | 24 ~ 72h | 24~48h **"어제 확인"**, 이후 **"N일 전 확인"** | 없음 | `--sub` 500 |
| `stale` | > 72h | **"N일 전 확인"** | 없음 | `--sub` 500 · opacity .7 |
| (`seen` 없음) | — | **"스캔 시점 기준"** (기존 폴백) | 없음 | `--sub` 500 |

- **현실 분포**(2026-08-20 실측, 딜 143건): <3h 21% · <24h 36% · <48h 54% · <72h 74% · <5일 97%.
  → `fresh`가 1/3 이상이라 hero·상단을 채우기 충분하고, `stale`도 1/4 남짓이라 흐름을 해치지 않는다.
- **왜 방문 시점 계산**: 정적 사이트라 HTML은 하루 한 번 생성된다. 백엔드가 "3시간 전"이라는 **문자열**을
  구우면 다음 날 방문자에게 거짓말이 된다. 백엔드는 **절대 시각(`seen`)만** 주고, 배지는 브라우저에서 늙는다.
  캐시된 페이지를 봐도 배지가 알아서 나이를 먹으므로 **오래될수록 정직해진다.**

### 어디에 붙나 — 새 요소가 아니라 **가격 라벨의 내용**
신선도는 딜의 속성이 아니라 **가격의 속성**이다. 카드에 배지를 하나 더 얹지 않고,
이미 있는 가격 밑 마이크로 라벨 `발견가 · 스캔 시점` 을 **`발견가 · 어제 확인`** 으로 바꿔 채운다.
1. **피드 카드** — 가격 바로 아래 한 줄. (카드는 이미 사진·도시·가격·날짜·도장·훅·태그로 빽빽 → 요소 추가 금지)
2. **지도 플로팅 카드(컴팩트)** — 동일한 한 줄.
3. **확장 상세** — 요약이 아니라 **근거**를 준다: `2026-08-18 03:17 관측 · 3일 전` 절대 시각 + 기존 고지 문구.
   `stale`일 때만 한 줄 추가 — *"사흘 전에 본 가격이에요. 지금은 다를 수 있으니 예약처에서 확인해 주세요."*

### 색·시각 규칙
- **신호등(빨강·노랑) 금지.** (a) 코랄 단일 시그니처 원칙을 깨고 (b) 색각이상 안전 조합을 깨고
  (c) "우리 데이터 못 믿는다"를 우리 입으로 크게 외치는 꼴이 된다.
- 신선한 것만 **점 하나로 조용히 강조**하고, 오래된 건 경고하지 않고 **사실만 담담하게** 적는다.
  등급 차이는 색상이 아니라 **명도·굵기·불투명도**로만 낸다.

### 신선도가 관여하는 곳 / 관여하지 않는 곳
- ✅ **hero 게이트**: `갈래말래?` hero는 **`fresh` 후보 중 할인율 최고**. 가장 크게 보여주는 한 장은
  가장 믿을 만해야 한다. `fresh` 0건이면 → `ok`까지 완화 → 그것도 0이면 전체에서 뽑되 **배지는 그대로 노출**.
- ✅ **동점 타이브레이커**: 정렬 토글(가성비·임박·할인율) 값이 **같을 때만** 신선한 쪽이 위로.
- ❌ **정렬 토글 4번째("신선순") 없음** — 신선도는 사용자의 여행 욕구가 아니라 우리 품질 문제다.
- ❌ **필터 도크에 신선도 항목 없음** — 사용자에게 우리 데이터 품질을 관리시키지 않는다.
- ❌ **핀 인코딩 없음** — 핀 색은 이미 `가격`이다. 두 번째 색 인코딩은 채널 충돌.

### 하드 컷 (유일한 삭제 기준)
`seen`이 **7일보다 오래되면 `deals.json`에서 제외**(백엔드). 실측 최대치가 5일이라 **오늘 기준 손실 0건**이며,
"영원히 안 죽는 유령 가격"만 막는 안전선이다. 3일로 조이면 딜의 26%와 소도시 롱테일이 먼저 잘린다 → 안 한다.

## 사진(썸네일) 정책
- 목적지당 1장 큐레이션, **호버 시 lazy-load**, 작은 webp. 라이선스 Unsplash/Wikimedia 표기. 정적 사이트 용량 고려($0 유지).

## 모션
- 출발지 선택·단계 전환 → projection scale/center **트윈**으로 "지도가 스르륵 열림".
- 핀 hover/tap → 항로 곡선 draw(≈0.4s) + 카드 페이드. 과하지 않게.

## 보이스
- 친근·위트. 친구가 "야 여기 싸던데 갈래?" 하는 톤. 정보 나열형 회피.
- 태그라인: "어디, 갈까?" · "시간 남는데 싸게 다녀올 곳" · "이 예산이면 여기 어때요?".

## 접근성
- 모바일(터치) 우선: 탭 기반(호버 의존 금지), 터치 영역 ≥ 점 크기, 단계 버튼.
- 색각이상 안전(코랄 단일 시그니처 + 청록 중립). 텍스트 대비 확보. JS 없는 환경 `<noscript>` 대체.
