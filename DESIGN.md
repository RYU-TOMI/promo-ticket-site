# DESIGN.md — 디자인 시스템 (시각 언어)

> **소유: 기획 세션.** 이 문서는 **"어떻게 보이는가"만** 다룬다.
> 무드보드에서 확정(2026-08-01), **2026-08-22 구현값 기준으로 재정리**.
>
> | 찾는 것 | 문서 |
> |---|---|
> | 화면·상태·인터랙션 스펙 (챕터별) | **`SPEC.md`** |
> | 사이트맵·URL·네비 | `IA.md` |
> | 경로·분기·실패 | `FLOWS.md` |
> | 화면 문자열·보이스 | `COPY.md` |
> | 왜 그렇게 정했나 | `DECISIONS.md` |
> | 제품 본질 | `PRODUCT.md` |
>
> 코드 토큰은 `docs/assets/discover.css`(홈)와 `collector/theme.py`(노선 페이지).
> **불일치가 생기면 구현값이 정본이다** — 실제 화면에서 조정된 값이 더 옳다.

## 원칙
1. **지도가 주인공** — 발견 서비스다. 홈은 헤더 외 전부 지도.
2. **정보보다 감정** — "떠나고 싶다"를 먼저 일으키되, **AI틱함을 경계**한다(사람이 공들인 편집물처럼).
3. **프로 타이포 + 선택적 손맛** — 구조·본문은 Pretendard, 손맛은 악센트로만(로고 비행운, 특가 도장의 기울기).
4. **개성은 폰트 장난이 아니라 크기 대비·색·지도에서 나온다.** (손글씨 폰트 실험은 폐기 — `DECISIONS.md`)

## 컬러 토큰
| 토큰 | HEX | 역할 |
|---|---|---|
| `--accent` (시그니처) | `#F2603F` | 로고·버튼·핀·포인트·특가 |
| `--accent2` | `#C6472A` | 비행기 아랫면·눌림 상태 |
| `--ink` | `#20353A` | 제목·본문 텍스트 |
| `--sub` | `#5E7A7C` | 보조 텍스트·라벨 |
| `--sea` | `#EDF4F3` | 지도 바다·라벨 외곽선 |
| `--land` | `#D2E7DE` | 지도 육지 |
| `--coast` | `#33534F` | 지도 윤곽선·차트 라인 |
| `--line` | `#E6EDEC` | 테두리·구분선 |
| `--soft` | `#F0F5F4` | 칩 배경·안내 박스 |
| `--card` | `#FFFFFF` | 카드·헤더 표면 |
| `--bg` | `#F4F8F7` | 앱 배경·서브페이지 |

- **시그니처색은 코랄 하나로 통일.** 두 번째 강조색을 만들지 않는다.
- 차트 데이터 라인은 `--coast`(청록), 강조·최저점만 `--accent`.
- **색을 두 가지 의미에 겹쳐 쓰지 않는다.** 지도 핀 색은 이미 "가격"에 배정돼 있다(`SPEC.md` §CH1).
- 다크 모드: 배경 `#0F2A29`, 텍스트 `#EAF3F0`, accent `#FF7A57` 계열 (별도 매핑, 미착수).

## 타이포그래피 — Pretendard Variable
`font-family: 'Pretendard Variable', Pretendard, sans-serif` — 폰트만 CDN 허용(그 외 런타임 CDN 의존 0).

| 역할 | weight · size | 비고 |
|---|---|---|
| Display / Hero | 900 · 2.6–2.9rem | `letter-spacing: -.035em`. "어디, 갈까?" |
| Heading | 900 · 1.3–1.5rem | 섹션 제목 |
| Subhead | 700 · 1.05–1.4rem | 노선명 등 |
| Body | 400/500 · 1rem | `line-height: 1.6` |
| Label | 700 · 0.78–0.85rem · `--sub` | 날짜·필드 라벨 |
| Micro | 700 · 0.56–0.74rem | 칩·태그·도장 (지도 위 밀집 영역) |
| Price | 900 · `tabular-nums` | 가격 |

- **손글씨·세리프 폰트 금지.** 아마추어로 읽힌다(실험 후 폐기 — `DECISIONS.md`).
- 홈은 지도 위 정보 밀도가 높아 **Micro 단계를 적극 쓴다**. 노선 페이지는 읽는 문서이므로 Body 위주.

## 로고
- 워드마크 **갈래말래** — Pretendard 900, `-.03em`, 현행 `1.3rem`. **"말래"만** `--accent`.
- "말래" 밑에 **페이드 비행운**(투명→코랄 그라디언트 스트로크, 우상향 곡선) + 끝에 **접힌 종이비행기**
  (윗면 `--accent`, 아랫면 `--accent2`, 흰 크리스 `#ffffff88`).
- 비행기·항적은 **워드마크 폭 안에 가둔다**(메뉴 침범 금지). 로고–메뉴 간격 ≥ 26px.
- 노선 페이지 헬퍼: `theme.logo()`.

## 스페이싱 · 라운드
8px 리듬 기준. **라운드는 요소가 작을수록 작게** — 지도 위 컨트롤이 많아 큰 라운드는 뭉개져 보인다.

| 대상 | 라운드 | 현행 |
|---|---|---|
| 플로팅 패널 (필터 도크·플로팅 카드) | 14px | ✅ |
| 카드 (피드) | 13px | ✅ |
| 작은 컨트롤 (줌·썸네일) | 10px | ✅ |
| 버튼 (CTA) | 8–9px | ✅ |
| 특가 도장 | 4px | ✅ |
| 칩·pill·태그 | 99px (완전 라운드) | ✅ |

- 헤더 높이 **56px**.

## 컴포넌트 (현행 구현값)

| 컴포넌트 | 정의 |
|---|---|
| **CTA 버튼** | `--accent` 배경 · 흰 글자 · 800 · radius 9 · `.8rem` |
| **보조 버튼** | 흰 배경 · `1.5px --line` 테두리 · `--sub` |
| **pill (출발지·단계)** | `1.5px --line` · radius 99 · `.78rem/700` · `--sub` / 활성 `.on` = `--accent` 배경 흰 글자 |
| **필터 칩** | `1px --line` · radius 99 · `.74rem/700` / 활성 = `--accent` 배경 흰 글자 |
| **정렬 칩** | `.68rem/700` / 활성 = **`--ink` 배경** 흰 글자 (필터와 구분하려 코랄이 아니다) |
| **분위기 태그** | `--soft` 배경 · `--sub` · `.6rem/700` · radius 99 |
| **특가 도장** | 회전 `-7°` · `1.5px --accent` 테두리 + 같은 색 글자 · radius 4 · `.56rem/900`. 예: `특가 41%↓` |
| **hero 배지** | 흰 배경 · `--accent` 글자 · `.6rem/900` · radius 99 · 그림자. 예: `진짜 갈래말래?` |
| **날짜 배지** | `--accent` 배경 · 흰 글자 · `.62rem/800` · radius 99 |
| **피드 카드** | 흰 배경 · `1.5px --line` · radius 13 · 썸네일 62px(radius 10) |
| **플로팅 카드** | 폭 184px · radius 14 · 그림자 `0 12px 32px rgba(20,40,40,.26)` |
| **필터 도크** | 폭 270px · radius 14 · 그림자 `0 10px 30px rgba(20,40,40,.18)` · 우측 하단 |
| **줌 컨트롤** | 흰 라운드 10 · `＋`／`－` · **우측 상단** |
| **안내 박스** | `--soft` 배경 · **점선** 테두리 · radius 10 · `.76rem` |
| **헤더** | 흰 배경 + 하단 `--line` · 로고 좌 · 네비(`발견`·`노선별`) · 우측 출발지 pill |

## 지도 스타일

### 데이터
- **Natural Earth 50m** + DP 단순화 (`docs/data/world.geojson`, 182 features, ~161KB).
- **왜 50m인가**: 110m에는 **제주·오키나와·괌·발리·푸켓 같은 섬이 아예 없어서 핀이 바다에 뜬다.**
  (2026-08-22 확인: 현재 파일은 이미 50m본이다. 이전 문서의 "110m 축소본 — 50m 준비 필요" 서술은 **오기였고 삭제**했다.)
- 렌더: `d3-geo`의 `geoEquirectangular` + `geoPath`. 벤더링 `docs/assets/d3-geo.min.js` + `d3-array.min.js`
  (전역 `d3`, **로드 순서 중요**). 런타임 CDN 의존 0.

### 색·선
| 요소 | 값 |
|---|---|
| 바다 (배경) | `--sea` |
| 육지 | `--land` |
| 해안선 | `--coast`, 얇게, round join |
| 핀 코어 | 채움 = 가격 색 스케일, `stroke #fff 2.2`, `r 6` |
| 핀 후광 | 같은 색, `r 9.6`, `opacity .16` |
| 핀 활성 | `stroke-width 3`, `r 9`, 후광 `opacity .28` |
| 핀 흐림(dim) | `opacity .28`, `stroke-width 1.4` |
| 도시 라벨 (major) | `13px/800` `--ink`, **핀 아래**, `paint-order: stroke` + `--sea` 외곽선 3px |
| 도시 라벨 (minor) | `11px/700` `#4a6a66` |
| 출발지 마커 | ring `r 9` + dot `r 4` + 라벨 **위** |

> 라벨 외곽선(`paint-order: stroke`)은 **필수**다. 없으면 해안선·핀 위에서 글자가 읽히지 않는다.

- **핀 색 스케일**: `#F7B29E`(비쌈) → `#D63C1E`(쌈). 상대값 계산 규칙은 `SPEC.md` §CH1.
- **서울 통합**: 인천·김포는 "서울" 한 핀. 출발지는 항상 한국.

## 모션
- **과하지 않게.** 발견 서비스라 "지도가 열리는" 느낌은 필요하지만, 조작할 때마다 기다리게 하지 않는다.
- 출발지 선택·거리 단계 전환 → projection `scale`/`center` **트윈**으로 스르륵 열림. (⚠️ 미구현 — `SPEC.md` B2)
- 핀 hover/tap → 항로 곡선 draw(≈0.4s) + 카드 페이드.
- 현행 트랜지션: 카드 `.12s`, 플로팅 카드 `.14s`, 화면0 페이드 `.5s`.

## 접근성 (시각)
- **색각이상 안전**: 코랄 단일 시그니처 + 청록 중립. **정보를 색으로만 전달하지 않는다** —
  가격은 핀 색과 함께 카드의 숫자로도 읽히고, 신선도는 점 + 문구로 함께 전달한다.
- 텍스트 대비 확보. `--sub`(`#5E7A7C`)를 `--card` 위에 쓸 때가 하한선이며, 그보다 옅은 회색을 만들지 않는다.
- 터치 우선: 탭 영역은 점 크기보다 크게. 호버에만 있는 기능을 두지 않는다.
- JS 없는 환경 `<noscript>` 대체 유지.

> 조작 접근성(키보드·`aria`·포커스)은 **미구현**이며 스펙도 미정이다 → `SPEC.md` §CH6 / B11.
