# design/ — 기획·디자인 산출물 (협업용)

다른 세션/사람/AI 모델과 협업할 때 **눈으로 확인**할 수 있는 디자인 근거.

- **feed_map.html** ⭐ (최신 확정 홈 레이아웃) — 지도(무대) + 좌 "오늘의 발견" 카드 피드 + 핀 플로팅 카드.
  카드↔지도 양방향 연동(카드 hover→항로, 핀 hover→카드), 날짜·"왜 지금" 훅·갈래/말래. `python design/build_feed_map.py`로 재생성.
- **storyboard.html** — 발견 지도의 상태별 정지 장면(입장 / 일본 확대 / 호버 / 동남아 축소·확대). LOD(축소=주요, 확대=소도시) 확인용.
  실제 d3-geo 지도·좌표로 렌더되며 d3·world.geojson이 인라인돼 있어 **더블클릭(파일 열기)만으로** 확인 가능.
- **freshness.html** — 신선도 배지 확정 스펙(등급 3단계·부착 위치 3곳·실측 분포·기각안·계약 영향).
  손으로 쓴 정적 페이지라 생성기 없음. `../SPEC.md` §CH3 "신선도 배지"의 시각 대응물.
- **stages.html** ⭐ (PH1 진행 중) — 거리 단계 후보안 비교. `＋유럽·미주`에 미주가 없는 원인을
  투영 범위·LOD 규칙·라벨 겹침 세 가지로 분해하고, 후보 4안을 실제 해안선·실제 딜 좌표로 나란히 렌더한다.
  `python design/build_stages.py`로 재생성.
- **build_*.py** — 위 HTML 생성기. 도시·뷰·LOD 임계값을 바꿔 다시 뽑으려면:
  ```
  python design/build_feed_map.py     # 홈 레이아웃
  python design/build_storyboard.py   # LOD 스토리보드
  ```
  (저장소 `docs/assets/d3-*.min.js` + `docs/data/world.geojson`를 읽어 인라인.)

## 확정 스펙의 단일 출처
- 디자인 시스템(시각 언어)·색·타이포·로고: **`../DESIGN.md`**
- 발견 인터랙션·화면 상태·LOD·가격/데이터 정책: **`../SPEC.md`** (챕터별 §CH1~§CH6)
- 사이트 구조: **`../IA.md`** · 경로·실패: **`../FLOWS.md`** · 문구: **`../COPY.md`**
- 제품 본질(대상·목적·포지셔닝): **`../PRODUCT.md`**
- 상태·로드맵·규칙·열린 결정: **`../PROJECT.md`**

> 이 폴더의 목업은 **룩앤필 합의용**이며 스타일라이즈드 샘플 데이터를 씁니다.
> 실제 사이트는 `docs/`(정적 생성 + 벤더링 d3-geo)에서 구현됩니다.
