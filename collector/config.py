# -*- coding: utf-8 -*-
"""수집 대상 노선과 특가 판정 기준 설정"""

# (출발지, 도착지) IATA 코드. 2026-07-09 Travelpayouts 커버리지 검증 완료 노선 중심.
ROUTES = [
    # 일본
    ("ICN", "NRT"), ("ICN", "KIX"), ("ICN", "FUK"), ("ICN", "OKA"),
    ("ICN", "CTS"), ("ICN", "NGO"),
    # 중화권/동남아
    ("ICN", "TPE"), ("ICN", "HKG"), ("ICN", "BKK"), ("ICN", "DAD"),
    ("ICN", "SGN"), ("ICN", "HAN"), ("ICN", "MNL"), ("ICN", "CEB"),
    ("ICN", "SIN"), ("ICN", "KUL"), ("ICN", "DPS"),
    # 휴양지/기타
    ("ICN", "GUM"),
    # 장거리
    ("ICN", "CDG"), ("ICN", "LHR"), ("ICN", "FCO"), ("ICN", "BCN"),
    ("ICN", "JFK"), ("ICN", "LAX"), ("ICN", "SYD"),
    # 국내
    ("GMP", "CJU"),

    # ---- 부산 출발 (2026-09-01 추가) ----
    # 왜: 딜의 29%(37건)가 부산인데 노선 페이지가 0개였다. 목적지만 보고 링크하면
    #     부산 딜이 인천 노선 분석으로 가는데, 가격도 시세도 다른 노선이다(BB22).
    #     목적지를 넓히는 건 페이지를 더하는 것이고 출발지를 넓히는 건 틀린 것을
    #     고치는 것이라, 기획이 출발지를 먼저 넓히라고 판단했다.
    # 선정: 부산 딜에 실제로 자주 잡히는 순으로 후보를 받아, v3 API가 하루에
    #     10건 이상 돌려주는 곳만 넣었다(2026-09-01 실측). 오키나와는 8건이라 뺐다 —
    #     표본이 얇으면 월별·요일별 차트가 비어 얇은 SEO 페이지가 된다.
    ("PUS", "CJU"), ("PUS", "NRT"), ("PUS", "KIX"), ("PUS", "FUK"),
    ("PUS", "SHA"), ("PUS", "DAD"), ("PUS", "PQC"), ("PUS", "TPE"),
    ("PUS", "CEB"),

    # ---- 타이중 (2026-09-02 추가, BE4 T2) ----
    # 왜: 광역 응답의 70.8%를 사전에 없다는 이유로 버리고 있었는데(BE3 T5),
    #     그중 한국발 직항이 있는 16곳을 추려 기획이 6곳을 채택했다. 그 6곳 중
    #     **노선 페이지 기준(하루 10건)을 넘는 건 타이중뿐**이다 —
    #     ICN-RMQ 응답 11건이 전부 직항이었다(2026-09-02 fetch_route 실측).
    #     나머지는 발견 피드에만 넣는다(보홀 9 · 이시가키 3 · 코타키나발루 2).
    # ⚠️ 오늘은 페이지가 안 생긴다. 수집 이력이 없으면 `route_page()`가 None을
    #     돌려주고, `_route_code()`도 목록이 아니라 실제 생성된 페이지로 판정하므로
    #     프론트가 404로 가지 않는다. 내일 크론이 수집한 뒤부터 나타난다.
    ("ICN", "RMQ"),
]

CURRENCY = "krw"

# 특가 판정: 해당 노선 최근 BASELINE_DAYS일 가격 중앙값 대비 DEAL_RATIO 이하이면 특가
BASELINE_DAYS = 30
DEAL_RATIO = 0.65

# 노선·유형(직항/경유)별 최소 표본 수 (이보다 적으면 판정 보류)
MIN_SAMPLES = 10
