# COPY.md — 카피 덱 (화면에 나오는 모든 문자열)

> **소유: 기획 세션.** 2026-08-22 작성 — **실제 구현에서 전수 수집**했다.
> 프론트는 **여기 없는 문자열을 지어내지 않는다.** 필요하면 기획 세션에 요청한다.
> 톤의 근거는 `PRODUCT.md`(브랜드 목소리), 화면 위치는 `SPEC.md`.

## 왜 이 문서가 있나

우리 차별점은 목소리다. 그런데 문자열이 `discover.js`·`discover_home.py`·`build_site.py` 세 곳에
흩어져 있으면 **톤을 비교할 수가 없다.** 비교할 수 없으면 무너지는 걸 아무도 못 본다.
"AI틱함"의 1순위 원인이 폰트가 아니라 **문장**이라서, 문장을 한 곳에 모은다.

---

## 1. 보이스 규칙

### 말투 — 화면에 따라 둘로 나눈다 (의도된 분리)
| 영역 | 말투 | 예 |
|---|---|---|
| **발견 홈** (지도·피드·필터·카드) | **해요체** — 친구가 말 거는 톤 | "출발 공항을 고르면 오늘 싼 곳들이 열려요" |
| **노선 페이지 · 법적 고지 · 오류** | **합니다체** — 분석·신뢰 톤 | "최근 30일 수집한 가격 1,204건으로 분석한 시세입니다" |

> 홈은 **감정**(떠나고 싶다)을, 노선 페이지는 **근거**(진짜 싸다)를 담당한다. 말투를 섞지 않는다.

### 원칙
1. **친근하되 반말은 아니다.** 목표는 "야 여기 싸던데 갈래?"의 **온도**지 반말 자체가 아니다.
2. **신뢰 사안은 담백하게.** 가격·고지·오류·빈 상태에서 장난치지 않는다.
   위트는 **발견·유도** 문구에만 쓴다. (신선도 배지가 오래된 딜을 경고하지 않고 사실만 적는 이유와 같다)
3. **정확한 숫자를 쓴다.** 근사·과장 금지 — "14만원대~"가 아니라 `142,500원~`. (`DECISIONS.md`)
4. **정보 나열형을 피한다.** 대구를 맞춘 3항목 나열, "~해보세요"의 반복은 AI틱함의 신호다.
5. **시즌 한정 표현 금지.** "광복절 연휴" 같은 건 지금만 유효해서 문구가 썩는다. 상대 표현만.
6. **발견 홈에서 "특가"라는 단어를 쓰지 않는다.** (2026-08-22 확정 — `SPEC.md` F19)
   제품 안에 정의가 둘이었다 — 노선 페이지·알림은 **급락 사건**(`중앙값의 65%`=`35%↓`),
   발견 홈은 **평소 시세와의 편차**. 재는 것이 다르므로 **단어를 나눈다.**
   발견 홈은 `평소보다 N%↓`라는 **서술만** 쓴다.

### ⚠️ 주장할 수 있는 것 / 없는 것 (2026-08-22 — 문구 쓰기 전 필독)

화면 문구는 **`deals.json`에 있는 것만** 주장할 수 있다. 없는 걸 추정해서 사실처럼 쓰면
그게 F18(피드의 54%가 근거 없는 인과를 단언)이 만들어진 경로다.
**이 표를 보지 않고 훅·배지·카피를 쓰지 않는다.**

| 사실 | 근거 | |
|---|---|---|
| 직항 여부 | `transfers == 0` | ✅ 직항 45건(43%). **2026-08-22부터 날짜 줄에 상시 표시** |
| 경유 횟수 | `transfers` | ✅ |
| 거리(km) | `lat`/`lon` 대권거리 (프론트 계산) | ✅ 검산: 제주 451 · 후쿠오카 540 · 호치민 3,599 · LA 9,584 |
| 거리대 | `haul` | ✅ short 31 · mid 36 · long 27 |
| 출발일·기간·상대시점 | `dep` `ret` `nights` `when` | ✅ |
| 가격의 나이 | `seen` | ✅ |
| 평소 시세 대비 | `median` `discount` | ✅ 단, **직항/경유 구분 없음** |
| **소요시간** | — | ❌ **필드 없음** (`duration`·`minutes`·`flight_time` 전부 없음) |
| **직항 대비 가격** | — | ❌ **필드 없음** → "경유해서 싸졌다"를 말할 수 없다 |
| **항공사** | — | ❌ 필드 없음 |

> **추정을 사실로 바꾸지 않는다.** 거리(km)는 좌표에서 정확히 나오지만 거기서 비행시간을
> 계산하면(km÷속도) 다시 추정이다. 기종·항로·바람에 따라 다르고, **경유 53건은 대기시간이
> 지배적**이라 거리로는 아무것도 못 맞춘다.
>
> 실제로 이 표가 없어서 기획 세션이 F18을 고치겠다면서 대안으로 "3시간이면 도착"을 예로 들었고,
> **그것 역시 없는 데이터였다**(프론트 세션이 잡음). 같은 실수를 두 번 하지 않으려고 표로 박는다.

### 태그라인 (2026-08-01 확정 — 재사용 가능한 자산)
- **"어디, 갈까?"** — 대표. 히어로·메타 타이틀에 사용 중.
- **"시간 남는데 싸게 다녀올 곳"** — 설명형. 메타 description 계열.
- **"이 예산이면 여기 어때요?"** — 예산 필터·빈 상태 유도에 어울림. **아직 화면에 안 쓰임.**

### 🔴 변수 뒤에 조사를 붙이지 않는다 — 붙여야 하면 **받침으로 고른다** (2026-09-04)

`{도시}는`처럼 **변수 바로 뒤에 조사**를 쓰면 화면에 **`베이징는`**이 나간다.
한국어 조사는 앞 글자의 **받침**을 따르는데, 변수 값은 매일 바뀐다.
프론트가 구현하다 발견했다 — **문서에 적힌 문구를 그대로 못 쓴 첫 사례**다.

**전수 확인 결과 이 문서에 그런 자리는 `COPY.md:280` 한 곳뿐이다**(2026-09-04 스캔).

| 자리 | 위험 | 왜 |
|---|---|---|
| `{도시}는` · `{도시}가` · `{도시}를` | 🔴 **위험** | 도시명 받침이 매일 다르다 |
| `{출발지} 출발은` · `{최다 허브}에서` | ✅ 안전 | 조사가 **고정 명사**(`출발`) 뒤에 붙거나, `에서`처럼 받침을 안 가린다 |
| `{N}곳이에요` · `{N}일 전` | ✅ 안전 | 같은 이유 |

**두 가지 중 하나로 쓴다.**

1. **조사를 피해 문장을 짠다** — `{도시} 노선은` 처럼 **고정 명사를 사이에 넣는다.** 이쪽이 낫다.
2. 피할 수 없으면 **한글 종성으로 고른다**: `(코드 − 0xAC00) % 28 != 0` 이면 받침 있음.
   해당 조사 쌍 — `은/는` · `이/가` · `을/를` · `과/와` · `으로/로`.

⚠️ **이건 문구를 지어내는 게 아니라 문법을 맞추는 것**이다. `COPY.md` 원칙("없는 문자열을
지어내지 않는다")과 안 부딪힌다. **다만 새 문자열을 쓸 때 이 자리를 만들지 않는 쪽이 먼저다.**

### 금지어·금지 패턴
- 공허한 형용사: `최고의` `완벽한` `특별한` `혁신적인` `스마트한`
- **비교 최상급 금지**: `역대급` `최저가 갱신` `올해 최저` — **역대 데이터가 계약에 없다.**
  지금은 도장이 커지는 걸로 충분하다. 시각이 말하게 두고 문구는 숫자만 적는다.
  - ⏳ **대체 예정**: 히스토리 필드가 들어오면 **`{N}일 중 최저가예요`**를 쓴다.
    `역대급`보다 약해 보이지만 **확인 가능해서 더 강하다.** 그리고 **N이 자라면 문구도 저절로 세진다.**
    `broad_offers`에 이미 23일치가 쌓여 있다(조합당 중앙값 12일). → `SPEC.md` §CH3, PH4에서 계약 요청.
- 느낌표 남발, **이모지 남발**
  - 허용된 기호는 둘뿐이다: 노선 페이지 브랜드바의 `✈️`, 직항 배지의 **`✈`**(U+2708, 변이 선택자 없음 —
    **텍스트 글리프**라 글자 색을 상속한다. 색 이모지가 아니다).
  - 그 외 기호·이모지를 새로 들이려면 **어떤 정보를 대신하는지** 근거가 있어야 한다.
- 불필요한 영어: `딜`은 내부 용어다. **화면에는 "특가"·"싼 곳"으로 쓴다.**
- "당신" — 한국어 UI에서 번역체로 읽힌다. 주어를 생략한다.

---

## 2. 발견 홈 — 현행 문자열 전수

### 헤더
| 위치 | 문자열 |
|---|---|
| 로고 | `갈래말래` ("말래"만 코랄) |
| 네비 | `발견` (활성) · `노선별` |
| 출발지 pill | `출발지 ▾` → 선택 후 `{도시} 출발 ▾` |

### S0 출발지 선택
| 위치 | 문자열 |
|---|---|
| 제목 | `어디서 출발하세요?` |
| 부제 | `출발 공항을 고르면 오늘 싼 곳들이 열려요` |
| **딜 0건일 때 (제목)** | **`오늘은 조용하네요`** (2026-08-22 확정, C-1) |
| **딜 0건일 때 (설명)** | **`수집한 특가가 없어요. 내일 아침에 다시 채워집니다.`** |
| **딜 0건일 때 (대안)** | **`노선별 최저가는 그대로 볼 수 있어요`** + 노선 페이지 링크 |
| 핀 라벨 | `서울` `부산` `대구` `제주` (딜이 있는 허브만) |

### S1 지도 무대
| 위치 | 문자열 |
|---|---|
| 안내 | `카드에 올리면 지도에 항로가 · 핀 클릭하면 상세가 열려요` |
| 거리 단계 | **`가까운 곳`** · **`조금 더 멀리`** · **`아주 멀리`** (2026-08-22 확정) |
| 거리 단계 (필터 ON) | 버튼 대신 **`전 지역에서 찾는 중`** 한 줄 |
| 단계 스테퍼 | **`＋` = 더 멀리** · **`－` = 가까이** — 기호만 두지 말고 뜻이 보이게 |
| 출발지 라벨 | `{도시} 출발` |

### S3 필터 도크
| 위치 | 문자열 |
|---|---|
| 토글 (접힘) | 조건 없으면 **`필터`** · 있으면 **조건 문구** (개수 아님, 2026-08-22 확정) |
| 접힘 예시 | `해변` · `50만 이하` · `이번 주말 · 해변` · `이번 주말 · 해변 외 1` |
| 라벨 | `언제 갈래요?` · `분위기` · `예산` |
| 날짜 칩 | `아무때` · `이번 주` · `이번 주말` · `다음 달` · `날짜 지정` |
| 분위기 칩 | **`해변` · `도시` · `미식` · `자연` · `문화` · `온천`** (6종, 2026-08-22 확정) |
| 예산 | `{N}만 이하` — `N = 값 ÷ 10,000` (step이 5만이라 항상 5의 배수) |
| 예산 (최대치) | **`제한 없음`** — 이때 예산 필터는 꺼진 상태다 |

### S2 카드 피드
| 위치 | 문자열 |
|---|---|
| 제목 | `오늘의 발견` |
| 부제 | `{출발지} 출발 · N곳` / **필터 시 `조건에 맞는 {N}곳`** (2026-08-22 확정) |
| 정렬 | `가성비순` · `임박순` · `할인율순` |
| hero 배지 | `진짜 갈래말래?` |
| hero CTA | `갈래 → 자세히 보기` |
| 가격 | `₩{n} ~` |
| 날짜 (1단·스캔) | `{상대라벨}` 배지(코랄) + `{N박N일}` |
| 날짜 (2단·확인) | `{M/D(요일)}~{M/D(요일)}` — 작고 옅게 |
| | **교통은 여기 넣지 않는다**(가격 줄 우측으로 이동) |
| 교통 (가격 줄 우측) | 중·장거리 직항 **`✈ 직항`**(배지) · 근거리 직항 **`직항`**(텍스트) · 경유 **`경유 {N}회`**(텍스트) |
| 빈 상태 (필터 0건) | **`이 조건엔 맞는 곳이 없어요 — 조건을 바꿔보세요`** (2026-08-22 수정, C-2) |
| 희소 안내 (10건 미만) | **1순위** `더 멀리까지 보면 {M}곳이에요.` (거리 단계를 넓히면 늘 때) · **2순위** `{출발지} 출발은 오늘 {M}곳이에요.` + `{최다 허브}에서 출발하면 {K}곳까지 늘어나요.` — 2026-09-04 개정, 근거는 `SPEC.md` §CH3 |

### ~~"왜 지금" 훅~~ — **폐기** (2026-08-22)
훅 줄을 없앴다. 우리가 못 하는 주장을 하고 있었다 — `경유로 확 싸진 특가`가 피드의 **54%**에 붙었는데
직항 대비 가격 데이터가 없다(`SPEC.md` F18). 그 자리에 있던 사실은 각자 제자리로 갔다.

| 예전 훅이 말하던 것 | 지금 |
|---|---|
| `평소보다 {N}%↓` | **도장** — `discount ≥ 15%`일 때만 |
| `경유로 확 싸진 특가` | **삭제** (근거 없음) |
| `이번 주말 바로 출발` | 날짜 줄이 이미 말한다 |
| `{상대라벨} 최저가` | 날짜 줄이 이미 말한다 |
| — | **`직항` / `경유 {N}회`가 새로 추가** — 자리는 **가격 줄 우측**이다(위 S2 표). ⚠️ 여기 원래 "날짜 줄에"라고 적혀 있었는데 **같은 문서 안에서 갈라진 것**이라 2026-09-04에 위 표 기준으로 맞췄다 |

### S4·S5 플로팅 / 확장 상세
| 위치 | 문자열 |
|---|---|
| 사진 자리 | `사진 준비중` |
| 할인 도장 | **`평소보다 {N}%↓`** — `discount ≥ 15%`일 때만. 미만이면 **도장 없음** |
| 할인 도장 (티어) | **문구는 T1·T2·T3 모두 같다.** 계단은 시각으로만 낸다(`DESIGN.md`) |
| 직항 배지 | **`✈ 직항`** — 중·장거리 직항에만. 근거리는 날짜 줄 텍스트로만 (2026-08-22 확정) |
| CTA | `갈래 → 자세히 보기` |
| 비교 섹션 제목 | `평소 시세와 비교` |
| 비교 행 | `평소 시세(중앙값)` / `발견가` |
| 기간 최저 (조건부) | **`{N}일 중 가장 싼 가격이에요`** — `obs_days≥7` + 신기록 + 낙폭 5%↑ 일 때만 (오늘 10%). 아니면 **아무 말도 안 한다** |
| 예약처 섹션 제목 | `어디가 제일 싼지 비교해보세요` |
| 예약처 버튼 | `{예약처명}` + `최저가 보기 →` |
| 공유 버튼 | **`공유`** — 확장 상세. `navigator.share` 없으면 클립보드 복사 |
| 공유 완료 | **`링크를 복사했어요`** (짧게 뜨고 사라짐) |
| 예약처 없음 | **`예약처 링크를 준비하지 못했어요`** — `links`가 빈 장애 상황 |
| **광고·가격 고지** | ~~`… 일부는 예약 시 수수료 (광고)`~~ → 아래로 교체 (2026-09-02) |

> 고지 문구는 **법적 요구사항**(정보통신망법·공정위)이다. 임의로 줄이거나 톤을 바꾸지 않는다.

### 🔴 제휴 고지 — 2026-09-03 크론부터 필요 (확정 2026-09-02)

**무엇이 바뀌나**: `TP_MARKER` 등록으로 **Aviasales 한 곳에만 실제 수수료가 붙는다.**
어제까지는 배포본에 `marker` 문자열이 **0건**이라 *"일부는 수수료"*의 "일부"가 **실질 0곳**이었다.
**이제 진짜로 1곳이 되므로 뭉뚱그린 고지로는 부족하다.**

**① 항목 옆에 `(광고)`** — Aviasales에만. 나머지 4곳엔 아무것도 안 붙인다.

```
스카이스캐너            412,000원
네이버 항공권           409,500원
구글 항공권             415,000원
Trip.com              407,800원
Aviasales  (광고)      411,200원
```

**② 하단 고지를 교체한다**

| | |
|---|---|
| 전 | `위 가격은 발견가(스캔 시점) · 실시간 최저가는 각 사이트에서 확인하세요 · 일부는 예약 시 수수료 (광고)` |
| **후** | **`위 가격은 발견가(스캔 시점) · 실시간 최저가는 각 사이트에서 확인하세요`**<br>**`(광고) 표시는 예약하시면 저희가 수수료를 받는 링크예요 · 가격은 같아요`** |

**왜 이렇게 하나 — 강조와 고지는 목적이 다르다**

2026-08-06에 *"중립 비교라면서 왜 하나만 빨간색이야?"*로 **강조를 없앤 게 중립화 결정**이었다.
그런데 지금 필요한 건 **강조가 아니라 고지**다.

| | 무엇을 말하나 | 중립성 |
|---|---|---|
| **강조** | "여기가 좋아요" | ❌ 깬다 |
| **고지** | "여기는 **우리가 돈을 받아요**" | ✅ **불리한 사실을 밝히는 것**이라 오히려 강화한다 |

- 🔴 **`(광고)`는 시각적으로 약하게** — 작은 회색. **눈에 띄게 만들면 그게 강조가 된다.**
- 🔴 **나머지 `tag`(`전체 비교`·`한국 인기`·`중립`·`한국어`)는 계속 숨긴다.**
  그건 **우리가 매기는 평가**라 중립성을 깬다. **`(광고)`만 사실 고지다.**
  → `CONTRACT.md`의 *"현재 프론트는 미표시"*는 **그대로 유효**하고, `(광고)`만 예외다.
- **`영어·수유료` 같은 복합 라벨을 쓰지 않는다.** *"영어 사이트"*는 유용한 정보지만
  **고지와 섞으면 둘 다 흐려진다.** 언어 표시는 별도 안건으로 남긴다.

> ⚠️ **`(광고)` 문구는 백엔드 `affiliates.py`의 `tag` 값이 아니라 프론트가 붙인다.**
> `tag`는 데이터고 `(광고)`는 **화면 규칙**이다 — 마커가 붙은 링크에만 표시한다.

> ⚠️ **저는 변호사가 아니다.** 위 문구는 *"경제적 이해관계를 소비자가 쉽게 인식하게 공개"*라는
> 공정위 취지에 맞춘 **보수적 안**이다. **`(광고)` + 구체적 설명**이라는 형태 자체는
> 국내 관행에 부합하지만, 최종 확인은 사람이 해야 한다.

> ✅ **`가격은 같아요`는 확인 후 붙였다** (2026-09-02, 백엔드 확인).
> Travelpayouts 공식 문서: *"receives fees directly from the brands"* — 수수료는
> **브랜드(항공사·OTA)가 지불**하므로 사용자는 제휴 링크로 가나 직접 가나 **같은 가격**을 낸다.
> **이 문장이 없으면 `"그럼 내가 더 내나?"`가 남는다.**
>
> ⚠️ **다만 1차 출처는 헬프센터 문서**이고 제휴 약관 원문(공개 오퍼)을 조항 단위로 확인한 건 아니다.
> 계약 조항으로 확실히 하려면 `Affiliate Agreement`를 짚어야 한다.

### `links[].tag` 값 — 백엔드가 만드는 화면 문자열

⚠️ **이 다섯은 `collector/affiliates.py`에 있고 여기 없었다.**
`COPY.md`가 "화면 문자열 전수"를 표방하는데 **백엔드가 만드는 문자열이 대조 범위 밖**이었다.

| 예약처 | `tag` | 화면 표시 |
|---|---|---|
| 스카이스캐너 | `전체 비교` | ❌ 숨김 |
| 네이버 항공권 | `한국 인기` | ❌ 숨김 |
| 구글 항공권 | `중립` | ❌ 숨김 |
| Trip.com | `한국어` | ❌ 숨김 |
| **Aviasales** | `영어·수수료` | ❌ 숨김 — **대신 프론트가 `(광고)`를 붙인다** |

### S6 noscript
| 위치 | 문자열 |
|---|---|
| 안내 | `JavaScript가 꺼져 있어 지도를 표시하지 못했습니다. 오늘의 특가와 노선별 분석은 아래에서 볼 수 있어요. (갱신 {시각})` ⚠️ 말투 혼재(§4) |
| 제목 | `오늘의 특가` · `노선별 최저가 분석` |
| 항목 | `{도시} · {가격}원~ · {상대라벨} 출발` |
| 비었을 때 | `수집된 특가가 아직 없습니다.` |

### 분위기 태그 (카드에 출력)
**어휘 24종 — 상위 6종(필터) + 하위 18종(표시)** (2026-08-22 확장, `CONTRACT.md`)

| 상위 (필터 칩) | 하위 (카드에서 이걸 앞세운다) |
|---|---|
| `해변` | `리조트` · `스노클링` · `서핑` · `섬` |
| `도시` | `야경` · `쇼핑` · `마천루` · `골목` |
| `미식` | `야시장` · `길거리음식` |
| `자연` | `화산` · `트레킹` · `사막` · `폭포` |
| `문화` | `사원` · `유적` · `고성` · `미술관` |
| `온천` | — (그 자체로 구체적) |

**표시 규칙: 하위 전부 + 상위 1개** (하위가 없으면 상위 2개), **최대 4개.**
하위끼리는 `tags` 배열 순서를 따른다.

⭐ **개수를 고정하지 않는다 — 개수 자체가 "즐길 게 얼마나 많은가"를 말한다.**

| | 표시 |
|---|---|
| 할 게 많은 도시 | `도쿄 = 야경 · 골목 · 쇼핑 · 미식` · `파리 = 미술관 · 골목 · 유적 · 미식` · `두바이 = 사막 · 마천루 · 쇼핑 · 도시` |
| 할 게 적은 도시 | `싼야 = 리조트 · 해변` · `프랑크푸르트 = 마천루 · 도시` · `다롄 = 해변 · 도시` |

실측 분포: **2개 37곳 · 3개 26곳 · 4개 21곳** (고유 표시 99%).

- **원칙은 그대로다: 화면에 보이는 태그는 고를 수 있어야 한다.**
  하위 태그는 **반드시 자기 상위와 함께 붙으므로**(계약 불변식) `야시장`을 보고 `미식`을 누르면 잡힌다.
- **왜 상위만 보여주던 걸 바꿨나**: 상위 6종은 여러 도시가 공유해서 **구별에 쓸모가 없었다.**
  `해변` 하나만 붙은 도시가 10곳이었다. 배정 후 **78개 도시 중 77개가 서로 다른 표시**를 갖는다(99%).
- **앞 2개를 그대로 쓰는 방식은 기각.** `도시`·`해변` 같은 상위가 앞에 와서 고유 표시가 **77% → 50%**로 떨어졌다.

### 딥링크로 들어왔는데 그 특가가 없을 때 (2026-08-22 확정)
| 상황 | 문구 |
|---|---|
| 같은 목적지가 **다른 허브에 있음** (53%) | **`{원래 출발지} 출발 {도시}는 오늘 없지만, {다른 허브} 출발이 있어요`** ⚠️ **`{도시}는`은 받침으로 `은/는`을 고른다**(§1 조사 규칙). 이 문서에서 조사가 변수에 직접 붙는 **유일한 자리**다 + 그 딜로 가는 링크 |
| 어디에도 없음 | **`찾으시던 특가는 오늘 없어요`** |

- **도시명은 "다른 허브에 있을 때만" 쓸 수 있다.** 딜이 아예 없으면 `ko`(한글 도시명)를 알 방법이 없고,
  **IATA 코드를 화면에 내보낼 순 없다**(`DAD 특가는 없어요`는 말이 안 된다).
- 오류처럼 보이게 하지 않는다. **출발지는 정상 적용되고 피드는 평소대로 뜬다** — 안내만 위에 얹는다.

### 메타 (SEO)
| 위치 | 문자열 |
|---|---|
| `<title>` | `갈래말래 — 어디, 갈까? 항공권 특가 발견 지도` |
| `description` | `시간 남는데 어디 싸게 갈까? 한국 출발 항공권을 매일 스캔해 지도에서 오늘 싼 여행지를 골라줍니다. 목적지를 정해주는 발견 서비스.` |

### 보이지 않는 문자열 (스크린리더·툴팁·`alt`)
**화면에 안 보이므로 육안 점검으로는 절대 안 잡힌다.** 그래서 절을 따로 둔다.
스크린리더 사용자에게는 이게 **유일한 설명**이므로, 보이는 문구와 같은 기준으로 검토한다.

| 위치 | 현행 | 상태 |
|---|---|---|
| 지도 SVG `aria-label` (`discover_home.py:100`) | `여행지 발견 지도` | ⚠️ **보이스 검토 안 됨** — 프론트가 임시로 쓴 문구 |
| 그 외 `role`·`aria-*`·`alt`·`title` | **전무** | ❌ 미작성 |

- 조작 접근성 자체가 미구현이라(`SPEC.md` §CH6 / B11) **CH6에서 대량으로 생긴다.**
  그때 프론트가 임의로 짓지 않도록 **PH6에서 이 표를 먼저 채운다.**
- 원칙: `aria-label`은 **보이는 라벨을 반복하지 말고, 보이지 않는 맥락을 보충**한다.
  (예: 핀은 도시명이 이미 보이므로 가격·거리 단계 같은 걸 얹는다)

---

## 2b. 지역 표시명 — **단일 출처** (2026-09-03 확정)

> 백엔드가 발견했다: 같은 사이트에서 **괌이 지도에서는 "휴양·섬", 노선 페이지에서는 "국내·괌"**
> 이었다. `labels.REGION`이 초기 26개 노선만 담고 있어 사전이 넓어질 때마다 조용히 `etc`로
> 떨어졌고(89곳 중 59곳이 어긋남), 어휘 자체도 `labels`와 `dests`가 달랐다.
>
> **근본 원인은 표시명 표가 이 문서에 없었다는 것이다.** 코드 두 곳이 각자 이름을 들고 있었다.
> 이 세션에서 일곱 번째 같은 유형이다(`timeutil` · `_fmt` · `labels.city()` · `when` 값 ·
> 목업 CSS 클래스 이름 · `TIERS`). **이제 이 표가 정본이다.**

`CONTRACT.md`의 `region` 코드 9종에 대해 **화면에 쓰는 이름은 하나뿐이다.**
노선 페이지 브레드크럼 · 발견 피드 지역 칩 · 「아주 멀리」 대륙 칩이 **모두 이 이름을 쓴다.**

| 코드 | 표시명 | 담는 곳(예) |
|---|---|---|
| `dom` | **국내** | 제주 |
| `jp` | **일본** | 도쿄·오사카·후쿠오카·이시가키 |
| `cn` | **중화권** | 타이베이·홍콩·상하이·타이중 |
| `sea` | **동남아** | 방콕·다낭·하노이·코타키나발루 |
| `island` | **섬** | 괌·사이판·발리·보홀·보라카이 |
| `oc` | **대양주** | 시드니·브리즈번·오클랜드 |
| `eu` | **유럽** | 런던·파리·로마·프라하 |
| `am` | **미주** | 뉴욕·LA·밴쿠버·호놀룰루 |
| `etc` | **그 외** | 두바이·콜롬보·울란바토르 |

**왜 이 단어들인가**

- **`중화권`** — `중국`이 아니다. 타이베이·타이중·홍콩이 들어간다. 정치적 표현이 아니라 노선 묶음이다.
- **`섬`** — `휴양·섬`을 기각했다. **분위기 칩에 이미 `해변`이 있다.** 지역 축과 분위기 축이
  같은 말을 하면 사용자는 둘의 차이를 못 읽는다. 지역은 **어디**, 분위기는 **무엇**이다.
- **`대양주`** — `오세아니아`를 기각했다. 옆에 서는 칩이 `유럽`·`미주`(2글자)라 5글자는 줄이 깨진다.
  뜻은 같고 국어 관용도 충분하다.
- **`국내·괌`을 폐기했다.** 괌은 국내가 아니다. 사실과 다른 라벨은 문구 문제가 아니라 오류다.
- **`그 외`** — `기타`를 기각했다. 「기타」는 서류 말투다(보이스 규칙 §1).

**규칙**: 이 표에 없는 코드가 생기면 **먼저 이 표에 이름을 넣고** 코드에 반영한다.
코드가 이름을 지어내면 다시 갈라진다.

---

## 2c. 공유 카드(OG) 문자열 — **홈** (2026-09-03 확정)

> 백엔드가 노선 페이지에 `og:image`를 물렸는데(`theme.py`) **`docs/index.html`에는 og 태그가
> 하나도 없다.** 홈 셸은 `discover_home.py`(프론트 소유)가 만든다.
> **가장 많이 공유될 URL이 홈**이다 — 커뮤니티 시딩도 홈 주소를 보낸다(`PRODUCT.md` §유입).
> 지금은 카톡에 홈 링크를 붙이면 **미리보기가 백지**다.

문자열을 여기서 확정한다. **프론트가 지어내지 않는다.**

| 속성 | 값 |
|---|---|
| `og:type` | `website` |
| `og:site_name` | `갈래말래` |
| `og:title` | `갈래말래 — 어디, 갈까?` |
| `og:description` | `시간 남는데 싸게 다녀올 곳. 한국 출발 항공권을 매일 스캔해 오늘 싼 여행지를 지도에 펼칩니다.` |
| `og:url` | 홈 절대 URL |
| `og:image` | **절대 URL** — `theme.py`의 `OG_IMAGE` 상수를 그대로 쓴다 |
| `og:image:width` / `height` | `1200` / `630` |
| `og:image:alt` | `갈래말래 — 어디, 갈까? 세계 지도에 오늘 싼 여행지가 찍혀 있다` |
| `twitter:card` | `summary_large_image` |

**왜 `<title>`·`description`과 다른 문자열인가**

- `<title>`은 **검색 결과**에 뜬다 → 검색어가 들어가야 한다(`항공권 특가 발견 지도`).
- `og:title`은 **카톡 말풍선**에 뜬다 → 이미 아는 사람이 친구에게 보내는 자리다.
  검색어를 넣으면 광고처럼 읽힌다. **태그라인만 쓴다.**
- `og:description`은 **74자 설명을 그대로 쓰지 않는다.** 카톡은 두 줄쯤에서 자른다.
  `목적지를 정해주는 발견 서비스`가 잘려 나가면 남는 게 없다 → 앞으로 당긴다.

⚠️ **`og:image`가 상대 경로면 카톡이 못 읽는다.** 반드시 절대 URL이다.
⚠️ 카톡은 미리보기를 **캐시**한다. 바꾼 뒤 안 바뀌어 보이면 카카오 디버거에서 초기화해야 한다.

### 노선 페이지 (2026-09-03 확정 — 백엔드 요청)

지금은 `<title>`을 `og:title`에 그대로 쓴다:
`인천 → 도쿄 항공권 최저가 · 시세 추이 | 갈래말래`.
**친구에게 보내는 말풍선에 `· 시세 추이 | 갈래말래`가 붙는 건 어색하다.**

| 속성 | 값 |
|---|---|
| `og:title` | `{출발} → {도착} 항공권, 지금 얼마?` |
| `og:description` | `최근 30일 수집한 가격으로 본 {출발} → {도착} 왕복 시세. 언제 가면 싼지 월·요일별로 비교했습니다.` |

🔴 **가격을 넣지 않는다.** 넣고 싶은 유혹이 크다 — `인천 → 도쿄 14만원부터`가 훨씬 잘 눌린다.
그런데 **카톡은 미리보기를 캐시한다.** 값이 내일 바뀌어도 말풍선은 어제 가격을 계속 보여준다.
**들어가 보니 다른 가격**이면 그건 우리가 가장 안 하기로 한 것이다
(`PRODUCT.md` §Brand Commitments · "딜의 질이 전부다").
**본문에는 가격이 있다.** 본문은 매일 다시 그려지고 캐시되지 않는다.

- `og:title`이 **질문형**인 건 태그라인(`어디, 갈까?`)과 같은 결이고,
  **답이 페이지 안에 있다**는 걸 알린다.
- `| 갈래말래`를 안 붙인다 — `og:site_name`이 이미 그 일을 한다.

---

## 2d. 신선도 문구 (2026-09-04 확정)

| 등급 | 문구 |
|---|---|
| `fresh` <3h | `발견가 · 방금 가격` |
| `fresh` 3~24h | `발견가 · {N}시간 전 가격` |
| `ok` 24~48h | `발견가 · 어제 가격` |
| `ok` 48~72h | `발견가 · {N}일 전 가격` |
| `stale` >72h | `발견가 · {N}일 전 가격` |
| `seen` 없음 | `발견가 · 스캔 시점 기준` |
| **확장 상세 절대 시각** | **`{M/D(요일)} {HH:MM} 기준`** — 예 `8/31(월) 08:52 기준` |

**절대 시각 줄에 상대 표현을 넣지 않는다** (2026-09-04, B39 — 프론트 발견).
`발견가 · 3일 전 가격` 바로 아래 `2026-08-31 08:52 관측 · 3일 전 가격`이라 **같은 말이 두 번** 나왔다.

- **날짜 표기와 같은 두 단 위계다**(§S2 카드): **위는 스캔용(상대), 아래는 확인용(절대).**
  각 줄이 한 가지만 말해야 둘 다 빨리 읽힌다. 새 규칙이 아니라 **이미 있는 규칙의 적용**이다.
- **연도를 뺀다.** 값이 7일 안쪽이라 `2026-`은 소음이고, 화면의 다른 날짜(`9/8(화)`)와 형식이 갈린다.
- **`관측` → `기준`.** `관측`도 **누가 했나**를 부른다(§2d와 같은 이유).


**`확인`을 쓰지 않는다.** "확인"은 동작이라 **누가 했나**를 부르고, 읽는 사람은 우리로 읽는다.
화면의 **72%가 `N일 전`**이라 `N일 전 확인`은 **"이 사이트는 5일에 한 번 일한다"**가 된다.
`N일 전 가격`은 **가격의 나이**를 말할 뿐이다. 사실은 그대로고 누명만 빠진다.

### 확장 상세 — 왜 며칠 전 값인가 (한 줄)

> **`항공권 가격은 예약 사이트가 마지막으로 조회한 값이라 며칠 전일 수 있어요.`**

- **설명은 여기 한 번만.** 카드는 스캔하는 자리라 짧아야 하고, 상세는 결정하는 자리다.
- **변명하지 않는다.** `죄송하지만`·`양해 부탁` 금지 — 사실만 적는다(보이스 규칙 §1).
- **"실시간"을 약속하지 않는다.** 바로 아래 `실시간 최저가는 각 사이트에서 확인하세요`가 이미 있다.
- 기각: `저희가 매일 아침 수집합니다` — **우리가 얼마나 부지런한지는 사용자 관심사가 아니다.**
  사용자가 알고 싶은 건 **이 숫자를 얼마나 믿을 수 있나**다.

---

## 3. 확정됐으나 아직 구현되지 않은 문자열

### ~~신선도 배지~~ — ✅ **구현됨 (2026-09-04). 문구는 §2d가 정본이다**

여기 있던 표를 지운다. **§2d와 다른 말을 하고 있었다** — `{N}시간 전 확인`·`어제 확인` 같은
옛 문구와, §2d의 상세 한 줄이 대체한 `사흘 전에 본 가격이에요…`가 남아 있었다.
프론트가 구현하다 발견했다.

> "나흘/닷새"처럼 날짜에 따라 고유어가 바뀌는 부분은 **`{N}일 전`으로 통일**한다.
> 고유어는 자연스럽지만 3·4·5일마다 표기가 갈려 검수가 불가능해진다. *(이 규칙은 유효하다 — §2d에 산다)*

### 🔴 이 절의 구조가 문제다 — 구현되면 **지운다**

§3은 "확정됐으나 미구현"인데, **구현된 뒤에 지우지 않으면 §2의 낡은 복사본**이 된다.
**오늘만 세 번 갈라졌다**: 147행(교통 위치) · 이 절(신선도) · 그리고 `SPEC.md`의 트윈 표.

**규칙**: 문자열이 화면에 올라가면 **§2로 옮기고 §3에서 지운다.** 여기 남아 있는 건
"아직 화면에 없는 것"뿐이어야 한다. **§3에 뭔가 오래 남아 있으면 그게 신호다.**

---

## 4. 미정 · 수정 대상

| # | 항목 | 문제 | 담당 |
|---|---|---|---|
| ~~C-1~~ | ~~딜 0건인 날 문구~~ | ✅ **해소** — 제목·설명·대안 3줄 확정(§2 화면0) |
| ~~C-2 일부~~ | ~~거리 단계 라벨~~ | ✅ **해소** — 지리 이름(`＋동남아`·`＋유럽·미주`)을 거리 표현으로 교체. 지리 이름은 데이터가 바뀌면 계속 약속을 어긴다 |
| ~~C-2~~ | ~~화면의 `딜` 표기~~ | ✅ **해소** — `맞는 곳이 없어요`로 교체(2026-08-22) |
| **C-3** | noscript 말투 혼재 | `표시하지 못했습니다`(합니다체) + `볼 수 있어요`(해요체)가 한 문장 안에 섞임 | PH6 |
| ~~C-4~~ | ~~빵부스러기 `특가 피드`~~ | ✅ **해소 (2026-09-03)** — `발견`으로 확정. 2026-09-01에 정해놓고 백엔드에 전달을 빠뜨려 코드에 안 갔고, JSON-LD가 들어가며 **없는 화면 이름이 검색엔진에 발행되고 있었다.** 노선 페이지를 실제로 렌더해 보고 발견했다 |
| ~~C-5~~ | ~~딜 희소 출발지 문구~~ | ✅ **해소** — 사실 + 최다 허브 제안 2줄(§2 카드 피드) |
| **C-6** | `links` 0건 확장 상세 | 지금은 예약처 섹션 제목만 남음 | PH4 / F4 |
| ~~C-7~~ | ~~사라진 딜 딥링크 착지 문구~~ | ✅ **해소** — 다른 허브 제안 / 담백한 안내 2종(§2) |
| **C-8** | 키보드·스크린리더용 `aria-label` 일체 | 전무. 지도 `aria-label` 1건만 존재하고 보이스 미검토 (§2 "보이지 않는 문자열") | PH6 / B11 |
| ~~C-9~~ | ~~예산 슬라이더 값 표기~~ | ✅ **해소** — `{N}만 이하`, 최대치는 `제한 없음`(2026-08-22) |
| ~~C-11~~ | ~~"특가" 도장·훅 문구가 근거를 앞선다~~ | ✅ **해소** — 훅 줄 제거 + 도장 `≥15%` + "특가" 단어 미사용 |
| ~~C-11 옛 메모~~ | ~~근거~~ | 도장 `특가 {N}%↓`가 할인 0%에도 붙는다(23건). 훅 `경유로 확 싸진 특가`는 **피드의 54%(51건)**에 붙는데 직항 대비 가격 데이터가 없어 **근거가 0이다.** "특가"를 몇 %부터 쓸지, 그 미만엔 무엇을 쓸지 정해야 함 (`SPEC.md` F17·F18·F19) | **PH3** |
| ~~C-12~~ | ~~근거 약한 문구를 늘리면 피드가 단조로워진다~~ | ✅ **해소** — 훅 줄이 없어져 "같은 문구가 절반" 문제 자체가 사라졌다. 대신 날짜 줄에 직항/경유가 붙어 정보량은 늘었다 |
| ~~C-12 옛 메모~~ | ~~근거~~ | 임계값을 올리면 딜이 `{when} 최저가`(현재 9건뿐)로 몰린다. 94건 중 절반이 같은 문구가 되면 "정보 나열형 회피" 원칙과 충돌 → **근거를 주장하지 않으면서도 지루하지 않은 문구군**이 필요 | **PH3** |
| **C-13** | **태그가 도시를 구별하지 못한다** | 실측: `해변` 하나만 붙은 도시가 **10곳**(괌·나디·몰디브·사이판·세부·싼야·팔라우·푸꾸옥·푸켓·호놀룰루), `도시` 하나만 **8곳**(나고야·두바이·상하이·프랑크푸르트…). **태그 1개짜리가 38%.** 어휘 12종으로 목적지 90곳을 구별할 수 없다. **표시 규칙만 바꿔도 안 된다** — 하위 태그 우선 표시를 시뮬레이션하니 제주·다낭·오키나와·푸꾸옥·싼야가 전부 `휴양 · 해변`으로 똑같았다(조합 20→26가지). **어휘를 늘려 `dests.py`에 재배정해야 한다** = 계약 변경 + 백엔드 작업 | **미정 (사용자 검토 중)** |
| ~~C-10~~ | ~~필터로 고를 수 없는 태그가 화면에 보인다~~ | ✅ **해소 확인**(2026-08-23) — **116/116건이 상위 6종 중 하나 이상**을 갖는다. 옛 "칩으로 아무것도 못 고르는 딜 4건(이스탄불×3·델리)"이 **0건**이 됐다. 상위/하위 구조가 정확히 이 문제를 겨냥한 설계였다 |
| ~~C-16~~ | ~~비교 막대가 없을 때 아무 말도 안 한다~~ | ✅ **해소** — 이유 둘을 구분해 각각 문구를 준다(`SPEC.md` §CH4). `median: null` 14건은 "아직 못 모았다", `median ≤ price`는 "평소와 비슷하다" |
| **C-15** | **화면은 "오늘"이라 말하는데 데이터는 어제 것일 수 있다** | 백엔드가 F1 방어를 넣어(`e6e7f55`), 수집이 실패하면 **파일을 안 쓰고 어제 산출물을 그대로 서빙**한다. 가용성 면에서 옳은 선택이지만 **실패가 조용해졌다** — 예전엔 0건이라 눈에 띄었다. 그런데 화면은 여전히 `오늘의 발견` · `오늘 싼 곳들이 열려요`라고 말하고, **`updated`는 `<noscript>` 안에만 있어 정상 방문자는 절대 못 본다**(`discover_home.py:111`). ⚠️ **딜은 원래도 최대 96시간 된 것을 보여주므로 "오늘의 발견"은 "오늘 우리가 고른 것"이지 "오늘 수집한 가격"이 아니다** — 문구 자체가 거짓은 아니다. 문제는 **실패했을 때 그걸 알 방법이 화면에 없다**는 것이다. → **제안**: 피드 헤드의 `서울 출발 · 65곳` 뒤에 **갱신 시각을 상시 노출**한다(중립적 사실, 주장 아님). `updated`가 오늘(KST)이 아니면 더 눈에 띄게. 딜별 `seen` 배지는 개별 나이를 이미 말하지만 **페이지 전체가 늙었다는 신호는 따로 필요하다** | PH6 / CH6 |
| **C-14** | **필터 칩 마크업이 아직 옛 5종** | `discover_home.py`에 `해변`·`도시`·`온천`·`자연`·`미식`만 있고 **`문화`가 빠져 있다**. `문화`는 **52건으로 2위** 어휘다. ⚠️ 단, `문화`만 가진 딜은 없어 **미매칭은 0건** — 손실은 "매칭이 안 된다"가 아니라 **"`문화`로 고를 수 없다"**는 것 | 프론트 CH2 |

---

## 5. 노선 페이지 (백엔드 소유 — 톤 참조용)

> 문자열 소유는 백엔드지만 **한 제품의 목소리**이므로 여기 기록한다. 수정이 필요하면 백엔드 세션에 전달.

| 위치 | 문자열 |
|---|---|
| 브랜드바 | `갈래말래 ✈️` |
| 빵부스러기 | `발견 › {지역} › {출발} → {도착}` ✅ C-4 해소(2026-09-03) — `{지역}`은 **§2b 표시명**을 쓴다 |
| 제목 | `{출발} → {도착} 항공권 최저가` |
| 태그라인 | `최근 30일 수집한 가격 {N}건으로 분석한 {노선} 왕복 항공권 시세입니다.` |
| hero 라벨 | `최근 30일 최저가` · `평소 시세(중앙값)` · `언제 가면 싼가` |
| 섹션 | `{노선} 최저가 추이` · `출발 월별 최저가` · `출발 요일별 최저가` · `항공사별 최저가` |
| 리드 | `매일 아침 수집한 이 노선의 왕복 최저가입니다. 아래로 꺾일수록 지금이 살 때입니다.` |
| | `출발 시기에 따라 {노선} 항공권 가격이 얼마나 달라지는지 비교했습니다.` |
| | `같은 노선도 무슨 요일에 떠나느냐로 가격이 달라집니다.` |
| 팁 | `{월} 출발이 가장 저렴합니다 ({N}원)` · `출발 요일은 {요일}요일이 가장 쌉니다 ({N}원)` |
| 팁 없음 | `데이터가 쌓이면 저렴한 시기를 분석해 보여드립니다.` |
| 구독 | `{노선} 특가 알림 받기` · `내용 수정 없이 그대로 보내주시면 구독이 완료됩니다.` · `{노선} 알림 신청` |
| 구독 해지 | `해지: {주소}로 제목 '구독취소' 메일을 보내주세요.` |
