# CONTRACT.md — 프론트↔백엔드 데이터 계약

> **이 문서가 프론트와 백엔드 사이의 유일한 인터페이스다.**
> 백엔드는 이 형식대로 `docs/data/deals.json`을 **생산**하고, 프론트는 이 형식만 믿고 **소비**한다.
> 서로의 코드를 읽지 않아도 이 문서만으로 각자 작업할 수 있어야 한다.
>
> 🔴 **문서가 둘로 나뉘어 있다.** 아래 §「파일」~§「프론트가 하는 파생」은 **분리 전 현행**이고,
> 맨 끝 §「v1 API」가 **레포 분리 후** 계약이다. 이전(`SPLIT.md` M0~M6)이 끝나면 앞을 지운다.

## 변경 규칙 (중요)
1. 스키마를 바꾸려면 **이 문서를 먼저 갱신**하고 커밋한 뒤, 양쪽 세션에 알린다.
2. **필드 추가는 안전**(프론트가 무시하면 됨). **필드 삭제·이름 변경·의미 변경은 파괴적** → 반드시 사전 합의.
3. 프론트는 **모든 필드에 방어적으로** 접근한다(`dl.links || []`). 백엔드 데이터가 없거나 0건인 날도 화면이 깨지면 안 된다.
4. 프론트는 커밋된 `docs/data/deals.json`을 **픽스처**로 쓴다 → 백엔드를 기다리지 않고 개발 가능.

---

## 파일
- 경로: `docs/data/deals.json`
- 생산자: `collector/discover_data.py` → `build_deals_json(conn)` (`build_site.py`가 호출)
- 소비자: `docs/assets/discover.js` (+ `collector/discover_home.py`가 HTML에 인라인)
- 인코딩: UTF-8, 압축 없음(`separators=(",",":")`)

## 최상위 구조
```json
{
  "updated": "2026-08-06 00:15",
  "origins": { "SEL": {"name":"서울","lat":37.55,"lon":126.99}, ... },
  "deals": [ { ...deal... }, ... ]
}
```

| 키 | 타입 | 의미 |
|---|---|---|
| `updated` | string | 생성 시각 `YYYY-MM-DD HH:MM` (KST) |
| `origins` | object | 출발 허브 맵. 키 = 허브코드, 값 = `{name, lat, lon}`. **딜이 있는 허브만** 포함 |
| `deals` | array | 딜 목록. **가격 오름차순** 정렬 |

### 출발 허브 코드 (`origins` 키 / `deal.o`)
`SEL`(서울=인천+김포 통합) · `PUS`(부산) · `TAE`(대구) · `CJU`(제주)
> ⚠️ `SEL`은 **실제 공항 코드가 아님**(집계용 가상 허브). 예약 링크에는 실제 공항(ICN/GMP)이 들어간다 — 백엔드가 처리하며 프론트는 신경 쓰지 않는다.

## deal 객체

| 필드 | 타입 | 예시 | 의미 |
|---|---|---|---|
| `o` | string | `"SEL"` | 출발 허브 코드 (`origins`의 키) |
| `d` | string | `"TYO"` | **목적지 코드** — IATA **도시 코드가 있으면 도시 코드**, 없으면 공항 코드. `(o, d)`는 **유일**해야 한다(URL 딥링크 `#{o}-{d}` 키 + 중복 제거 키) |
| `ko` | string | `"제주"` | 목적지 한글 도시명 (**화면 표시용**) |
| `country` | string | `"한국"` | 국가 한글명 |
| `region` | string | `"dom"` | 지역 코드 (`jp`/`cn`/`sea`/`island`/`oc`/`eu`/`am`/`etc`/`dom`) |
| `haul` | string | `"short"` | 거리대 — `short`(근거리) / `mid`(중거리) / `long`(장거리) |
| `tier` | string | `"major"` | LOD 등급 — `major`(축소에도 표시) / `minor`(확대 시 표시) |
| `tags` | string[] | `["야경","미식","도시","쇼핑"]` | 분위기 태그. **통제 어휘 24종 · 상위6/하위18**(아래 참조). 필터·카드 표시·사진 색에 사용 |
| `lat` `lon` | number | `33.51`, `126.49` | 목적지 공항 위경도 (**지도 핀 좌표**) |
| `price` | number | `42522` | **발견가**(KRW, 왕복 총액). 화면에선 "발견가 · 스캔 시점"으로 표기 |
| `transfers` | number | `0` | 경유 횟수 (0=직항) |
| `dep` | string | `"2026-09-16"` | 출발일 `YYYY-MM-DD` |
| `ret` | string\|null | `"2026-09-17"` | 귀국일. 편도면 `null` |
| `nights` | string | `"1박2일"` | 표시용 기간 문자열. 편도/불명이면 `""` |
| `median` | number\|null | `59319` | 해당 노선 **평소 시세(중앙값)**. "평소 시세 vs 발견가" 막대에 사용. **관측 이력이 없으면 `null`** — 오늘 가격을 평소 시세라고 단언하지 않는다. 프론트는 `null`이면 **비교 막대를 생략**한다(`median <= price`일 때와 같은 처리) |
| `route` | string\|null | `"ICN-FUK"` | **그 딜에 해당하는 노선 페이지 코드.** 없으면 `null`. 프론트는 이 값이 있을 때만 `이 노선 시세 자세히` 링크를 보인다 — `o`가 가상 허브(`SEL`)라 프론트는 실제 공항을 모른다. **아는 쪽(백엔드)이 판정해서 넣는다** |
| `discount` | number | `28` | 중앙값 대비 할인율(%). 0~70로 클램프. 할인 없으면 `0` |
| `when` | string | `"다음 달"` | 상대 날짜 라벨. **통제 어휘** — 아래 `when` 절 참조 |
| `low` | number\|null | `203758` | **오늘(이번 수집)을 제외한 이전 관측 기간의 최저가.** 오늘 값을 포함하지 않는 게 핵심이다 — 포함하면 "최저" 판정이 동어반복이 된다. 이력이 없으면 `null` |
| `obs_days` | number | `17` | 그 `(허브, 목적지)`를 **관측한 일수**. `broad_offers` 기준. 이력이 없으면 `0` |
| `seen` | string\|null | `"2026-08-18T03:17:35+09:00"` | **소스가 이 가격을 마지막으로 관측한 시각**(신선도 배지용). ISO8601, **KST 기준**. 값이 없거나 파싱 불가면 `null` |
| `links` | object[] | 아래 참조 | **예약처 비교 링크** |

### `links[]` — 비교 패널
```json
{ "name": "Aviasales", "tag": "영어·수수료", "ad": true, "url": "https://..." }
```
| 필드 | 의미 |
|---|---|
| `name` | 예약처 표시명 (프론트가 그대로 출력) |
| `tag` | 성격 라벨. **프론트는 미표시**(중립성 위해 숨김). 필드는 유지 |
| **`ad`** | 🔴 **이 링크로 예약하면 우리가 수수료를 받는가** (boolean, **항상 존재**). **프론트는 이 값이 `true`인 링크에만 `(광고)`를 붙인다.** **이름·순서·URL 모양으로 추측하지 않는다.** |
| `url` | 노선·날짜가 프리필된 딥링크. 프론트는 `target="_blank" rel="noopener sponsored"` 로 연다 |

> ✅ **`ad` 신설 (2026-09-02)** — 법적 고지(`COPY.md` 제휴 고지 절)를 화면에 붙이려면
> **어느 링크가 제휴인지 프론트가 알아야 한다.** 기존 필드로는 넷 다 **추측**이 된다 —
> 이름 하드코딩 · 순서 가정 · `tag` 문자열 파싱 · URL에 `marker=` 검색.
> **판정을 아는 건 백엔드뿐이고, 마커를 붙이는 그 자리에서 단언하는 게 맞다.**
>
> 🔑 **`ad`는 "Aviasales인가"가 아니라 "수수료를 받는가"다.**
> Trip.com이 승인되면 `tp.media` 래퍼가 씌워져 **그 순간부터 그것도 제휴 링크**다.
> Aviasales만 참으로 박았다면 승인되는 날 **고지가 조용히 빠졌을** 것이다.
> **선언(`ad`)과 실제(URL이 수수료를 추적하는가)가 갈리면 고지가 거짓말이 된다** — 백엔드 테스트가 그 일치를 검사한다.

- 순서 = 화면 노출 순서. 현재: 스카이스캐너 · 네이버 항공권 · 구글 항공권 · Trip.com
- **Aviasales(영어)는 `TP_MARKER` 환경변수가 있을 때만** 배열에 추가된다(수수료 발생 시에만 노출).
- 개수는 **가변**(3~5). 프론트는 개수를 가정하지 말고 반복 렌더할 것.
  **2026-09-02 실측: `TP_MARKER` 등록 후 전 딜이 5개**(스카이스캐너 · 네이버 · 구글 · Trip.com · Aviasales).
  **5는 지금 사실이지 계약이 아니다** — 마커가 빠지면 4로 준다.

### `seen` — 신선도 배지 (2026-08-22 추가)
프론트는 `seen`으로 "**N시간/N일 전 확인**" 배지를 그린다. 표기 규칙·등급은 **`SPEC.md` §CH3 "신선도 배지"** 참조.

- **원천**: `broad_offers.found_at` (Travelpayouts 응답의 `found_at` = 캐시에 그 가격이 관측된 시각).
  우리가 스캔한 날(`fetched_date`)이 **아니다** — 사용자에게 의미 있는 건 가격 자체의 나이다.
- ⚠️ **타임존을 반드시 정규화할 것.** API 원본 `found_at`은 오프셋 없는 naive 문자열이고 **UTC다.**
  근거(백엔드 세션 검증, 2026-08-22): 최근 10일 수집분을 크론 실행 순간 기준으로 계산하면
  **UTC로 해석할 때 최소 나이가 `-0.2 ~ +3.5h`** 로 0에 붙는다(캐시가 방금 관측한 값을 돌려주는 자연스러운 그림이고,
  음수는 크론이 정각보다 몇 분 늦게 도는 만큼이다). KST로 해석하면 **가장 신선한 가격조차 8.8h**가 되고
  최대 나이가 수집 필터 자신의 컷을 넘어 **애초에 저장될 수 없는 값**이 된다.
  그대로 흘리면 **모든 딜이 9시간씩 늙어 보인다.** 백엔드가 KST로 변환해 **오프셋을 붙여**(`+09:00`) 내보낸다.
  오프셋을 붙일 수 없으면 값을 신뢰할 수 없다는 뜻이므로 `null`을 보낸다(프론트가 배지를 생략한다).
- **하드 컷**: `seen`이 **7일보다 오래된 딜은 `deals.json`에서 제외**한다.
  (실측 최대가 5일이라 현재 손실 0건. 3일 컷은 딜 26% 손실이라 **하지 않는다** — `DECISIONS.md` 참조.)
- **`null` 허용**: 프론트는 `null`이면 배지 대신 기존 폴백 문구("스캔 시점 기준")를 쓴다. 화면은 깨지지 않는다.
- 프론트는 **방문 시점**(`Date.now()`)에 나이를 계산한다. 백엔드는 "3시간 전" 같은 **완성된 문구를 만들지 않는다**
  (정적 사이트라 다음 날 거짓말이 된다).

### `tags` 통제 어휘 (2026-08-22 **확장** — 12종 → 24종)

> ⚠️ **계약 변경.** 어휘가 12종에서 **24종**으로 늘고, **상위/하위 구조**가 생겼다.
> 배정안(84개 목적지 전부)은 **`TAGS.md`**에 있고, 백엔드가 `collector/dests.py`에 반영한다.

**왜 늘렸나**: 12종으로는 목적지 84곳을 구별할 수 없었다. `해변` 하나만 붙은 도시가 **10곳**
(몰디브·싼야·푸켓…), `도시` 하나만 붙은 도시가 **8곳**(두바이·나고야…)이었다.
**몰디브와 싼야가 같은 태그, 두바이와 나고야가 같은 태그**였다.

| 태그 | 곳 | 상위 | 하위 |
|---|---|---|---|
| **`해변`** | 24 | — | `리조트` · `스노클링` · `서핑` · `섬` |
| `리조트` | 13 | **`해변`** | |
| `스노클링` | 6 | **`해변`** | |
| `서핑` | 5 | **`해변`** | |
| `섬` | 5 | **`해변`** | |
| **`도시`** | 52 | — | `야경` · `쇼핑` · `마천루` · `골목` |
| `야경` | 7 | **`도시`** | |
| `쇼핑` | 19 | **`도시`** | |
| `마천루` | 10 | **`도시`** | |
| `골목` | 9 | **`도시`** | |
| **`미식`** | 28 | — | `야시장` · `길거리음식` |
| `야시장` | 7 | **`미식`** | |
| `길거리음식` | 6 | **`미식`** | |
| **`자연`** | 24 | — | `화산` · `트레킹` · `사막` · `폭포` |
| `화산` | 5 | **`자연`** | |
| `트레킹` | 4 | **`자연`** | |
| `사막` | 2 | **`자연`** | |
| `폭포` | 1 | **`자연`** | |
| **`문화`** | 38 | — | `사원` · `유적` · `고성` · `미술관` |
| `사원` | 11 | **`문화`** | |
| `유적` | 11 | **`문화`** | |
| `고성` | 8 | **`문화`** | |
| `미술관` | 8 | **`문화`** | |
| **`온천`** | 8 | — | — |

**구조 — 이 불변식이 계약의 핵심이다.**

- **상위 6종**(`해변` `도시` `미식` `자연` `문화` `온천`) = **필터 칩**이 쓰는 어휘.
- **하위 18종** = **카드 표시**용. 그 도시가 왜 좋은지 말한다.
- ⭐ **하위 태그가 붙은 목적지는 반드시 그 상위 태그도 함께 갖는다.**
  그래야 `야시장`을 보고 `미식` 필터를 눌렀을 때 잡힌다.
  **`design/build_tags.py`가 매번 검증하며, 어기면 실패한다.** 계약 검증기에서도 검사할 수 있다.
- 어휘 추가·삭제·개명은 **여전히 계약 변경**이다. 위 표가 단일 출처다.
- 프론트는 이 목록을 하드코딩해도 된다(변경 시 통지를 받는다).

**시즌 표현은 어휘에 넣지 않는다** — `벚꽃`·`단풍`·`설경`은 계절을 타서 데이터가 썩는다.

### `d` 는 왜 도시 코드인가

> ✅ 2026-09-01 변경(백엔드 BB16 제기 · 프론트 동의). **공항 코드 → 목적지 코드.**

**광역 수집이 도시 단위(`TYO`)로 주므로 그 딜이 나리타행인지 하네다행인지 우리는 모른다.**
그런데 `d = "NRT"`라고 적으면 **"이 딜은 나리타행"이라고 주장하는 셈**이다.

- 예약 링크는 이미 **도시 코드로 나간다**(백엔드가 브라우저로 예약처 4곳 확인).
  트립닷컴에서 **지금 링크로는 안 보이던 하네다 편**이 나왔다.
- **중복 제거 조항이 이미 `(허브, 도시)`**라고 적고 있어 키가 공항 코드인 게 그 문장과 어긋났다.
- **URL 딥링크가 아직 구현 전이라 지금이 가장 싸다** — 나중이면 뿌려진 공유 링크가 깨진다.
  프론트 확인: `discover.js`에서 `d` 참조 **0건**, `location`/`history` 사용 **0건**.

```
전  #SEL-NRT     공유하면 "나리타?"로 읽힌다
후  #SEL-TYO     도쿄를 가리킨다
```

> **출발지(`o`)는 그대로 실제 공항 기준이다.** 우리가 `ICN`/`GMP`를 지정해 물은 값이라 **안다**.
> 실측으로도 두 공항 응답의 가격 일치가 3%뿐이다(김포→다낭이 인천의 2배). **아는 것과 모르는 것을 가른다.**

### `when` 통제 어휘

> ✅ 2026-09-01 확정(백엔드 BB7·BB8 제기). **검증기가 어휘 밖 값을 잡는다** — 태그와 같은 방식.

**위에서부터 먼저 맞는 것 하나**를 쓴다. 순서가 곧 규칙이다.

| # | 조건 | 값 | 예 |
|---|---|---|---|
| 1 | **이번 주(월~일)**의 금·토·일 | `이번 주말` | |
| 2 | **다음 주**의 금·토·일 | `다음 주말` | |
| 3 | **이번 주** 안 (나머지 요일) | `이번 주` | |
| 4 | 같은 달 | `이번 달` | 9/1 오늘, 9/16 출발 |
| 5 | 다음 달 | `다음 달` | 9/1 오늘, 10/5 출발 |
| 6 | 같은 해, 2개월 이후 | `N월` | `11월` |
| 7 | 이듬해 | `내년 N월` | `내년 1월` |
| 8 | 2년 이후 | `YYYY년 N월` | `2028년 3월` |

**신설은 2(`다음 주말`) · 4(`이번 달`) · 7(`내년 N월`)이다.**

> 🔴 **"N일 이내"가 아니라 "달력 주"다** (2026-09-01 정정, 백엔드 코드 리뷰가 제기).
> 처음엔 1을 `9일 이내 + 금·토·일`, 3을 `7일 이내`로 적었는데 **둘 다 틀렸다** —
> **상대 거리로 재면서 라벨은 달력 단위를 말했다.**
>
> ```
> 9/12(토) 접속 · 현행 규칙
>   9/18(금) +6  → "이번 주말"   ❌ 다음 주말이다
>   9/14(월) +2  → "이번 주"     ❌ 다음 주다
> ```
>
> **`이번 주` 쪽이 더 넓다** — 백엔드는 `이번 주말`만 짚었는데 **같은 뿌리가 `이번 주`에도 있고
> 월요일 접속에도 생긴다.** 토요일 접속이면 **4일이 오라벨**된다.
> **주는 월요일에 시작한다**(한국 관례).

- **왜 `다음 주말`을 새로 두나**: 1·3을 달력으로 고치면 다음 주말이 `이번 달`로 떨어져
  **임박 신호를 잃는다.** 여행에서 **주말은 특별한 단위**라 따로 받는 값이 있어야 한다.
  (다음 주 *평일*은 `이번 달`로 두었다 — 주말만큼 그날이 중요하지 않다.)
- **1·2가 4·5보다 먼저다**: 9/30(수) 오늘, 10/3(토) 출발이면 `다음 달`이 아니라 **`이번 주말`**이다.
  달을 넘어도 **이번 주면 이번 주**다.

- **왜 `이번 달`인가**: `이번 주`와 `다음 달` 사이가 비어 있어 7일 초과 이번 달이 **월 이름으로 떨어졌다.**
  **오늘이 9월인데 `9월`은 아무 말도 아니다.** 실측 **44건(35%)** — 가장 큰 덩어리다.
  월초일수록 여기 몰리므로 **매달 초에 3분의 1이 이 상태**가 된다.
- **왜 `내년 N월`인가**: 딜 출발일이 **2026-09-01 ~ 2027-08-20**까지 뻗어 있어
  `7월`·`8월`이 **지난 2026년이 아니라 2027년**이다. 실측 **13건(10%)**이 지난 달로 읽힐 수 있었다.
- **4가 6보다 먼저다.** 오늘이 12월이고 출발이 1월이면 `내년 1월`이 아니라 **`다음 달`**이다 —
  더 가깝게 느껴지는 쪽이 사실에도 맞다.
- **7은 방어용**이다. 현재 데이터에 없고(최대 약 1년) 화면에 뜰 일이 없지만,
  없으면 미래에 조용히 `3월`로 떨어진다.

> ⚠️ 프론트: `when` 값으로 분기하는 곳이 있으면 **`이번 달`·`다음 주말`·`내년 N월` 경로를 확인**해야 한다.
> 특히 `whyOf()`가 `이번 주말`을 문자열 비교로 보고 있다(`discover.js`).

## 데이터 정책 (백엔드가 보장)
- **게이팅**: `dests.DEST` 사전에 있고 좌표가 있는 목적지만 포함(롱테일 쓰레기 제외).
- **신선도**: `broad_offers` 최근 수집분 + **미래 출발**만. 추가로 `seen` 7일 초과 딜 제외(위 참조).
  수집 컷은 **96시간(4일)**이다 — `MAX_AGE_HOURS = 96`.
  > ✅ 2026-09-01 정리(백엔드 BB10). 이전엔 상수 이름이 `MAX_AGE_DAYS = 3`인데 `timedelta.days`가
  > 내림이라 실제로는 95시간59분까지 통과했다. **이름과 동작을 일치시킨 것이지 정책 변경이 아니다** —
  > 96시간이 원래부터 실제 동작이었고, 72시간으로 조이는 건 `DECISIONS.md`에서 이미 기각됐다.
  > 시각 계산은 `collector/timeutil.py` 한 곳에서만 한다(`found_at`은 UTC).
- **중복 제거**: `(허브, 도시)` 단위로 **최저가 1건**만.
- 데이터가 0건인 날도 `{"updated":..., "origins":{}, "deals":[]}` 형태는 유지된다.

## 프론트가 하는 파생 (백엔드는 몰라도 됨)
`discover.js`가 deal → 화면용으로 변환(`toCity`)하며 다음을 **자체 계산**한다:
- 가격 포맷(`42522` → `42,522`), 날짜 표기(`9/16(수)~9/17(목)`)
- "왜 지금" 훅 문구(`discount`/`transfers`/`when` 기반 규칙)
- 태그 → 사진 그라디언트, `haul` → 지도 단계(near/sea/far)
- 핀 색 농도(현재 화면에 보이는 딜들 사이의 상대 가격)
- **신선도 배지 문구·등급**(`seen` → "방금/N시간 전/어제/N일 전 확인") — 백엔드는 절대 시각만 준다

> 이 파생 로직은 **프론트 소유**다. 백엔드는 위 원천 필드만 정확히 채우면 된다.

---

# v1 API — 레포 분리 후 (2026-09-08 확정)

> 위 절들은 **분리 전 현행**(`docs/data/deals.json`, 파일)이다. 아래는 분리 후 계약이다.
> 이전이 끝나면 위를 지우고 이 절만 남는다. 이전 계획은 `SPLIT.md`.

## 왜 파일이 아니라 URL인가

분리 후 백엔드는 **HTTP로 응답하는 것처럼** 행동한다. 오늘은 GitHub Pages가 정적 JSON을
서빙하고, 나중에 자체 서버가 생기면 **DNS만 옮긴다 — 프론트는 한 글자도 안 바뀐다.**
이게 v1 설계의 유일한 목표다.

```
base: https://api.galmal.kr/v1/

GET /v1/meta.json           생성 시각·건수·수집 상태
GET /v1/deals.json          발견 홈이 쓰는 전부
GET /v1/routes/index.json   노선 목록 (네비·sitemap·「다른 노선 보기」)
GET /v1/routes/{code}.json  노선 1개 통계
```

**소비 시점은 빌드 타임이다.** 프론트 Actions가 받아 HTML을 만든다(브라우저가 아니다).
따라서 CORS는 v1의 관심사가 아니다 — 런타임 fetch로 바꾸는 날 다시 본다.

## 공통 규칙

**봉투**: 모든 응답은 최상위에 `schema`와 `generated`를 갖는다.

```json
{ "schema": "v1", "generated": "2026-09-08T07:12:03+09:00", "...": "..." }
```

- `generated`는 **ISO 8601 + 오프셋 필수.** 화면이 신선도를 표시하고 있어
  (`SPEC.md` §CH3 `발견가 · N일 전 가격`) 날짜 경계에서 하루가 어긋나면 조용히 틀린다.
  현행 `updated`의 `"2026-08-06 00:15"`는 **완성된 문장**이라 v1에선 쓰지 않는다(P7).
  표시 문자열은 프론트가 만든다.
- 인코딩 UTF-8, 압축 없음(`separators=(",",":")`).
- **깨는 변경은 `/v2/`를 새로 낸다.** 필드 추가는 안전. 삭제·개명·의미 변경은 양쪽 합의 후에만.
- 값이 없으면 필드를 빼지 말고 `null`. **단 "표본이 없다"를 `null`로 표현하지 않는다** — 아래.

---

## 🔴 창(window)과 임계(threshold)를 가른다 — v1의 핵심 규칙

| | 무엇을 정하나 | 누가 | 예 |
|---|---|---|---|
| **창** | 어느 기간·범위를 분석하나 | **백엔드** | 최근 30일 |
| **임계** | 이 숫자를 보여줘도 되나 | **프론트** | 버킷 2개 미만이면 안 그린다 |

**따라서 백엔드는 얇다고 버리지 않는다.** 모든 버킷을 **건수와 함께** 낸다.
자를지 말지는 프론트가 정한다.

### 왜 이렇게 정했나 (백엔드 발견 2건, 2026-09-08)

**① 지금은 백엔드가 조용히 버린다.**
`build_site.py:54` `month_min()`의 `HAVING COUNT(*)>=3`이 3건 미만인 달을 **말없이 뺀다.**
그래서 프론트는 「10월이 없다」가 *자료가 없어서*인지 *얇아서 버려진 건지* 구분할 수 없다.
v1이 만들 자리는 표본 수 필드 하나가 아니라 **버킷마다 건수**다.

**② 🔴 임계가 지배하는 건 차트만이 아니다 — 실제로 잘못 나간 건 문장이었다.**
`build_site.py:207`은 `if best_month:`다. 참·거짓만 본다.
**버킷이 하나뿐이어도 「9월 출발이 가장 저렴합니다」라고 쓴다.** 하나는 비교가 아니다.

- 지금 안 터지는 건 위 ①의 `HAVING`이 **우연히 가려주기 때문**이다
  (2026-09-08 실측: 36노선 월 버킷 수 최소 3 · 최빈 10).
- **새 노선을 넣으면 그날 재현된다.** 타이중이 지난 길이다.
- `tests/test_charts.py`는 차트만 본다. **문장은 아무도 안 본다.**

→ 백엔드 `BACKEND.md` BB28. **안 고쳤다** — 이전하면 `tips`는 P1 기준 사이트 산출물이라
프론트 구역이 된다. 곧 옮겨질 자리를 지금 고칠 이유가 없다.

### 그래서 프론트가 지켜야 할 것

> **임계는 「차트」가 아니라 「버킷 비교로 만든 모든 주장」에 건다.**

프론트는 판정 함수를 **하나만** 만들고 차트와 문장에 **같이** 적용한다.

```
usable(buckets) = buckets.length >= 2      // 하나는 비교가 아니다. 전 블록 공통
enough(bucket)  = bucket.n >= 3            // months 에만. weekdays 에는 걸지 않는다
```

임계는 **프론트 소유**다. 문구는 `COPY.md`(현행 `charts.NOT_ENOUGH`).

### 🔴 이전 중에는 동작을 바꾸지 않는다

**임계 값(2·3·상위 10)은 현행 코드에서 그대로 가져온 것이다.** 고쳐서 가져오지 않는다.

이유: 이전의 관문은 `SPLIT.md` M2 T5 **「새 화면 37장이 현 배포본과 diff 0」**이다.
이전이 화면을 바꾸면 **diff가 났을 때 이전이 깨뜨린 건지 우리가 고친 건지 구분할 수 없다.**

> 🔴 **이 규칙이 잠그는 것은 「산출물」이지 「코드」가 아니다.**
> 코드는 바뀐다 — 이전 자체가 코드를 옮기는 일이다.
> **판정 기준은 하나: `docs/`의 바이트가 바뀌는가.**
>
> 그래서 이런 것들은 **허용된다**(백엔드 확인, 2026-09-08):
> - 필터를 기본 인자로 빼기 (`month_min(…, min_samples=3, limit=10)`) — 기본값이 현행이면 산출물 불변
> - **반환 모양 바꾸기** (`(m, price)` → `(m, price, n)`) — **호출자를 같이 고치면** 산출물 불변
> - 표시명 변환을 소비 지점으로 옮기기 (`weekday_min`이 `SQL_WEEKDAY[wd]` 대신 정수 `wd`를 내고 `route_page`가 이름을 붙인다)
>
> ⚠️ **반환 모양은 기본 인자로 못 덮는다.** `charts.bar_chart`가 `vals = [v for _, v in rows]`로
> 2-튜플을 못박고 있고 `route_page:287`이 `bar_chart(weekdays)`로 가공 없이 넘긴다 —
> `n`을 넣으면 **`ValueError`로 빌드가 죽는다.** 조용히 틀리지 않는 건 다행이지만,
> 「호출자가 둘」의 파급은 필터보다 여기가 크다. 넷을 **항상 `(라벨, 가격, n)` 3-튜플로 통일**한다.
> `n`은 필터가 아니라 **사실**이라 조건부로 낼 이유가 없다 — 옵션으로 만들면 두 모양이 생기고 갈린다.
개선하고 싶은 것이 있으면 **이전 전에 현행 코드에서** 고치고 크론이 한 번 배포하게 하거나,
**M3 이후에** 별도 챕터로 한다. 이전은 **옮기기만** 한다.

이 규칙 때문에 프론트가 재현해야 하는 현행 동작이 셋이다.

| 블록 | 현행 SQL | 프론트가 재현할 것 |
|---|---|---|
| `months` | `HAVING COUNT(*)>=3` | `n >= 3` 필터 |
| `months` | `ORDER BY m LIMIT 10` | 월 오름차순 **앞에서 10개**. 창 수정 후에도 5개 노선이 여기 걸린다(11~12개월) |
| `airlines` | `ORDER BY MIN(price) LIMIT 8` | 최저가 오름차순 **상위 8개** |

`weekdays`에는 **건수 필터가 없다.** 초안에서 `n >= 3`을 적었던 것은 내 착오다 —
현행 `weekday_min`은 `GROUP BY wd`가 전부다. 걸면 `ICN-RMQ`(타이중)가 5개→3개로
**줄어든다**(프론트 실측). `n`은 계약에 남기되 이전 중에는 쓰지 않는다.

`LIMIT 10`이 창인지 임계인지, `weekdays`에 건수 필터가 있어야 하는지는
**이전이 끝난 뒤 판단한다.** 지금은 「현행대로」가 답이다.

> 🔴 **`LIMIT 10`을 건드리는 챕터는 `COPY.md` §6을 같이 처리한다.**
> `fmt_month`가 연도를 버리기 때문에(`'2027-02'` → `'2월'`), 월 버킷 상한을 풀어
> 12개를 넘기면 **막대 두 개가 같은 이름**을 단다(백엔드 BB31).
> 「몇 달치를 보여줄 것인가」와 「그 달을 어떻게 부를 것인가」는 **하나의 결정**이다.

---

## 1) `GET /v1/meta.json`

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "window_days": 30,
  "counts": { "deals": 119, "routes": 36, "origins": 4 },
  "preserved": false,
  "subscribe": {
    "address": "flightpromokr@gmail.com",
    "subject_subscribe": "구독신청",
    "subject_unsubscribe": "구독취소",
    "route_token": "{code}"
  }
}
```

| 키 | 타입 | 의미 |
|---|---|---|
| `window_days` | int | 통계 분석 창. 백엔드가 정한다 |
| `counts` | object | 이번 생성분의 건수 |
| `preserved` | bool | `true`면 **하한선 미달로 갱신을 건너뛰었다**(BB1) — 데이터가 어제 것이다 |
| `subscribe` | object | 구독 메일 규약. 아래 참조 |

### 🔴 `subscribe` — 표시가 아니라 전선(wire) 규약이다

구독 CTA는 `mailto:` 링크다. 초안은 이걸 「프론트가 파생」으로 넘겼는데 **틀렸다.**
이 넷은 **백엔드 IMAP 파서가 실제로 읽는 값**이다(`subscriptions.py`).

| 필드 | 정본 | 파서 |
|---|---|---|
| `address` | 구독 메일함 주소 | `subscriptions.py`가 IMAP으로 로그인해 읽는 바로 그 메일함 |
| `subject_subscribe` | `"구독신청"` | `:23` `SUBSCRIBE` — 제목에 이 문자열이 있어야 구독으로 판정 |
| `subject_unsubscribe` | `"구독취소"` | `:24` `UNSUBSCRIBE` |
| `route_token` | 본문에 넣을 노선 코드 | `:25` `ROUTE_RE = ([A-Z]{3})\s*[-→~]\s*([A-Z]{3})` |

**⚠️ 틀리면 조용한 정도가 아니라 반대로 동작한다.**
`_extract_route()`가 노선을 못 찾으면 `None`을 반환하고,
`load_subscribers()`가 `route or "ALL"`로 받는다(`subscriptions.py:61`).
→ **프론트가 본문 형식을 바꾸면 구독 실패가 아니라 「전 노선 구독」이 된다.**
사용자는 도쿄만 신청했는데 36노선 메일을 받는다. **실패보다 나쁘다.**

> 왜 「실패보다 나쁘다」인가 (프론트 정리): **실패였으면 사용자가 "메일이 안 와요"라고
> 알려주기라도 한다. 이건 반대로 메일이 더 온다 — 신고가 아니라 스팸 신고로 돌아온다.**
> 피해자가 신고자가 되는 실패와 피해자가 침묵하는 실패는 등급이 다르다.

`theme.py`가 프론트로 가면 이 넷을 프론트가 다시 적게 되고 아무도 대조하지 않는다 —
`SITE_URL`에서 R1b로 막은 것과 **같은 함정이 하나 더 있는 것**이다(백엔드 발견).
계약에 실으면 **R1 상태 점검이 어차피 `meta.json`을 받으므로 점검 대상이 공짜로 는다.**

공개 연락처라 PII가 아니고 이미 전 페이지에 노출돼 있다.
문구(「아래 버튼을 누르면…」·해지 안내)는 `COPY.md`가 정본이다 — **규약만** 백엔드가 낸다.

**이 응답이 감시 지점이다.** 백엔드 크론 마지막 「상태 점검」이 `$SITE_URL/v1/meta.json`을
받아 `generated`가 오늘 것인지 본다 — 주소가 틀리면 404, 배포가 멈췄으면 어제 날짜다
(`SPLIT.md` R1·R1b).

## 2) `GET /v1/deals.json`

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "origins": { "SEL": {"name":"서울","lat":37.55,"lon":126.99} },
  "deals": [ { "...deal..." } ]
}
```

**`deal` 객체는 현행과 완전히 같다.** 위 「deal 객체」 절이 그대로 정본이다.
바뀐 것은 봉투뿐 — `updated`(문장) → `generated`(ISO 8601 + 오프셋).

### v1에서 일부러 안 바꾼 것

`nights`(`"8박9일"`) · `when`(`"이번 주"`) · `links[].tag`(`"전체 비교"`)는 **완성된 문장**이라
P7대로면 프론트가 만들어야 한다. **v1에서는 안 옮긴다.**

- 이전의 관문은 `SPLIT.md` M2 T5 **「새 화면 37장이 현 배포본과 diff 0」**이다.
  같은 작업에서 deal 스키마까지 바꾸면 **diff가 나왔을 때 그게 이전 탓인지 스키마 탓인지
  구분할 수 없다.** 한 번에 하나씩 바꾼다.
- `when`은 자유 문장이 아니라 **통제 어휘**(위 §`when` 통제 어휘)라 급하지 않다.
- 이전이 끝난 뒤 별도 챕터에서 옮긴다. **놓친 게 아니라 미룬 것이다.**

## 3) `GET /v1/routes/index.json`

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "routes": [
    { "code":"ICN-FUK", "o":"ICN", "d":"FUK",
      "o_name":"인천", "d_name":"후쿠오카", "region":"jp", "cheapest":158000 }
  ]
}
```

| 필드 | 타입 | 의미 |
|---|---|---|
| `code` | string | `{출발}-{도착}`. **URL 키**이자 `routes/{code}.json`의 이름 |
| `o` `d` | string | ⚠️ **실제 공항 코드**다(`ICN`·`GMP`·`PUS`). deal의 `o`(허브 `SEL`)·`d`(도시 코드)와 **다른 네임스페이스**다 |
| `o_name` `d_name` | string | 도시 표시명. **참조 데이터**라 백엔드가 준다(deal의 `ko`와 같은 성격) |
| `region` | string | 지역 코드. 표시명은 `COPY.md` §2b가 정본 — 백엔드는 코드만 준다 |
| `cheapest` | int\|null | 최근 30일 최저가(KRW). 노선 목록 정렬·표시용 |

**정렬**: `config.ROUTES` 순서를 그대로 유지한다. 프론트가 필요한 순서로 다시 정렬한다.
**`o_name`·`d_name`을 붙여 `"인천 → 후쿠오카"`를 만드는 건 프론트다** — 화살표는 문장이다.

## 4) `GET /v1/routes/{code}.json`

노선 페이지의 원천 전부. 현행 `route_page()`가 sqlite에서 뽑던 값과 **1:1 대응**한다.

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "code": "ICN-FUK",
  "o": "ICN", "d": "FUK",
  "o_name": "인천", "d_name": "후쿠오카", "region": "jp",
  "window_days": 30,

  "summary":  { "cheapest": 158000, "median": 214000, "n": 412 },

  "trend":    [ {"date":"2026-08-12", "price":158000} ],

  "months":   [ {"m":"2026-09", "price":158000, "n":42} ],

  "weekdays": [ {"wd":1, "price":161000, "n":58} ],

  "airlines": [ {"code":"KE", "name":"대한항공", "min":158000, "n":42} ]
}
```

### 블록별 규칙

| 블록 | 현행 함수 | 창(백엔드) | 임계(프론트) |
|---|---|---|---|
| `summary` | `route_summary` | 최근 30일 전체 | — |
| `trend` | `daily_min(…, 30)` | 최근 30일, **날짜 오름차순** | `length >= 2` |
| `months` | `month_min` | 🔴 **`depart_date >= 오늘`**, 출발월 기준 | `length >= 2` + `n >= 3` + 앞에서 10개 |
| `weekdays` | `weekday_min` | **전 기간**, 출발요일 기준 (요일은 순환한다) | `length >= 2` **만** |
| `airlines` | `airline_min` | 최근 30일 | 표시 상위 8개 |

**🔴 백엔드가 바꿔야 할 것 (M1)**

1. `month_min`의 `HAVING COUNT(*)>=3`과 `LIMIT 10`을 **뺀다.** 전 버킷을 `n`과 함께 낸다.
   (프론트가 임계로 재현한다 — 위 「이전 중에는 동작을 바꾸지 않는다」)
2. `weekday_min`에 `COUNT(*)`를 **추가**한다. 지금은 안 센다.
3. `airline_min`의 `LIMIT 8`을 **뺀다.** 상위 8개는 표시 결정이라 프론트가 자른다.
4. `daily_min`의 30일 창은 **유지**한다. 이건 임계가 아니라 창이다.
5. `route_summary`는 이미 `(최저가, 중앙값, 표본수)`를 낸다 — **나머지를 여기 맞추는 것**이다.
6. 🔴 `month_min`에 **출발일 창 `depart_date >= 오늘`을 넣는다.** 지금은 창이 아예 없다.
   **1번과 방향이 반대다** — 1번은 빼고 6번은 넣는다. 헷갈리지 말 것.

> ⚠️ **6번은 이전이 아니라 라이브 결함 수정이다**(`BACKEND.md` BB29).
> 2026-09-08 배포본이 **이미 지난 달에 떠나라고 말하고 있다** — `ICN-KUL` 「7월 출발이
> 가장 저렴합니다」, `ICN-GUM`·`ICN-HKG`·`ICN-LAX`·`ICN-NGO` 「8월 출발」. 오늘은 9월 8일이다.
> `offers.depart_date`가 2026-07-09부터라 **이력이 두 달을 넘긴 이번 달부터 나타났다** —
> 시간이 지나야 드러나는 종류다.
> **임계로는 못 거른다.** `ICN-NRT`의 2026-08 버킷은 **915건**이라 `n >= 3`을 가뿐히 통과한다.
> 창으로만 잡히고, 창은 백엔드다 — 「창은 백엔드, 임계는 프론트」의 첫 시험이었고
> 규칙이 옳은 쪽을 가리켰다.
> **위 「동작 불변」 규칙대로, 이건 M1이 아니라 이전 전에 현행 코드에서 고친다.**
> 그래야 M2 T5의 비교 기준이 옳은 화면이 된다.

### 필드 주의

- **`n`은 버킷 안의 건수다.** 배열 길이가 아니다. 배열 길이는 프론트가 `length`로 안다 —
  같은 사실을 두 곳에 두면 갈라지므로 **배열마다 표본 수를 따로 넣지 않는다.**
- **`trend`에는 `n`이 없다.** 하루 = 한 점이고, 「이 날은 표본이 얇다」는 판단을 우리가
  하지 않기 때문이다. 필요해지면 그때 추가한다(필드 추가는 안전).
- **`wd`는 `0=일요일 … 6=토요일`**(SQLite `strftime('%w')` 규약)다. 🔴 배열은
  **월요일부터** 정렬되어 오지만 **순서를 믿지 말고 `wd`를 읽는다.** 요일명은 `COPY.md`.
- **`m`은 `"YYYY-MM"`**이다. `"9월"`은 프론트가 만든다.
- `airlines[].name`은 `labels.py`의 항공사 표시명 — **참조 데이터**라 백엔드가 준다.
- `summary.cheapest`·`median`이 `null`이면 표본 0이다. 그런 노선은 **페이지를 만들지 않는다**
  (현행 `route_page()`가 `None`을 반환하는 것과 같다) → `routes/index.json`에도 없다.

### 프론트가 이 응답에서 파생하는 것 (백엔드는 몰라도 됨)

- `"인천 → 후쿠오카"` — `o_name`·`d_name` 결합
- **「언제 가면 싼가」 문장** — `months`·`weekdays`의 최솟값. 🔴 **위 임계를 반드시 적용한다**
  (BB28). 버킷이 하나뿐이면 문장을 쓰지 않는다
- 가격 포맷(`158000` → `158,000원`), 월 표기(`"2026-09"` → `"9월"`), 요일명
- 스파크라인·라인차트·막대차트 SVG
- 빵부스러기의 지역 표시명(`region` → `COPY.md` §2b)
- `<title>`·`description`·OG·JSON-LD — **사이트 주소를 아는 건 프론트뿐이다**(P2)
- 구독 `mailto:` — **문구는 프론트가, 규약은 `meta.json`의 `subscribe`를 그대로 쓴다.**
  주소·제목·노선 표기를 프론트가 지어내면 구독이 「전 노선」으로 잘못 걸린다(위 §1 참조)
- `dateModified`(JSON-LD) — `generated`의 UTC 날짜. 빌드 시각이 아니라 **데이터 생성 시각**이다
