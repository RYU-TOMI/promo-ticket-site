# CONTRACT.md — 프론트↔백엔드 데이터 계약

> **이 문서가 프론트와 백엔드 사이의 유일한 인터페이스다.**
> 백엔드는 이 형식대로 `docs/data/deals.json`을 **생산**하고, 프론트는 이 형식만 믿고 **소비**한다.
> 서로의 코드를 읽지 않아도 이 문서만으로 각자 작업할 수 있어야 한다.

## 변경 규칙 (중요)
1. 스키마를 바꾸려면 **이 문서를 먼저 갱신**하고 커밋한 뒤, 양쪽 세션에 알린다.
2. **필드 추가는 안전**(프론트가 무시하면 됨). **필드 삭제·이름 변경·의미 변경은 파괴적** → 반드시 사전 합의.
3. 프론트는 **모든 필드에 방어적으로** 접근한다(`dl.links || []`). 백엔드 데이터가 없거나 0건인 날도 화면이 깨지면 안 된다.
4. 프론트는 커밋된 `docs/data/deals.json`을 **픽스처**로 쓴다 → 백엔드를 기다리지 않고 개발 가능.

---

## 파일
- 경로: `docs/data/deals.json`
- 생산자: `collector/discover_data.py` → `build_deals_json(conn)` (`build_site.py`가 호출)
- 소비자: `docs/assets/discover.js` (+ `collector/discover_home.py`가 HTML에 인라인)
- 인코딩: UTF-8, 압축 없음(`separators=(",",":")`)

## 최상위 구조
```json
{
  "updated": "2026-08-06 00:15",
  "origins": { "SEL": {"name":"서울","lat":37.55,"lon":126.99}, ... },
  "deals": [ { ...deal... }, ... ]
}
```

| 키 | 타입 | 의미 |
|---|---|---|
| `updated` | string | 생성 시각 `YYYY-MM-DD HH:MM` (KST) |
| `origins` | object | 출발 허브 맵. 키 = 허브코드, 값 = `{name, lat, lon}`. **딜이 있는 허브만** 포함 |
| `deals` | array | 딜 목록. **가격 오름차순** 정렬 |

### 출발 허브 코드 (`origins` 키 / `deal.o`)
`SEL`(서울=인천+김포 통합) · `PUS`(부산) · `TAE`(대구) · `CJU`(제주)
> ⚠️ `SEL`은 **실제 공항 코드가 아님**(집계용 가상 허브). 예약 링크에는 실제 공항(ICN/GMP)이 들어간다 — 백엔드가 처리하며 프론트는 신경 쓰지 않는다.

## deal 객체

| 필드 | 타입 | 예시 | 의미 |
|---|---|---|---|
| `o` | string | `"SEL"` | 출발 허브 코드 (`origins`의 키) |
| `d` | string | `"CJU"` | 목적지 IATA 공항 코드 |
| `ko` | string | `"제주"` | 목적지 한글 도시명 (**화면 표시용**) |
| `country` | string | `"한국"` | 국가 한글명 |
| `region` | string | `"dom"` | 지역 코드 (`jp`/`cn`/`sea`/`island`/`oc`/`eu`/`am`/`etc`/`dom`) |
| `haul` | string | `"short"` | 거리대 — `short`(근거리) / `mid`(중거리) / `long`(장거리) |
| `tier` | string | `"major"` | LOD 등급 — `major`(축소에도 표시) / `minor`(확대 시 표시) |
| `tags` | string[] | `["해변","자연"]` | 분위기 태그(통제 어휘). **필터·사진 색 선택에 사용** |
| `lat` `lon` | number | `33.51`, `126.49` | 목적지 공항 위경도 (**지도 핀 좌표**) |
| `price` | number | `42522` | **발견가**(KRW, 왕복 총액). 화면에선 "발견가 · 스캔 시점"으로 표기 |
| `transfers` | number | `0` | 경유 횟수 (0=직항) |
| `dep` | string | `"2026-09-16"` | 출발일 `YYYY-MM-DD` |
| `ret` | string\|null | `"2026-09-17"` | 귀국일. 편도면 `null` |
| `nights` | string | `"1박2일"` | 표시용 기간 문자열. 편도/불명이면 `""` |
| `median` | number | `59319` | 해당 노선 **평소 시세(중앙값)**. "평소 시세 vs 발견가" 막대에 사용 |
| `discount` | number | `28` | 중앙값 대비 할인율(%). 0~70로 클램프. 할인 없으면 `0` |
| `when` | string | `"다음 달"` | 상대 날짜 라벨 — `이번 주` / `이번 주말` / `다음 달` / `N월` |
| `links` | object[] | 아래 참조 | **예약처 비교 링크** |

### `links[]` — 비교 패널
```json
{ "name": "스카이스캐너", "tag": "전체 비교", "url": "https://..." }
```
| 필드 | 의미 |
|---|---|
| `name` | 예약처 표시명 (프론트가 그대로 출력) |
| `tag` | 성격 라벨. **현재 프론트는 미표시**(중립성 위해 숨김). 필드는 유지 |
| `url` | 노선·날짜가 프리필된 딥링크. 프론트는 `target="_blank" rel="noopener sponsored"` 로 연다 |

- 순서 = 화면 노출 순서. 현재: 스카이스캐너 · 네이버 항공권 · 구글 항공권 · Trip.com
- **Aviasales(영어)는 `TP_MARKER` 환경변수가 있을 때만** 배열에 추가된다(수수료 발생 시에만 노출).
- 개수는 **가변**(3~5). 프론트는 개수를 가정하지 말고 반복 렌더할 것.

## 데이터 정책 (백엔드가 보장)
- **게이팅**: `dests.DEST` 사전에 있고 좌표가 있는 목적지만 포함(롱테일 쓰레기 제외).
- **신선도**: `broad_offers` 최근 3일 수집분 + **미래 출발**만.
- **중복 제거**: `(허브, 도시)` 단위로 **최저가 1건**만.
- 데이터가 0건인 날도 `{"updated":..., "origins":{}, "deals":[]}` 형태는 유지된다.

## 프론트가 하는 파생 (백엔드는 몰라도 됨)
`discover.js`가 deal → 화면용으로 변환(`toCity`)하며 다음을 **자체 계산**한다:
- 가격 포맷(`42522` → `42,522`), 날짜 표기(`9/16(수)~9/17(목)`)
- "왜 지금" 훅 문구(`discount`/`transfers`/`when` 기반 규칙)
- 태그 → 사진 그라디언트, `haul` → 지도 단계(near/sea/far)
- 핀 색 농도(현재 화면에 보이는 딜들 사이의 상대 가격)

> 이 파생 로직은 **프론트 소유**다. 백엔드는 위 원천 필드만 정확히 채우면 된다.
